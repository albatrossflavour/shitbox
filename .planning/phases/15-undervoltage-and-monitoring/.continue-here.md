---
context: phase
phase: 15-undervoltage-and-monitoring
task: 0
total_tasks: 5
status: wave-1-complete
last_updated: 2026-04-24T08:42:35.950Z
---

# Phase 15 — Paused After Wave 1, Before 15-05 Health Page

4/5 plans done. Only 15-05 (Health page UI) remains. Branch: `gsd/phase-15-undervoltage-and-monitoring`.

## Where we are

| Plan | Status | What landed |
|------|--------|-------------|
| 15-01 | ✓ | `src/shitbox/health/alerts.py` helper with two-counter sustain (active + clear), `speak_power_restored` TTS, 10 tests |
| 15-02 | ✓ | `thermal_monitor.py:297` sticky-bit bug fixed via `raw & 0xf` + alerts helper; 6 regression tests |
| 15-03 | ✓ | `ring_buffer._health_monitor` fires CAPTURE_FAILURE / CAPTURE_DOWN / CAPTURE_RESTORED; 4 tests |
| 15-04 | ✓ | MON-01/02 checkboxes flipped; home-ops `shitbox-mqtt-exporter/` deleted + pushed (`0b3dadd1`) |
| 15-05 | pending | D-13 Health page (sse.py + index.html + tests/test_dashboard.py) |

39/39 tests pass on the phase branch (alerts + thermal + ffmpeg modules).

## Next action (cold-start safe)

```
/clear
/gsd-execute-phase 15
```

Only Wave 2 (15-05) remains; the workflow will skip the 4 plans with SUMMARY.md.

## What 15-05 actually does (plain terms)

Three changes in one plan, all UI-focused:

**Backend (`src/shitbox/dashboard/sse.py`)**
Add `_system_conditions_payload()` returning a list of exactly 3 dicts (one per system role: `undervoltage`, `thermal`, `capture`), each with exactly 5 scalar fields: `role, label, tier, state, since_ms`. Wire it into the `/sse/slow` payload as `system_conditions`. State derivation from `alerts.snapshot()`: `fired && active → "active"`, `fired && !active → "restored"`, else `"clear"`. `since_ms = (now - last_change_ts) × 1000`.

**Frontend (`src/shitbox/dashboard/static/index.html`)**
- Rename Hardware modal title to "Health" (line 386)
- Add a SYSTEM section ABOVE the existing HARDWARE list, rendered via Alpine `x-for` bound to `systemConditions` (mirror `sortedHardware()` at index.html:396-404 — do NOT use `getElementById` + `innerHTML`)
- Reuse `.hw-row.hw-row-lg` scaffold; each row gets an `sc-{state}` class
- `hwBadgeClass` returns `bg-red-700` if any `systemConditions` row has `state="active"`
- `showAlert` branches green (`#238636`, 3000ms) when subtype `endsWith('_CLEARED')` or `endsWith('_RESTORED')`; otherwise red (`#da3633`, 10000ms)

**Tests (`tests/test_dashboard.py`)**
Assert payload shape (5 fields, 3 always-emit rows in order undervoltage/thermal/capture, tier='critical'). Assert state derivation matches the rule above.

## Required reading on resume

1. `.planning/phases/15-undervoltage-and-monitoring/15-05-PLAN.md` — full task breakdown
2. `.planning/phases/15-undervoltage-and-monitoring/15-UI-SPEC.md` — locked design contract
3. `.planning/phases/15-undervoltage-and-monitoring/15-PATTERNS.md` — Alpine + sse.py analog mappings (file:line anchors)
4. `src/shitbox/health/alerts.py` (`snapshot()` is the API the SSE payload reads from)
5. `src/shitbox/dashboard/static/index.html:380-410` — existing hardware rendering pattern to mirror

## Critical anti-patterns

| Pattern | Severity | Mechanism |
|---------|----------|-----------|
| Vanilla `getElementById` + `innerHTML` for SYSTEM rows | advisory | UI-SPEC + 15-PATTERNS.md require Alpine `x-for` mirroring `sortedHardware()` |
| Adding extra payload fields beyond the locked 5 | advisory | UI-SPEC contract; tests assert exact field set |
| Replanning AlertStatus shape | advisory | Already locked in 15-01 (two counters) — do not collapse |
| Touching home-ops without checkpoint | n/a | Already done in 15-04, no further home-ops work this phase |

## Quota / execution mode

15-02 and 15-03 ran out of subagent quota mid-execution and were finished inline. If quota is constrained on resume, run 15-05 inline; otherwise spawn a `gsd-executor` agent in a worktree (this is the recommended path — UI work benefits from a fresh context window).

## Working tree state

Clean. All 4 wave commits + 4 SUMMARY commits + 4 merge commits + 1 home-ops push committed.

## Phase completion checklist (after 15-05)

1. Run `gsd-verifier` for phase completion check
2. Run `gsd-code-review` (gate in execute-phase workflow)
3. Update ROADMAP.md (`gsd-sdk query roadmap.update-plan-progress` for each plan)
4. Update STATE.md (mark phase 15 COMPLETE)
5. On-Pi UAT: force undervoltage briefly, confirm overlay + TTS + Health page sticky state
6. Merge `gsd/phase-15-undervoltage-and-monitoring` → main

## Resume

```
/clear
/gsd-execute-phase 15
```
