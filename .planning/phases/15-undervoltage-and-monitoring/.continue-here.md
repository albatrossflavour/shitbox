---
context: phase
phase: 15-undervoltage-and-monitoring
task: 0
total_tasks: 0
status: pre-discuss
last_updated: 2026-04-24T00:43:40Z
---

# Phase 15 — Paused Before discuss-phase

Paused because prior session hit 91% context after a hard compaction. Phase 15 directory exists; no CONTEXT.md yet.

## Phase Order Locked
Sequence for the v2.0 remaining work: **15 → 17 → 21**. User confirmed.

Why this order: Phase 15 wires the undervoltage + thermal + capture-failure alerts into the SSE stream. Phase 17-02 (kiosk rework) is the downstream consumer of those alert overlays, so 15 lands the payloads 17 needs. Phase 21 then formalises the whole hardware-state story and will retrofit 15's three ad-hoc alerts into `HardwareState`.

## Requirements (from REQUIREMENTS.md §51-58)

- **PWR-01** Undervoltage hardware fix confirmed; software reads throttled bitmask bits 0-3 only, ignoring sticky historical bits 16-19
- **PWR-02** Undervoltage alert surfaces visibly on dashboard + spoken TTS
- **MON-01** HLTH-01 closed — CPU temp, disk %, sync backlog reach Prometheus end-to-end; fix `insert_readings_batch` cpu_percent bug
- **MON-02** Prometheus scrape-job label conflict resolved (`shitbox-mqtt-exporter` vs `shitbox` metrics)
- **MON-03** Critical events (thermal, undervoltage, capture-failure) visible on the live dashboard

## Gray Areas Staged for discuss-phase

1. **Throttled-bitmask read pattern** — mask-on-read vs compare-on-write; what's an "alert" vs a continuous state; de-dup cadence
2. **Alert fan-out shape** — extend existing SSE `alert` channel or add a new `system_event` channel? Message shape.
3. **cpu_percent bug scope** — fix only, or fix + relabel cleanup? Blast radius check against existing Prometheus queries/Grafana dashboards.
4. **Prometheus scrape conflict** — rename `shitbox-mqtt-exporter` job, or relabel at scrape time? Effect on historical series.
5. **Phase 21 coupling seam** — introduce a minimal `system_state` dict now as the migration seam, or hard-code three alert paths and retrofit under `HardwareState` in Phase 21?
6. **TTS wording + cadence** — once-only, repeat every N minutes, or repeat until acknowledged? Consistency with Phase 5 TTS patterns and Phase 21 D-05 (critical tier "repeated TTS until acknowledged").

## Prior-Phase Decisions to Respect

- **Phase 21 D-04** daemon boots regardless of missing hardware — alert loudness differs per tier. Phase 15 should follow the same "never refuse boot" discipline.
- **Phase 21 D-05** criticality mapping is already sketched; Phase 15's undervoltage/thermal alerts should map cleanly: undervoltage is a *power* concern, thermal is *critical* tier.
- **Phase 21 D-10** status surfaces are Pi-local (OLED + dashboard + TTS). Phase 15 dashboard alerts must honour this — no website widget for system events.
- **Phase 10** already built the SSE pipeline and dashboard alert conventions; reuse not reinvent.

## Code Surfaces Identified (not yet read in depth)

- `src/shitbox/collectors/power.py` — INA226 collector, likely where throttled-bitmask read lands
- `src/shitbox/sync/batch_sync.py` + `prometheus_write.py` — `insert_readings_batch` cpu_percent bug lives here; scrape-job labels set here
- `src/shitbox/dashboard/sse.py` + `snapshot.py` — alert fan-out extension point
- `src/shitbox/utils/tts.py` (or wherever Piper invokes) — TTS wording pattern
- `src/shitbox/events/engine.py` — `UnifiedEngine` where a lightweight `system_state` dict would mount

## Uncommitted Work
None — working tree clean, main pushed.

## Next Action When Resuming

```
/gsd-discuss-phase 15
```

Walk the six gray areas above. Don't let scope creep into Phase 21 territory; the goal is three concrete alert paths (undervoltage, thermal, capture-failure) with a clean seam for Phase 21 to retrofit later.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Running discuss-phase in a compacted session | Session hit 91% ctx before gray-area 1 finished; full interactive flow needs a fresh window | advisory | Start discuss-phase from /clear; never run it after a compaction |

## Infrastructure State
- Pi (`laser` @ 10.10.20.107): shitbox-telemetry healthy; `cpufreq-performance.service` enabled (governor pinned to `performance`)
- Main branch: pushed and clean
