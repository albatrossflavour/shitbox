---
phase: 11-v2-hardware-migration
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - pyproject.toml
  - config/config.yaml
  - deploy/udev/99-shitbox-cameras.rules
autonomous: false
requirements:
  - D-02
  - D-13
  - D-14
  - D-22
  - D-23
  - D-25
must_haves:
  truths:
    - "pyproject.toml lists every v2 sensor library and no v1-only libraries"
    - "pip install -e '.[dev]' resolves cleanly with no version conflicts"
    - "config/config.yaml reflects the v2 sensor set with a canonical brain-note reference comment at the top"
    - "ELP 4K front camera has a udev rule pointing /dev/camera-front at it by VID:PID"
    - "camera_controls section in config is retuned for the ELP (no UGREEN controls remain)"
  artifacts:
    - path: "pyproject.toml"
      provides: "v2 sensor dependency list"
      contains: "adafruit-circuitpython-lsm6ds"
    - path: "config/config.yaml"
      provides: "v2 config with LSM6DSOX/LIS3MDL/DS18B20/VEML7700/SEN0460/INA226 sections"
    - path: "deploy/udev/99-shitbox-cameras.rules"
      provides: "udev rule mapping ELP VID:PID to /dev/camera-front"
  key_links:
    - from: "config/config.yaml"
      to: ".planning/phases/11-v2-hardware-migration/HARDWARE_IDS.md"
      via: "DS18B20 sensor_id strings + ELP VID:PID"
      pattern: "sensor_id:"
    - from: "deploy/udev/99-shitbox-cameras.rules"
      to: "/dev/camera-front symlink"
      via: "ATTRS{idVendor}=... ATTRS{idProduct}=..."
      pattern: "SYMLINK\\+=\"camera-front\""
---

<objective>
Wire the dependency manifest, the v2 config file, and the camera udev rule. This is the
integration point where hardware discovery from wave 0 (HARDWARE_IDS.md) meets the
collectors from waves 1-2 (config wiring) and the camera pipeline (udev rule).

Purpose: D-02, D-13, D-14, D-22, D-23, D-25. Get the Pi 5 to the point where
`pip install -e ".[dev]"` followed by a manual smoke test actually produces symlinks and
data.
Output: Fixed pyproject, new config.yaml, camera udev rule, manual Pi-side verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-v2-hardware-migration/11-CONTEXT.md
@.planning/phases/11-v2-hardware-migration/11-RESEARCH.md
@.planning/phases/11-v2-hardware-migration/HARDWARE_IDS.md
@pyproject.toml
@config/config.yaml
@src/shitbox/utils/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pyproject.toml sensor deps end-to-end</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current deps list)
    - .planning/phases/11-v2-hardware-migration/11-RESEARCH.md (standard stack table)
    - .planning/phases/11-v2-hardware-migration/11-CONTEXT.md D-22
  </read_first>
  <action>
    Open pyproject.toml `[project]` dependencies (or optional-dependencies if sensor libs live there).

    **REMOVE these entries entirely if present:**
    - `adafruit-circuitpython-bme280` (wrong — we use BME680, D-02)
    - `adafruit-circuitpython-mcp9808` (MCP9808 deleted, D-17)
    - `adafruit-circuitpython-ina219` (INA219 deleted, D-16)
    - Any `mpu6050` package reference (D-18)

    **ADD / ENSURE these entries are present with the versions below (from RESEARCH.md standard stack):**
    - `adafruit-circuitpython-bme680>=3.7.15` (keep; fixes D-02 mistake)
    - `adafruit-circuitpython-lsm6ds>=4.6.2`
    - `adafruit-circuitpython-lis3mdl>=1.2.7`
    - `adafruit-circuitpython-veml7700>=2.2.1`
    - `w1thermsensor>=2.3.0`
    - `smbus2` (should already be present — used by INA226 and SEN0460 vendor drivers)

    Do NOT add INA226 or SEN0460 packages — we vendored drivers for those in wave 2.

    After editing run:
    ```bash
    pip install -e ".[dev]"
    pip check
    python -c "import shitbox"
    ```
    All three must succeed.
  </action>
  <verify>
    <automated>pip check && python -c "import shitbox; from shitbox.collectors.temperature import DS18B20Collector; from shitbox.collectors.light import VEML7700Collector; from shitbox.collectors.particulate import SEN0460Collector; from shitbox.collectors.power import INA226Collector; from shitbox.collectors.imu_heading import IMUHeadingCollector; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "adafruit-circuitpython-bme280" pyproject.toml` == 0
    - `grep -c "adafruit-circuitpython-mcp9808" pyproject.toml` == 0
    - `grep -c "adafruit-circuitpython-ina219" pyproject.toml` == 0
    - `grep -q "adafruit-circuitpython-bme680" pyproject.toml` succeeds
    - `grep -q "adafruit-circuitpython-lsm6ds" pyproject.toml` succeeds
    - `grep -q "adafruit-circuitpython-lis3mdl" pyproject.toml` succeeds
    - `grep -q "adafruit-circuitpython-veml7700" pyproject.toml` succeeds
    - `grep -q "w1thermsensor" pyproject.toml` succeeds
    - `pip check` exits zero
    - `python -c "import shitbox"` exits zero
  </acceptance_criteria>
  <done>pyproject.toml aligned with v2 sensor stack, `pip check` clean, `import shitbox` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite config/config.yaml for v2 and add camera udev rule</name>
  <files>config/config.yaml, deploy/udev/99-shitbox-cameras.rules</files>
  <read_first>
    - config/config.yaml (current v1 — rewriting)
    - .planning/phases/11-v2-hardware-migration/HARDWARE_IDS.md (DS18B20 IDs, ELP VID:PID, Brio VID:PID)
    - src/shitbox/utils/config.py (dataclass layout — YAML must match)
    - .planning/phases/11-v2-hardware-migration/11-CONTEXT.md D-23, D-25
  </read_first>
  <action>
    **1. Rewrite config/config.yaml for the v2 sensor set.** Use the dataclass names added in waves 1-2 as the YAML keys.

    **Top of file — canonical reference comment (D-25):**
    ```yaml
    # Shitbox v2 telemetry config
    #
    # Canonical hardware reference: ~/Brain/projects/shitbox-rally-2026.md
    # This file is the CODE's view of the hardware. The brain note is the
    # source of truth for wiring, addresses, and intent. If they disagree,
    # the brain note wins — update this file to match.
    ```

    **Required sections (pull sensor_id values + camera VID/PID from HARDWARE_IDS.md):**

    ```yaml
    imu:   # LSM6DSOX accel+gyro at 0x6A
      enabled: true
      i2c_bus: 1
      address: 0x6A
      sample_rate_hz: 104.0
      accel_offset_x: 0.0
      accel_offset_y: 0.0
      accel_offset_z: 0.0

    magnetometer:   # LIS3MDL at 0x1C
      enabled: true
      i2c_bus: 1
      address: 0x1C

    imu_heading:   # complementary filter output
      enabled: true
      sample_rate_hz: 10.0
      complementary_alpha: 0.98

    environment:   # BME680 at 0x77 (NOT BME280)
      enabled: true
      i2c_bus: 1
      address: 0x77

    temperature:   # DS18B20 1-Wire, 2 probes
      enabled: true
      sample_rate_hz: 1.0
      probes:
        - role: exterior
          sensor_id: "<from HARDWARE_IDS.md>"
        - role: engine_bay
          sensor_id: "<from HARDWARE_IDS.md>"

    light:   # VEML7700 at 0x10
      enabled: true
      sample_rate_hz: 1.0
      i2c_bus: 1
      address: 0x10

    particulate:   # SEN0460 at 0x19 — rail not wired, ships disabled (D-05)
      enabled: false
      i2c_bus: 1
      address: 0x19
      sample_rate_hz: 0.2

    power:   # INA226 at 0x40 — shunt not wired, ships disabled (D-06)
      enabled: false
      i2c_bus: 1
      address: 0x40
      shunt_ohms: 0.1
      sample_rate_hz: 1.0

    oled:   # SSD1306 at 0x3C — optional, not wired in v2 by default
      enabled: false

    camera:
      front:
        enabled: true
        device: /dev/camera-front
        resolution: "1920x1080"
        framerate: 30
        pixelformat: mjpeg
        # ELP 4K IMX317 controls — retuned from UGREEN (D-13)
        controls:
          brightness: 0
          contrast: 32
          saturation: 60
          exposure_auto: 3
      cabin:
        enabled: true
        device: /dev/camera-cabin
        resolution: "1280x720"
        framerate: 30
        # Brio 100 — unchanged from v1 (D-14)
    ```

    Delete any `mpu6050`, `mcp9808`, `ina219`, `bme280` keys. Delete the old UGREEN `camera_controls` block. Keep GPS, sync, storage, capture, buzzer, speaker sections from v1 unchanged.

    Replace `<from HARDWARE_IDS.md>` with the real IDs.

    **2. Create deploy/udev/99-shitbox-cameras.rules:**
    ```
    # Shitbox front camera: ELP 4K IMX317 (D-13)
    SUBSYSTEM=="video4linux", ATTRS{idVendor}=="<VID>", ATTRS{idProduct}=="<PID>", ATTR{index}=="0", SYMLINK+="camera-front"

    # Shitbox cabin camera: Logitech Brio 100 (D-14, unchanged from v1)
    SUBSYSTEM=="video4linux", ATTRS{idVendor}=="<brio-VID>", ATTRS{idProduct}=="<brio-PID>", ATTR{index}=="0", SYMLINK+="camera-cabin"
    ```
    Substitute `<VID>`, `<PID>` etc. from HARDWARE_IDS.md (lowercase 4-digit hex, no `0x`).

    **3. Sanity-check yaml parses:**
    ```bash
    python -c "import yaml; yaml.safe_load(open('config/config.yaml'))"
    ```
    Must exit zero.

    **4. Sanity-check load_config parses into dataclasses:**
    ```bash
    python -c "from shitbox.utils.config import load_config; c = load_config('config/config.yaml'); print(c.imu.address, c.temperature.probes, c.power.enabled)"
    ```
    Must print `106 [DS18B20ProbeConfig(...), ...] False` or similar. If this raises, wave 2 dataclass layout does not match YAML keys — fix config.py or the YAML, not both.
  </action>
  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('config/config.yaml'))" && python -c "from shitbox.utils.config import load_config; c = load_config('config/config.yaml'); assert c.imu.address == 0x6A; assert c.power.enabled is False; assert c.particulate.enabled is False; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Canonical hardware reference" config/config.yaml` succeeds (D-25)
    - `grep -q "shitbox-rally-2026.md" config/config.yaml` succeeds
    - `grep -c "mpu6050\|mcp9808\|ina219\|bme280" config/config.yaml` == 0
    - `grep -q "0x6A" config/config.yaml` succeeds (LSM6DSOX address)
    - `grep -q "0x1C" config/config.yaml` succeeds (LIS3MDL address)
    - `grep -q "0x77" config/config.yaml` succeeds (BME680 address)
    - `grep -q "engine_bay" config/config.yaml` succeeds
    - `grep -q "exterior" config/config.yaml` succeeds
    - `grep -E "sensor_id: \"28-" config/config.yaml | wc -l` >= 2 (real IDs, not placeholders)
    - File `deploy/udev/99-shitbox-cameras.rules` exists
    - `grep -q "SYMLINK+=\"camera-front\"" deploy/udev/99-shitbox-cameras.rules` succeeds
    - `grep -q "SYMLINK+=\"camera-cabin\"" deploy/udev/99-shitbox-cameras.rules` succeeds
    - `python -c "import yaml; yaml.safe_load(open('config/config.yaml'))"` exits zero
    - `python -c "from shitbox.utils.config import load_config; load_config('config/config.yaml')"` exits zero
  </acceptance_criteria>
  <done>config.yaml parses, loads into dataclasses, no v1 sensor keys remain, camera udev rule present with real VID:PIDs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Pi 5 smoke test — install udev rule, check symlinks, verify BME680 + I2C bus</name>
  <what-built>
    pyproject.toml aligned, config.yaml rewritten, camera udev rule created. Waves 1-2
    collectors exist and are unit-tested but have not been exercised against real hardware.
  </what-built>
  <action>See how-to-verify block below for concrete SSH commands and expected outputs.</action>
  <how-to-verify>
    SSH to Pi 5 (10.10.20.107, services stopped per project_pi5_setup.md). Do NOT start
    the engine — physical cutover is explicitly out of scope for this phase.

    **1. Sync code to Pi 5:**
    ```bash
    rsync -av --exclude .git --exclude __pycache__ ~/dev/shitbox/ tgreen@10.10.20.107:~/shitbox/
    ssh tgreen@10.10.20.107 'cd ~/shitbox && pip install -e ".[dev]"'
    ssh tgreen@10.10.20.107 'cd ~/shitbox && pip check'
    ```
    Expected: pip check clean, no version conflicts.

    **2. Install udev rule:**
    ```bash
    ssh tgreen@10.10.20.107 'sudo cp ~/shitbox/deploy/udev/99-shitbox-cameras.rules /etc/udev/rules.d/'
    ssh tgreen@10.10.20.107 'sudo udevadm control --reload-rules && sudo udevadm trigger'
    ssh tgreen@10.10.20.107 'ls -la /dev/camera-front /dev/camera-cabin'
    ```
    Expected: both symlinks exist and point at real /dev/videoN devices.

    **3. Probe ELP for supported format:**
    ```bash
    ssh tgreen@10.10.20.107 'v4l2-ctl -d /dev/camera-front --list-formats-ext | head -40'
    ```
    Expected: MJPG 1920x1080 30fps listed.

    **4. Verify BME680 (not BME280) detected at 0x77:**
    ```bash
    ssh tgreen@10.10.20.107 'i2cdetect -y 1'
    ssh tgreen@10.10.20.107 'python -c "import board, busio, adafruit_bme680; i2c=busio.I2C(board.SCL,board.SDA); bme=adafruit_bme680.Adafruit_BME680_I2C(i2c); print(\"temp=\", bme.temperature, \"hum=\", bme.humidity)"'
    ```
    Expected: i2cdetect shows 0x77, BME680 reads plausible temperature (15-35°C).

    **5. Verify LSM6DSOX at 0x6A and LIS3MDL at 0x1C:**
    ```bash
    ssh tgreen@10.10.20.107 'python -c "import board, busio; from adafruit_lsm6ds.lsm6dsox import LSM6DSOX; from adafruit_lis3mdl import LIS3MDL; i2c=busio.I2C(board.SCL,board.SDA); print(\"accel=\", LSM6DSOX(i2c).acceleration); print(\"mag=\", LIS3MDL(i2c).magnetic)"'
    ```
    Expected: accel tuple with |az| ≈ 9.81 m/s² (Pi flat), mag tuple with non-zero values.

    **6. Verify DS18B20 probes match the IDs in HARDWARE_IDS.md:**
    ```bash
    ssh tgreen@10.10.20.107 'python -c "from w1thermsensor import W1ThermSensor; [print(s.id, s.get_temperature()) for s in W1ThermSensor.get_available_sensors()]"'
    ```
    Expected: both probe IDs match HARDWARE_IDS.md, both report plausible temperatures.

    Record any discrepancies.
  </how-to-verify>
  <resume-signal>
    Type "verified" if all six checks pass, or describe failures.
  </resume-signal>
  <acceptance_criteria>
    - User confirms symlinks /dev/camera-front and /dev/camera-cabin exist
    - User confirms ELP lists MJPG 1920x1080 30fps
    - User confirms BME680 read succeeded (temperature value reported)
    - User confirms LSM6DSOX and LIS3MDL both respond via i2c
    - User confirms both DS18B20 IDs match HARDWARE_IDS.md
    - User confirms `pip check` was clean
  </acceptance_criteria>
  <done>Pi 5 has the v2 code installed, udev rules active, and every v2 sensor the rail powers responds via its library. Engine stays stopped — cutover is a separate op.</done>
</task>

</tasks>

<verification>
- `pip check` clean on dev laptop AND Pi 5
- `python -c "from shitbox.utils.config import load_config; load_config('config/config.yaml')"` exits zero
- User has confirmed Pi-side smoke test passed
</verification>

<success_criteria>
pyproject + config + udev all aligned with v2 hardware. Pi 5 can install the code and detect every powered sensor.
</success_criteria>

<output>
Create `.planning/phases/11-v2-hardware-migration/11-03-SUMMARY.md` with: deps added/removed, config.yaml diff summary, udev rule contents, Pi-side smoke test results.
</output>
