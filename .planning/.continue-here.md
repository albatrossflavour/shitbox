---
context: default
phase: between-phases
status: phase-27-shipped-awaiting-next-pick
last_updated: 2026-04-23T23:57:33.730Z
---

<current_state>
Phase 27 (Slate Visual Theming) fully shipped: 2/2 plans, on-Pi UAT passed 2026-04-24 ("videos look good, loving the layout"), plus a post-UAT tweak dropping the coloured event-type badge fill per user feedback. Branch `gsd/phase-27-slate-visual-theming` merged to `main` via `--no-ff` merge commit `3b4982d`. Working tree clean. Local `main` is 9 commits ahead of `origin/main`, not yet pushed. Three open threads to pick from next session.
</current_state>

<completed_work>
- Wave 1 — vendored Cinzel Bold/Regular (SIL OFL 1.1) under `src/shitbox/capture/assets/cinzel/`, extended `pyproject.toml` package-data glob. Commit `a0ff8df`.
- Wave 2 — swapped `title_card.py` font constants to Cinzel (Bold hero / Regular meta rows), ALL CAPS hero, AU state suffix dropped (`Narellan, New South Wales` → `NARELLAN`). Commit `dbdb634`.
- Code review — 0 critical, 1 warning (OFL Reserved Font Name hygiene), 3 info. Commit `7b53e0c`.
- Verification — 5/5 success criteria satisfied in code; status `human_needed` pending on-Pi UAT. Commit `68b866a`.
- User on-Pi UAT — 2026-04-24 approval: "videos look good, loving the layout".
- Post-UAT tweak — dropped coloured badge fill + ROLLOVER stripe overlay; event-type label now plain white text at same anchor. Removed dead `_hex_to_rgb` + `ROLLOVER_STRIPE_COLOUR` import. Commit `2eb7711`.
- Phase close — STATE.md hand-fixed (SDK corruption bug: `milestone_complete` → `in_progress`, 9/13 → 10/13 phases), ROADMAP checkbox flipped. Commit `c630b0c`.
- Merge — `gsd/phase-27-slate-visual-theming` → `main` via `--no-ff`. Commit `3b4982d`.
</completed_work>

<remaining_work>
- **Push `main` to `origin/main`** — 9 commits ahead locally. User greenlit ship but push not asked. Quick check before pushing.
- **Pi 5 firmware mailbox hang** — separate `/gsd-debug` investigation. First seen 2026-04-24 09:34:31, coincident with Phase 27 deploy but platform-level, not a typography regression. Palliative: pin cpufreq governor to `performance`. Failure mode: `rpi_firmware_property_list` timeout → `brcmfmac` WiFi fail → xHCI dead → systemd 10s watchdog kills `shitbox-telemetry`.
- **Next open phases** — 14 (Sensor Calibration), 15 (Undervoltage), 16 (Video Capture Tuning), 20 (Physical Integration, blocked on prints), 24 (Phase 20 gap closure, blocked on prints), 25 (Nyquist validation sweep, queued after 24).
- **Non-blocking code-review follow-ups** — WR-01 OFL.txt Reserved Font Name says "Cinzel Decorative" (should be "Cinzel"); IN-01 `AU_STATE_ABBREVIATIONS` and `_abbreviate_au_states` now dead helpers (state suffix is dropped, not abbreviated).
</remaining_work>

<decisions_made>
- Scope Phase 27 to typography-only — palette, badge composition, coord-row DejaVu Mono all preserved. Broader theming deferred.
- Drop AU state suffix entirely (`Narellan, NSW` → `NARELLAN`) rather than abbreviating — user wanted a ranked-location feel at hero weight.
- Bundle Cinzel TTFs in-tree under SIL OFL 1.1 via `pyproject.toml` package-data glob — no `fonts-cinzel` apt package exists on Raspberry Pi OS.
- Drop coloured badge fill + ROLLOVER stripe overlay after UAT — kept `_draw_badge` signature stable (colour/is_rollover params explicitly `del`-ed) so callers are zero-touch.
- Merge via `--no-ff` — preserves the per-wave + gap-closure commit history, matches the Phase 18/26 precedent.
</decisions_made>

<blockers>
None structural. Pi 5 firmware hang is an operational concern but does not block forward work on other phases.
</blockers>

## Required Reading (in order)

1. `.planning/STATE.md` — current position (Phase 27 complete; 10/13 phases). The `Current Position` block lists what's done and what's next.
2. `.planning/ROADMAP.md` §Phase 14/15/16/20/24/25 — open phases, pick goals/success criteria before specing.
3. `.planning/phases/27-slate-visual-theming/27-VERIFICATION.md` — what Phase 27 actually shipped so a future phase does not re-scope typography.
4. `~/Brain/projects/shitbox-rally-2026.md` — live project state; update "Active Now" if starting a new phase.

## Infrastructure State

- `main` branch: `3b4982d` (local), `origin/main` 9 commits behind
- Phase 27 branch: `gsd/phase-27-slate-visual-theming` still exists locally at `c630b0c`, not yet deleted
- Pi (`laser`, 10.10.20.107): running Phase 27 code; manual capture at 09:38:17 UTC 2026-04-24 produced `/var/lib/shitbox/captures/2026-04-24/manual_capture_093817_001.mp4`
- Pi 5 firmware: suspect `cpufreq` ondemand governor racing with brcmfmac WiFi mailbox — keep an eye on `/var/log/kern.log` for repeat of 2026-04-24 09:34:31 failure pattern
- Tests: 496 passed, 1 skipped, green on `main` after merge

<next_action>
Pick one:
- `git push origin main` if ready to publish Phase 27 (check with user first)
- `/gsd-debug` for the Pi 5 firmware mailbox hang (platform stability before rally)
- `/gsd-discuss-phase 14` (or 15/16/20/24/25) to spec the next phase
</next_action>
