---
phase: 12-schema-foundation-and-logbook-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shitbox/storage/database.py
  - tests/test_database.py
  - tests/test_logbook.py
  - tests/test_capture_sync_generators.py
autonomous: true
requirements: [NOTE-01, NOTE-02, FUEL-01, FUEL-02]
must_haves:
  truths:
    - "Fresh database on first connect contains 'notes' and 'fuel_stops' tables"
    - "Existing v5 database migrates to v6 and gains both tables without data loss"
    - "schema_version row reads 6 after migration"
    - "Wave 0 test files exist and run green as stubs (skipped or trivially pass)"
  artifacts:
    - path: "src/shitbox/storage/database.py"
      provides: "SCHEMA_VERSION=6, SCHEMA_SQL with notes+fuel_stops, _migrate_to_v6()"
      contains: "SCHEMA_VERSION = 6"
    - path: "tests/test_logbook.py"
      provides: "Stubs for test_create_note, test_note_gps_stale, test_note_event_pin, test_create_fuel_stop, test_fuel_efficiency, test_fuel_efficiency_no_odo, test_fuel_json_no_cost"
    - path: "tests/test_capture_sync_generators.py"
      provides: "Stubs for generator registry behaviour"
    - path: "tests/test_database.py"
      provides: "test_v6_migration and test_v6_fresh_schema cases"
  key_links:
    - from: "Database.connect()"
      to: "_migrate_to_v6"
      via: "if current_version < 6 branch"
      pattern: "current_version < 6"
---

<objective>
Lay the schema v6 foundation and create Wave 0 test scaffolds so every subsequent plan has a
verifiable landing pad. No REST, no UI — just tables on disk and empty test files that import
and pass.

Purpose: Schema must exist before storage/API can target it; test stubs satisfy Nyquist
sampling so later tasks can add real assertions without creating new files mid-plan.
Output: `database.py` at schema v6, three test files seeded with stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-schema-foundation-and-logbook-api/12-CONTEXT.md
@.planning/phases/12-schema-foundation-and-logbook-api/12-RESEARCH.md
@src/shitbox/storage/database.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 test stubs</name>
  <files>tests/test_logbook.py, tests/test_capture_sync_generators.py, tests/test_database.py</files>
  <read_first>
    - tests/test_database.py (see existing test patterns + fixtures)
    - .planning/phases/12-schema-foundation-and-logbook-api/12-VALIDATION.md (required test names)
  </read_first>
  <behavior>
    - All stubs import cleanly and either pass trivially or are marked pytest.skip with a TODO reason
    - Running `pytest tests/test_logbook.py tests/test_capture_sync_generators.py -x` exits 0
    - Running `pytest tests/test_database.py::test_v6_migration tests/test_database.py::test_v6_fresh_schema -x` exits 0
  </behavior>
  <action>
    Create `tests/test_logbook.py` containing these exact test function names, each as a stub
    using `pytest.skip("pending plan 12-02 implementation")`:
      - test_create_note
      - test_note_gps_stale
      - test_note_event_pin
      - test_create_fuel_stop
      - test_fuel_efficiency
      - test_fuel_efficiency_no_odo
      - test_fuel_json_no_cost

    Create `tests/test_capture_sync_generators.py` containing:
      - test_generators_run_before_rsync (skip: "pending plan 12-03")
      - test_generator_failure_does_not_abort_sync (skip: "pending plan 12-03")
      - test_register_json_generator_adds_entry (skip: "pending plan 12-03")

    Append to `tests/test_database.py` two NEW tests (do not touch existing ones):
      - test_v6_fresh_schema: creates a fresh `Database(tmp_path / "t.db")`, calls connect(),
        then asserts both `notes` and `fuel_stops` tables exist via
        `SELECT name FROM sqlite_master WHERE type='table' AND name IN ('notes','fuel_stops')`
        returning 2 rows, and that `SELECT version FROM schema_version` returns 6.
      - test_v6_migration: create a sqlite db manually, set schema_version to 5 and build a
        minimal v5 layout (just the schema_version table is sufficient since _migrate_to_v6
        only adds new tables — do NOT replicate the full v5 schema), then instantiate
        `Database(path)` and call connect(). Assert both tables now exist and version == 6.

    Every file must start with `"""Tests for Phase 12 logbook."""` docstring and
    `import pytest` / `import sqlite3` / `from shitbox.storage.database import Database` as
    appropriate.
  </action>
  <verify>
    <automated>pytest tests/test_logbook.py tests/test_capture_sync_generators.py tests/test_database.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_logbook.py` exists and `grep -c "^def test_" tests/test_logbook.py` returns 7
    - `tests/test_capture_sync_generators.py` exists and `grep -c "^def test_" tests/test_capture_sync_generators.py` returns 3
    - `grep -q "def test_v6_fresh_schema" tests/test_database.py` exits 0
    - `grep -q "def test_v6_migration" tests/test_database.py` exits 0
    - `pytest tests/test_logbook.py tests/test_capture_sync_generators.py -x` exits 0 (skipped tests count as pass)
  </acceptance_criteria>
  <done>All test files exist, all stubs either skip or pass, pytest returns 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema v6 migration — notes and fuel_stops tables</name>
  <files>src/shitbox/storage/database.py</files>
  <read_first>
    - src/shitbox/storage/database.py (see SCHEMA_VERSION, SCHEMA_SQL, connect(), _migrate_to_v4, _migrate_to_v5)
  </read_first>
  <behavior>
    - Fresh connect() on an empty path creates both new tables and sets schema_version to 6
    - connect() on a v5 db calls _migrate_to_v6 and the tables are created
    - Existing v1-v5 migrations still run in sequence (no elif, matches current pattern)
  </behavior>
  <action>
    In `src/shitbox/storage/database.py`:

    1. Change `SCHEMA_VERSION = 5` to `SCHEMA_VERSION = 6`.

    2. Add to the `SCHEMA_SQL` string, BEFORE the indexes/CREATE INDEX block, these two
       CREATE TABLE IF NOT EXISTS statements verbatim:

       ```sql
       CREATE TABLE IF NOT EXISTS notes (
           id INTEGER PRIMARY KEY AUTOINCREMENT,
           timestamp_utc TEXT NOT NULL,
           body TEXT NOT NULL,
           event_id INTEGER,
           lat REAL,
           lng REAL,
           gps_stale BOOLEAN NOT NULL DEFAULT 0,
           created_at TEXT DEFAULT (datetime('now'))
       );

       CREATE TABLE IF NOT EXISTS fuel_stops (
           id INTEGER PRIMARY KEY AUTOINCREMENT,
           timestamp_utc TEXT NOT NULL,
           volume_litres REAL NOT NULL,
           cost_aud REAL,
           lat REAL,
           lng REAL,
           gps_stale BOOLEAN NOT NULL DEFAULT 0,
           odometer_km REAL,
           created_at TEXT DEFAULT (datetime('now'))
       );
       ```

    3. In `connect()`, after the existing `if current_version < 5:` branch, add a new
       branch (NOT elif — follow the existing non-short-circuiting pattern):

       ```python
       if current_version < 6:
           self._migrate_to_v6(conn)
       ```

       Then ensure the final `schema_version` update writes `SCHEMA_VERSION` (6). If the
       current code writes a literal 5, change it to `SCHEMA_VERSION`.

    4. Add a new method `_migrate_to_v6(self, conn: sqlite3.Connection) -> None` that
       executes both CREATE TABLE IF NOT EXISTS statements above (use conn.execute for
       each), calls `conn.commit()`, and logs `log.info("migrated_to_v6", tables=["notes", "fuel_stops"])`.

    5. Replace the stub bodies in `tests/test_database.py::test_v6_fresh_schema` and
       `test_v6_migration` from Task 1 with real assertions (they were stubs; now make
       them assert). Remove any `pytest.skip` from these two tests.

    DO NOT touch existing migration methods or the v5 branch. DO NOT use SELECT *
    anywhere. DO NOT add indexes on the new tables in this plan (can follow in a later
    phase if needed).
  </action>
  <verify>
    <automated>pytest tests/test_database.py::test_v6_fresh_schema tests/test_database.py::test_v6_migration -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "SCHEMA_VERSION = 6" src/shitbox/storage/database.py` exits 0
    - `grep -q "_migrate_to_v6" src/shitbox/storage/database.py` exits 0
    - `grep -q "CREATE TABLE IF NOT EXISTS notes" src/shitbox/storage/database.py` exits 0
    - `grep -q "CREATE TABLE IF NOT EXISTS fuel_stops" src/shitbox/storage/database.py` exits 0
    - `grep -q "migrated_to_v6" src/shitbox/storage/database.py` exits 0
    - `pytest tests/test_database.py::test_v6_fresh_schema tests/test_database.py::test_v6_migration -x` exits 0
    - `pytest tests/test_database.py -x` exits 0 (no regression on existing tests)
  </acceptance_criteria>
  <done>Schema version is 6, both new tables exist on fresh and migrated databases, v6 tests pass, no existing tests regress.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_database.py tests/test_logbook.py tests/test_capture_sync_generators.py -x`
— all three files green. Inspect `database.py` for SCHEMA_VERSION = 6 and the new migration method.
</verification>

<success_criteria>
- Schema at v6, both new tables created on fresh and migrated databases
- Wave 0 test files exist with the correct test function names as stubs
- No existing tests regress
</success_criteria>

<output>
After completion, create `.planning/phases/12-schema-foundation-and-logbook-api/12-01-SUMMARY.md`
</output>
