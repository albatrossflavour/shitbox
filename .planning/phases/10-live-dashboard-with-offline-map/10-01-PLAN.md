---
phase: 10-live-dashboard-with-offline-map
plan: 01
type: execute
wave: 1
depends_on: [10-00]
files_modified:
  - pyproject.toml
  - src/shitbox/utils/config.py
  - config/config.yaml
  - src/shitbox/dashboard/__init__.py
  - src/shitbox/dashboard/snapshot.py
autonomous: true
requirements: [D-01, D-02, D-06, D-10]
must_haves:
  truths:
    - "fastapi, uvicorn, sse-starlette declared in pyproject.toml dependencies"
    - "pyproject.toml [tool.setuptools.package-data] includes dashboard static files"
    - "DashboardConfig dataclass exists and loads from config/config.yaml dashboard: section"
    - "shitbox.dashboard.snapshot module provides read_snapshot/update_snapshot with all 16 default keys"
    - "test_snapshot_atomicity and test_snapshot_default_keys turn GREEN"
  artifacts:
    - path: pyproject.toml
      provides: "Runtime deps + package-data for dashboard/static"
      contains: "fastapi"
    - path: src/shitbox/utils/config.py
      provides: "DashboardConfig dataclass wired into Config"
      contains: "class DashboardConfig"
    - path: src/shitbox/dashboard/snapshot.py
      provides: "Lock-free shared state, single writer many readers"
      contains: "def update_snapshot"
    - path: config/config.yaml
      provides: "dashboard: YAML section"
      contains: "dashboard:"
  key_links:
    - from: src/shitbox/utils/config.py
      to: config/config.yaml
      via: "DashboardConfig in _dict_to_dataclass loading"
      pattern: "DashboardConfig"
---

<objective>
Wave 1: Lay the dependency, config, package, and snapshot foundation for the dashboard. No server, no routes, no engine wiring yet — just the substrate the next waves build on.

Purpose: Get fastapi/uvicorn/sse-starlette installable, get the dashboard package importable, get the snapshot module turning the first two RED tests GREEN.
Output: Snapshot module shipping default keys; DashboardConfig dataclass wired into the YAML loader; pyproject.toml updated with deps and package-data so `pip install -e .` ships dashboard/static; empty `dashboard/__init__.py` so the package is importable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-live-dashboard-with-offline-map/10-CONTEXT.md
@.planning/phases/10-live-dashboard-with-offline-map/10-RESEARCH.md
@CLAUDE.md
@pyproject.toml
@src/shitbox/utils/config.py
@config/config.yaml
@tests/test_dashboard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: pyproject.toml — add deps + dashboard package-data</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current state — see lines 10-49)
    - .planning/phases/10-live-dashboard-with-offline-map/10-RESEARCH.md (Pitfall 7 — package-data; "uvicorn[standard] may fail on Pi arm64" note)
  </read_first>
  <action>
1. Add three new lines to `[project] dependencies` (between existing entries, preserve ordering style):
   ```
       "fastapi>=0.115",
       "uvicorn>=0.32",
       "sse-starlette>=2.1",
   ```
   Use bare `uvicorn` (NOT `uvicorn[standard]`) per RESEARCH "uvicorn extras fallback" — uvloop is unreliable on Pi arm64. Pure-asyncio is fine for our handful of SSE clients.

2. Update `[tool.setuptools.package-data]` from:
   ```
   shitbox = ["capture/assets/*.png"]
   ```
   to:
   ```
   shitbox = ["capture/assets/*.png", "dashboard/static/**/*"]
   ```
   This ensures `pip install -e .` and any future wheel builds ship the vendored static files.

3. Do NOT touch other sections (ruff, mypy, scripts).
  </action>
  <verify>
    <automated>grep -E "^\s*\"fastapi" pyproject.toml && grep -E "^\s*\"uvicorn" pyproject.toml && grep -E "^\s*\"sse-starlette" pyproject.toml && grep "dashboard/static" pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"fastapi>=0.115"' pyproject.toml` matches
    - `grep -n '"uvicorn>=0.32"' pyproject.toml` matches
    - `grep -nE '"uvicorn\[standard\]' pyproject.toml` returns NOTHING (fallback honoured)
    - `grep -n '"sse-starlette>=2.1"' pyproject.toml` matches
    - `grep -n 'dashboard/static/\*\*/\*' pyproject.toml` matches
    - `grep -n 'shitbox = \[' pyproject.toml` shows the package-data line still includes `capture/assets/*.png`
    - `python -c "import tomllib; tomllib.loads(open('pyproject.toml').read())" 2>&1 || python -c "import tomli; tomli.loads(open('pyproject.toml').read())"` exits 0 (TOML still valid)
  </acceptance_criteria>
  <done>
    pyproject.toml parses; deps and package-data updated; uvicorn extras NOT used.
  </done>
</task>

<task type="auto">
  <name>Task 2: DashboardConfig dataclass + YAML section</name>
  <files>src/shitbox/utils/config.py, config/config.yaml</files>
  <read_first>
    - src/shitbox/utils/config.py (existing dataclass pattern, _dict_to_dataclass, AppConfig.data_dir at line 296, Config class at line 300)
    - config/config.yaml (existing top-level structure)
    - .planning/phases/10-live-dashboard-with-offline-map/10-RESEARCH.md (Open Question 3 — mbtiles_path default)
  </read_first>
  <action>
**In src/shitbox/utils/config.py:**

1. Add a new dataclass after `DisplayConfig` (around line 284), before `AppConfig`:
   ```python
   @dataclass
   class DashboardConfig:
       enabled: bool = False
       host: str = "0.0.0.0"
       port: int = 8080
       mbtiles_path: str = "/var/lib/shitbox/tiles/rally.mbtiles"
       max_sse_clients: int = 8
   ```
   Use `Optional`/`List` from typing if needed (Python 3.9 compat — do NOT use `str | None`).

2. Add `dashboard: DashboardConfig` field to the top-level `Config` dataclass (around line 300). Default: `field(default_factory=DashboardConfig)`. Import `field` from dataclasses if not already imported.

3. Verify `_dict_to_dataclass` (or whatever the YAML loader calls it) handles the new field — if it walks dataclass fields generically (which the existing pattern does, per CLAUDE.md "hierarchical YAML config loaded into nested dataclasses"), no loader changes are required. If it has explicit per-field handling, add the dashboard branch following the same shape as `display` or `sync`.

**In config/config.yaml:**

4. Add a new top-level section (place it after `sync:` or wherever feels consistent with existing ordering):
   ```yaml
   dashboard:
     enabled: false  # set true to start in-process FastAPI on the Pi
     host: 0.0.0.0
     port: 8080
     mbtiles_path: /var/lib/shitbox/tiles/rally.mbtiles
     max_sse_clients: 8
   ```
   Keep `enabled: false` as the default — Wave 4 flips it on after engine wiring is verified.
  </action>
  <verify>
    <automated>python -c "from shitbox.utils.config import DashboardConfig, load_config; c = DashboardConfig(); assert c.port == 8080; assert c.max_sse_clients == 8; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class DashboardConfig" src/shitbox/utils/config.py` matches
    - `grep -n "dashboard: DashboardConfig" src/shitbox/utils/config.py` matches
    - `grep -n "max_sse_clients" src/shitbox/utils/config.py` matches
    - `grep -nE "^dashboard:" config/config.yaml` matches
    - `grep -n "mbtiles_path" config/config.yaml` matches
    - `python -c "from shitbox.utils.config import load_config; c = load_config('config/config.yaml'); assert c.dashboard.port == 8080; assert c.dashboard.enabled is False"` exits 0
    - `ruff check src/shitbox/utils/config.py` passes
  </acceptance_criteria>
  <done>
    DashboardConfig importable, loads from YAML, defaults sane, ruff clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: dashboard package + snapshot module</name>
  <files>src/shitbox/dashboard/__init__.py, src/shitbox/dashboard/snapshot.py</files>
  <read_first>
    - .planning/phases/10-live-dashboard-with-offline-map/10-RESEARCH.md (Pattern 2 — Lock-free snapshot dict)
    - tests/test_dashboard.py (test_snapshot_atomicity, test_snapshot_default_keys — these define the required key set)
    - CLAUDE.md (structlog kwargs, ruff line 100, py39 typing)
  </read_first>
  <action>
1. Create `src/shitbox/dashboard/__init__.py` with a single line:
   ```python
   """Dashboard subsystem — in-process FastAPI server, SSE streams, MBTiles tile serving."""
   ```

2. Create `src/shitbox/dashboard/snapshot.py` with the lock-free snapshot pattern from RESEARCH Pattern 2. Use Python 3.9 compatible typing (`Dict` from typing, NOT `dict[...]` annotations). Add `from __future__ import annotations` at the top to be safe.

   Required exports:
   - `_snapshot: Dict[str, Any]` — module-level sentinel with ALL of these keys preset to safe defaults:
     `ts=0.0, speed_kmh=0.0, g_x=0.0, g_y=0.0, g_z=0.0, heading_deg=0.0, lat=None, lng=None, gps_fix_mode=0, gps_sat_count=0, gps_hdop=None, imu_temp_c=None, soc_temp_c=None, sync_connected=False, sync_backlog=0, event_count_today=0`
     (16 keys — these are the exact keys test_snapshot_default_keys checks for.)
   - `def update_snapshot(new: Dict[str, Any]) -> None` — atomic rebind via `global _snapshot`. Sole writer is the high-rate path. Docstring must call this out.
   - `def read_snapshot() -> Dict[str, Any]` — returns `_snapshot` directly (no copy — readers iterate immediately).

   Module docstring explains: GIL-atomic rebind, single writer (high-rate sample callback at 10 Hz, NOT 100 Hz — see RESEARCH Pitfall 3), insertion point is the engine sample callback NOT the sampler itself (CONTEXT D-02: capture path is sacred).

   No structlog import needed (this is a pure data module — logging belongs in the writer).
  </action>
  <verify>
    <automated>python -c "from shitbox.dashboard.snapshot import read_snapshot, update_snapshot; s = read_snapshot(); keys = ('ts','speed_kmh','g_x','g_y','g_z','heading_deg','lat','lng','gps_fix_mode','gps_sat_count','gps_hdop','imu_temp_c','soc_temp_c','sync_connected','sync_backlog','event_count_today'); [s[k] for k in keys]; update_snapshot({**s, 'speed_kmh': 99.0}); assert read_snapshot()['speed_kmh'] == 99.0; print('ok')" && pytest tests/test_dashboard.py::test_snapshot_atomicity tests/test_dashboard.py::test_snapshot_default_keys -x</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/shitbox/dashboard/__init__.py` succeeds
    - `test -f src/shitbox/dashboard/snapshot.py` succeeds
    - `grep -n "def update_snapshot" src/shitbox/dashboard/snapshot.py` matches
    - `grep -n "def read_snapshot" src/shitbox/dashboard/snapshot.py` matches
    - `grep -n "global _snapshot" src/shitbox/dashboard/snapshot.py` matches
    - `grep -n "from __future__ import annotations" src/shitbox/dashboard/snapshot.py` matches
    - `grep -nE "from typing import.*Dict" src/shitbox/dashboard/snapshot.py` matches (py39 compat — no `dict[...]` annotations)
    - `pytest tests/test_dashboard.py::test_snapshot_atomicity tests/test_dashboard.py::test_snapshot_default_keys -x` exits 0 (these turn GREEN)
    - `ruff check src/shitbox/dashboard/snapshot.py` passes
    - `mypy src/shitbox/dashboard/snapshot.py` passes
  </acceptance_criteria>
  <done>
    Package importable, snapshot module exports the contract, two tests flip RED→GREEN.
  </done>
</task>

</tasks>

<verification>
- `pip install -e ".[dev]"` installs cleanly (or at minimum `python -m pip install fastapi uvicorn sse-starlette` succeeds in the dev venv)
- `python -c "from shitbox.dashboard.snapshot import read_snapshot, update_snapshot; from shitbox.utils.config import DashboardConfig"` exits 0
- `pytest tests/test_dashboard.py::test_snapshot_atomicity tests/test_dashboard.py::test_snapshot_default_keys` GREEN
- `ruff check src/shitbox/` passes
- Other Phase 10 tests still RED (server.py and tiles.py do not exist yet)
</verification>

<success_criteria>
- 3 deps added, package-data extended
- DashboardConfig wired into Config and YAML
- snapshot.py exports the 16-key contract
- 2 of 13+ Phase 10 tests now GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/10-live-dashboard-with-offline-map/10-01-SUMMARY.md`
</output>
