---
phase: 13-driver-tracking
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - src/shitbox/events/storage.py
  - src/shitbox/events/engine.py
  - src/shitbox/dashboard/sse.py
  - src/shitbox/sync/capture_sync.py
autonomous: true
requirements: [DRVR-02, DRVR-03]
must_haves:
  truths:
    - "save_event() writes driver_name into event JSON metadata when provided"
    - "UnifiedEngine passes active driver to save_event() at event-record time"
    - "/sse/slow payload includes active_driver field"
    - "Engine registers driver-stats JSON generator with CaptureSyncService"
    - "driver-stats.json is written to captures_dir before rsync with {active_driver, drivers}"
  artifacts:
    - path: "src/shitbox/events/storage.py"
      provides: "save_event(driver_name=...) parameter"
      contains: "driver_name"
    - path: "src/shitbox/events/engine.py"
      provides: "DriverStorage instantiation + snapshot active_driver + save_event attribution + driver-stats generator registration"
      contains: "driver_storage"
  key_links:
    - from: "src/shitbox/events/engine.py"
      to: "src/shitbox/events/storage.py save_event"
      via: "driver_name=driver_state.get_active_driver()"
      pattern: "driver_name=.*get_active_driver"
    - from: "src/shitbox/events/engine.py"
      to: "src/shitbox/sync/capture_sync.py register_json_generator"
      via: "driver-stats generator"
      pattern: "driver-stats"
    - from: "src/shitbox/dashboard/sse.py"
      to: "snapshot active_driver"
      via: "sse_slow payload field"
      pattern: '"active_driver":\s*snap'
---

<objective>
Wire driver tracking into the running system: event attribution through
save_event(), engine-side DriverStorage instantiation, snapshot active_driver
population, SSE broadcast, and the driver-stats JSON sync generator.

Purpose: Plan 02 built the REST surface but nothing is actually attributing
events or broadcasting the active driver yet. This plan closes DRVR-03 and the
SSE half of DRVR-01.
Output: All 8 tests in tests/test_driver.py green; driver-stats.json written to
captures_dir on each sync cycle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-driver-tracking/13-CONTEXT.md
@.planning/phases/13-driver-tracking/13-RESEARCH.md
@src/shitbox/events/storage.py
@src/shitbox/events/engine.py
@src/shitbox/dashboard/sse.py
@src/shitbox/sync/capture_sync.py
@src/shitbox/dashboard/driver_state.py
@src/shitbox/storage/driver.py

<interfaces>
From src/shitbox/sync/capture_sync.py (Phase 12 established pattern):
- `register_json_generator(name: str, generator: Callable[[], dict], filename: str)`
- Generator is called before each rsync, output written to `{captures_dir}/{filename}`

From src/shitbox/events/engine.py (Phase 12 logbook wiring pattern to mirror):
- UnifiedEngine instantiates LogbookStorage unconditionally in __init__
- Passes it through build_dashboard_server kwargs
- _record_telemetry is where gps_state is currently updated (Phase 12)

From src/shitbox/events/storage.py save_event (existing):
- Current signature: `save_event(self, event: Event, video_path: Optional[Path] = None) -> tuple[Path, Path]`
- Writes JSON metadata dict; add driver_name as optional kwarg, insert into metadata when not None

From src/shitbox/dashboard/sse.py sse_slow:
- Generator yields _format_sse("slow", {...snap fields...})
- Add `"active_driver": snap.get("active_driver")` to the yielded dict
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Event attribution + save_event driver_name kwarg + SSE field</name>
  <files>
    src/shitbox/events/storage.py,
    src/shitbox/dashboard/sse.py
  </files>
  <read_first>
    - src/shitbox/events/storage.py (entire save_event method)
    - src/shitbox/dashboard/sse.py sse_slow function
    - .planning/phases/13-driver-tracking/13-RESEARCH.md "save_event() driver attribution" example
  </read_first>
  <behavior>
    - save_event(event, driver_name="Tony") writes "driver_name": "Tony" into the JSON metadata file
    - save_event(event) with no driver_name kwarg does NOT include a driver_name key (or writes None, matching test expectation)
    - /sse/slow payload includes "active_driver" key reading from snapshot
    - No behaviour regression for existing save_event callers
  </behavior>
  <action>
    **1. Edit `src/shitbox/events/storage.py` — extend save_event:**

    Find the `save_event` method. Add an optional keyword-only parameter:
    ```python
    def save_event(
        self,
        event: Event,
        video_path: Optional[Path] = None,
        *,
        driver_name: Optional[str] = None,
    ) -> tuple[Path, Path]:
    ```

    In the body, where the metadata dict is built (look for lines assigning
    `metadata["csv_file"]` and `metadata["saved_at"]`), add:
    ```python
        if driver_name is not None:
            metadata["driver_name"] = driver_name
    ```

    This is a pure pass-through — no I/O change, no other call sites affected because
    the parameter is optional keyword-only.

    **2. Edit `src/shitbox/dashboard/sse.py` — add active_driver to /sse/slow payload:**

    Locate the `sse_slow` function. Inside its generator where the payload dict is
    built (look for `_format_sse("slow", { ... })`), add:
    ```python
    "active_driver": snap.get("active_driver"),
    ```
    Use `snap.get` not `snap[]` so the key is safe even if an older snapshot without
    the key leaks through (defensive — the default should always have it after plan 02).
  </action>
  <verify>
    <automated>pytest tests/test_driver.py::test_event_attribution tests/test_driver.py::test_event_attribution_no_driver tests/test_driver.py::test_sse_slow_includes_active_driver -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "driver_name" src/shitbox/events/storage.py` shows the kwarg and metadata assignment
    - `grep -n "active_driver" src/shitbox/dashboard/sse.py` shows the added SSE payload field
    - test_event_attribution passes (JSON file contains driver_name)
    - test_event_attribution_no_driver passes (no driver_name or None in JSON)
    - test_sse_slow_includes_active_driver passes
    - No existing EventStorage tests break: `pytest tests/events/ -x`
    - ruff check and mypy on both files pass
  </acceptance_criteria>
  <done>DRVR-03 tests green; SSE broadcasts active_driver; existing event save pipeline unchanged for non-driver callers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Engine wiring — DriverStorage instance, snapshot update, event attribution, sync generator</name>
  <files>
    src/shitbox/events/engine.py,
    src/shitbox/sync/capture_sync.py
  </files>
  <read_first>
    - src/shitbox/events/engine.py (UnifiedEngine.__init__, _update_snapshot / _record_telemetry, the point where save_event is called, build_dashboard_server call)
    - src/shitbox/sync/capture_sync.py (register_json_generator from Phase 12)
    - .planning/phases/13-driver-tracking/13-RESEARCH.md (Pattern 5: Engine wiring)
    - src/shitbox/utils/config.py (to find the drivers field on the root Config)
  </read_first>
  <behavior>
    - UnifiedEngine has self.driver_storage: DriverStorage instantiated in __init__
    - Snapshot update includes "active_driver": driver_state.get_active_driver()
    - save_event calls in the engine pass driver_name=driver_state.get_active_driver()
    - build_dashboard_server is called with driver_storage and drivers_roster kwargs
    - CaptureSyncService has a driver-stats JSON generator registered that writes driver-stats.json on each sync cycle
  </behavior>
  <action>
    **1. Edit `src/shitbox/events/engine.py`:**

    a) At the top, add imports:
    ```python
    from shitbox.storage.driver import DriverStorage
    from shitbox.dashboard import driver_state
    ```

    b) In `UnifiedEngine.__init__()`, after the LogbookStorage instantiation (Phase 12
       pattern, look for `self.logbook_storage = LogbookStorage(...)`), add:
    ```python
        self.driver_storage = DriverStorage(self.db)
    ```
    DriverStorage is cheap, REST-only, idempotent — register unconditionally (same
    approach as Phase 12 LogbookStorage per STATE.md D-note).

    c) Find where `build_dashboard_server()` is called. Add the new kwargs:
    ```python
            driver_storage=self.driver_storage,
            drivers_roster=self.config.drivers if hasattr(self.config, "drivers") else [],
    ```
    (Adjust attribute path if drivers lives on a nested config dataclass — match
    whatever plan 02 chose.)

    d) Find the snapshot update call. It builds a dict of 16+1 keys. Add:
    ```python
        "active_driver": driver_state.get_active_driver(),
    ```
    to the dict literal.

    e) Find EVERY call to `self.event_storage.save_event(` in engine.py (there should
       be 1–2 of them, inside the event-handling path). Add the kwarg:
    ```python
        self.event_storage.save_event(
            event,
            video_path=video_path,
            driver_name=driver_state.get_active_driver(),
        )
    ```

    f) After CaptureSyncService is instantiated, register the driver-stats generator
       (guarded on capture_sync not None, same pattern as Phase 12 logbook generators):
    ```python
        if self.capture_sync is not None:
            self.capture_sync.register_json_generator(
                "driver-stats",
                self.driver_storage.get_driver_stats_payload,
                "driver-stats.json",
            )
    ```
    Check the exact register_json_generator signature in capture_sync.py — if it takes
    (name, generator) and derives filename, drop the third arg. If it takes
    (name, filename, generator), re-order. Match what Phase 12 did for notes/fuel.

    **2. Verify `src/shitbox/sync/capture_sync.py`:**

    Read the file to confirm `register_json_generator` exists from Phase 12. No
    modification expected — just confirm the API matches the call site in engine.py.
    If the Phase 12 generators are registered the same way (they should be), no
    change here. If the signature differs from what you wrote above, update the
    engine.py call to match; do NOT modify capture_sync.py itself (Phase 12 contract).
  </action>
  <verify>
    <automated>pytest tests/test_engine_boot.py tests/test_driver.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "DriverStorage" src/shitbox/events/engine.py` matches (import + instantiation)
    - `grep -n "driver_state.get_active_driver" src/shitbox/events/engine.py` matches at snapshot update AND every save_event call site
    - `grep -n "driver-stats" src/shitbox/events/engine.py` matches the register_json_generator call
    - `grep -n "driver_storage=" src/shitbox/events/engine.py` matches in the build_dashboard_server call
    - Engine boot test still passes: `pytest tests/test_engine_boot.py -x`
    - ALL 8 tests in tests/test_driver.py now pass: `pytest tests/test_driver.py -x`
    - Full suite green: `pytest -x` (no regressions)
    - ruff and mypy clean
  </acceptance_criteria>
  <done>Full Phase 13 backend is live: DriverStorage wired, active driver attributed to events, SSE broadcasts it, driver-stats.json generated for sync. All 8 test_driver.py tests pass.</done>
</task>

</tasks>

<verification>
- pytest tests/test_driver.py -x  (all 8 green)
- pytest tests/test_engine_boot.py -x
- pytest --cov=shitbox  (no regressions)
- ruff check src/shitbox/events/engine.py src/shitbox/events/storage.py src/shitbox/dashboard/sse.py
- mypy src/shitbox/events/engine.py
</verification>

<success_criteria>
- DRVR-03 fully satisfied: events carry driver attribution in JSON metadata
- DRVR-02 sync half satisfied: driver-stats.json generated for website consumption
- /sse/slow broadcasts active_driver
- Engine boot still works, no 100 Hz capture path regression
</success_criteria>

<output>
After completion, create `.planning/phases/13-driver-tracking/13-03-SUMMARY.md`
</output>
