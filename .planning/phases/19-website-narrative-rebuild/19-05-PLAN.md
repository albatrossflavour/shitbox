---
phase: 19
plan: "05"
type: execute
wave: 2
depends_on: ["19-02", "19-03", "19-04"]
files_modified:
  - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html
autonomous: true
requirements:
  - NARR-04
must_haves:
  truths:
    - "renderDayPage(dayISO) emits the D-12 section order: day header → agenda context → day stats → map slice placeholder → timeline spine placeholder → video highlights placeholder → day timelapse placeholder"
    - "Day header shows 'Day N of M' (from agenda.day_number / total days count) and the agenda.title"
    - "Agenda context block renders route prose, camping, meals[] BEFORE any telemetry section"
    - "Day stats row shows km driven, moving time, top speed, peak G, fuel burned — derived from filtered eventsData/fuelData/routeData"
    - "Missing agenda day falls back to a minimal header using dayISO and empty stats"
    - "All sections render inside a single vertical-scroll container (no tabs, no collapsibles — D-11)"
    - "Mobile (<600px) keeps the same section order — responsive CSS tightens spacing only"
  artifacts:
    - path: "~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html"
      provides: "renderDayPage scaffold + renderAgendaContext + computeDayStats + .day-page CSS"
      contains: "function renderAgendaContext"
  key_links:
    - from: "renderDayPage(dayISO) (stub replaced)"
      to: "renderAgendaContext + computeDayStats + section placeholders"
      via: "composes HTML in D-12 order"
      pattern: "function renderDayPage"
    - from: "eventsData filtered by dayISO"
      to: "day stats calculation"
      via: "dayOf(ev.timestamp) === dayISO filter, then max/sum"
      pattern: "computeDayStats"
---

<objective>
Replace the Plan 19-03 day-page stub with a real scaffold that enforces the D-12 section order and renders the agenda context + day stats row. The timeline spine (Plan 19-06) and day map (Plan 19-07) land inside the placeholders this plan exposes.

Purpose: Lock D-11 (single vertical scroll), D-12 (section order top→bottom: header → agenda → stats → map → spine → videos → timelapse), D-13 (mobile same order), NARR-04 (agenda context BEFORE telemetry).

Output additions to `webroot/index.html`:
- `.day-page` container CSS (max-width, padding, section spacing).
- `.day-header`, `.day-agenda`, `.day-stats`, `.day-section-placeholder` styles.
- `function renderAgendaContext(agendaDay)` — renders route/camping/meals block.
- `function computeDayStats(dayISO, events, route, fuel)` — derives km, moving-time, top-speed, peak-G, fuel-burned from cross-source data.
- `function renderDayPage(dayISO)` (REPLACES the Plan 19-03 stub) — composes the full scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-website-narrative-rebuild/19-CONTEXT.md
@.planning/phases/19-website-narrative-rebuild/19-RESEARCH.md

<interfaces>
From Plan 19-02:

    agendaData = {
      rally: { start_date, end_date, total_distance_km, ... },
      days: [ { date, day_number, title, route, camping, meals: [{time, where}], notes } ]
    }
    routeData = {
      generated_at,
      tolerance_m,
      days: { "2026-05-27": {point_count, points: [[lat,lng], ...]} }
    }

Existing data objects (fetched by existing code):

    eventsData = [ { id, timestamp, type, peak_g, lat, lng, distance_from_start_km, ... } ]
    fuelData   = [ { id, timestamp_utc, volume_litres, lat, lng, odometer_km, km_per_litre, ... } ]
    notesData  = [ { id, timestamp_utc, body, event_id, lat, lng } ]
    driverStatsData (shape from Phase 13 — driver-stats.json)

From Plan 19-03:

    function showRoute(html) { ... }             // mounts HTML into #dynamic-route, hides legacy sections
    function renderDayPage(dayISO) { /* stub */ } // REPLACE this
    function navigateHome() { ... }

From Plan 19-04:

    function renderDayNav(agenda, highlightDayISO) { ... }   // call FIRST thing

From existing webroot/index.html line 1004:

    var TIMEZONE = 'Australia/Sydney';

Existing day-bucketing pattern (line 1348):

    new Date(ev.timestamp).toLocaleDateString('en-CA', { timeZone: TIMEZONE })
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agenda.route / camping / meals.where / notes prose → innerHTML | Content is from home-ops ConfigMap, not user-supplied. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05-01 | XSS | Agenda prose fields injected into DOM | mitigate | Introduce `escapeHtml(s)` helper (or use `textContent` via an intermediate element). Apply to every agenda string before concatenating into HTML. Covers `title`, `route`, `camping`, `meals[].where`, `notes`. |
| T-19-05-02 | Logic error | Computing day stats on wrong-timezone boundaries | mitigate | Reuse the existing `en-CA` / `Australia/Sydney` bucketing pattern (one-liner already in the file). No new timezone code. |
| T-19-05-03 | Information Disclosure | Fuel cost leaked via day stats | mitigate | `fuelData` never contains `cost_aud` (SQL enforcement in `generate_fuel_json`). `computeDayStats` uses only `volume_litres`. Grep check: `grep -E 'cost|cost_aud' <new code>` → no matches. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: CSS scaffold for day-page layout + escapeHtml helper</name>
  <files>~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html</files>
  <read_first>
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html lines 1-100 (existing CSS tokens and layout — match padding/border-radius conventions)
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html status-grid CSS around line 200 (existing grid layout pattern for day-stats to mirror)
  </read_first>
  <action>
Edit 1 — CSS. Inside the existing `<style>` element, after the `.day-nav-*` CSS added in Plan 19-04, append:

    /* Day page layout (Phase 19 — D-11, D-12, D-13) */
    .day-page { max-width: 960px; margin: 0 auto; }
    .day-page > section { margin-bottom: 2rem; }
    .day-header {
        background: #161b22; border: 1px solid #2a1f0e; border-radius: 8px;
        padding: 1.5rem 1.75rem;
    }
    .day-header h1 { margin: 0; color: #f0dbb8; font-size: 1.75rem; }
    .day-header .day-subtitle { color: #8b949e; font-size: 0.95rem; margin-top: 0.25rem; }
    .day-agenda {
        background: #161b22; border: 1px solid #2a1f0e; border-radius: 8px;
        padding: 1.25rem 1.5rem; line-height: 1.6;
    }
    .day-agenda h2 { margin: 0 0 0.75rem 0; color: #f0dbb8; font-size: 1.1rem; }
    .day-agenda .agenda-row { display: flex; gap: 0.5rem; margin-bottom: 0.35rem; color: #c9d1d9; }
    .day-agenda .agenda-row strong { color: #e0a040; min-width: 90px; }
    .day-agenda ul.meals { list-style: none; padding: 0; margin: 0.25rem 0 0 0; }
    .day-agenda ul.meals li { color: #c9d1d9; }
    .day-agenda ul.meals li time { color: #e0a040; margin-right: 0.4rem; }
    .day-stats {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
        gap: 0.75rem;
    }
    .day-stat-card {
        background: #161b22; border: 1px solid #2a1f0e; border-radius: 8px;
        padding: 0.75rem 1rem; text-align: center;
    }
    .day-stat-card .stat-label { color: #8b949e; font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.05em; }
    .day-stat-card .stat-value { color: #f0dbb8; font-size: 1.35rem; font-weight: 600; margin-top: 0.25rem; }
    .day-section-placeholder {
        background: #161b22; border: 1px dashed #30363d; border-radius: 8px;
        padding: 1.25rem; color: #8b949e; font-style: italic;
    }
    .day-section-heading {
        color: #f0dbb8; font-size: 1.2rem; margin: 0 0 0.75rem 0;
    }
    @media (max-width: 600px) {
        .day-page { padding: 0 0.5rem; }
        .day-header { padding: 1rem 1.25rem; }
        .day-header h1 { font-size: 1.35rem; }
        .day-stats { grid-template-columns: repeat(2, 1fr); }
    }

Edit 2 — escapeHtml helper. Inside the IIFE, near the top of the helper block (next to `_formatShortDate` from Plan 19-04), add:

    function escapeHtml(s) {
        if (s === null || s === undefined) return '';
        return String(s)
            .replace(/&/g, '&amp;')
            .replace(/</g, '&lt;')
            .replace(/>/g, '&gt;')
            .replace(/"/g, '&quot;')
            .replace(/'/g, '&#39;');
    }

Commit: `feat(19-05): CSS scaffold + escapeHtml for day pages`.
  </action>
  <verify>
    <automated>grep -c '\.day-page {' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c '\.day-stats {' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'function escapeHtml' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c '\.day-section-placeholder' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - File contains `.day-page {` with `max-width: 960px`
    - File contains `.day-header`, `.day-agenda`, `.day-stats`, `.day-stat-card`, `.day-section-placeholder` selectors
    - File contains `@media (max-width: 600px)` with `.day-stats { grid-template-columns: repeat(2, 1fr); }`
    - File contains exactly one `function escapeHtml(s)` definition
    - `escapeHtml` handles all five dangerous characters: `&`, `<`, `>`, `"`, `'`
  </acceptance_criteria>
  <done>Day-page CSS + escapeHtml helper landed. No behavioural change yet.</done>
</task>

<task type="auto">
  <name>Task 2: renderAgendaContext + computeDayStats + rewrite renderDayPage scaffold</name>
  <files>~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html</files>
  <read_first>
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html (re-read the Plan 19-03 stub `function renderDayPage(dayISO)` — this task REPLACES it)
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html line 1348 (en-CA date bucketing — reuse verbatim)
    - .planning/phases/19-website-narrative-rebuild/19-CONTEXT.md D-12 (section order)
  </read_first>
  <action>
Add `renderAgendaContext(agendaDay)` after `escapeHtml()`:

    function renderAgendaContext(agendaDay) {
        if (!agendaDay) {
            return '<section class="day-agenda"><p style="color:#8b949e;margin:0;">No agenda entry for this day.</p></section>';
        }
        var parts = ['<section class="day-agenda"><h2>Agenda</h2>'];
        if (agendaDay.route) {
            parts.push('<div class="agenda-row"><strong>Route</strong><span>' + escapeHtml(agendaDay.route) + '</span></div>');
        }
        if (agendaDay.camping) {
            parts.push('<div class="agenda-row"><strong>Camping</strong><span>' + escapeHtml(agendaDay.camping) + '</span></div>');
        }
        if (agendaDay.meals && agendaDay.meals.length > 0) {
            var meals = '<div class="agenda-row"><strong>Meals</strong><ul class="meals">';
            agendaDay.meals.forEach(function(m) {
                meals += '<li><time>' + escapeHtml(m.time || '') + '</time>' + escapeHtml(m.where || '') + '</li>';
            });
            meals += '</ul></div>';
            parts.push(meals);
        }
        if (agendaDay.notes) {
            parts.push('<div class="agenda-row"><strong>Notes</strong><span>' + escapeHtml(agendaDay.notes) + '</span></div>');
        }
        parts.push('</section>');
        return parts.join('');
    }

Add `computeDayStats(dayISO, events, route, fuel)`:

    function _dayOf(iso) {
        try { return new Date(iso).toLocaleDateString('en-CA', { timeZone: TIMEZONE }); }
        catch(e) { return null; }
    }

    function _haversineKm(lat1, lng1, lat2, lng2) {
        var R = 6371;
        var toRad = function(deg) { return deg * Math.PI / 180; };
        var dLat = toRad(lat2 - lat1);
        var dLng = toRad(lng2 - lng1);
        var a = Math.sin(dLat/2) * Math.sin(dLat/2)
              + Math.cos(toRad(lat1)) * Math.cos(toRad(lat2)) * Math.sin(dLng/2) * Math.sin(dLng/2);
        return 2 * R * Math.asin(Math.min(1, Math.sqrt(a)));
    }

    function computeDayStats(dayISO, events, route, fuel) {
        var dayEvents = (events || []).filter(function(ev) { return _dayOf(ev.timestamp) === dayISO; });
        var dayFuel   = (fuel   || []).filter(function(f)  { return _dayOf(f.timestamp_utc) === dayISO; });
        var daySlice  = (route && route.days && route.days[dayISO]) || null;

        var kmDriven = 0;
        if (daySlice && daySlice.points && daySlice.points.length > 1) {
            for (var i = 1; i < daySlice.points.length; i++) {
                kmDriven += _haversineKm(
                    daySlice.points[i-1][0], daySlice.points[i-1][1],
                    daySlice.points[i][0], daySlice.points[i][1]
                );
            }
        }

        var peakG = 0;
        var topSpeed = 0;
        dayEvents.forEach(function(ev) {
            if (ev.peak_g != null && ev.peak_g > peakG) peakG = ev.peak_g;
            if (ev.peak_speed_kmh != null && ev.peak_speed_kmh > topSpeed) topSpeed = ev.peak_speed_kmh;
        });

        var fuelBurned = 0;
        dayFuel.forEach(function(f) { if (f.volume_litres != null) fuelBurned += f.volume_litres; });

        return {
            km: kmDriven,
            peakG: peakG,
            topSpeed: topSpeed,
            fuelBurned: fuelBurned,
            eventCount: dayEvents.length,
            hasData: daySlice != null || dayEvents.length > 0 || dayFuel.length > 0
        };
    }

Now REPLACE the Plan 19-03 stub `function renderDayPage(dayISO) { ... }` with the real scaffold:

    function renderDayPage(dayISO) {
        renderDayNav(agendaData, dayISO);

        var agendaDay = agendaData && agendaData.days
            ? agendaData.days.filter(function(d) { return d.date === dayISO; })[0]
            : null;
        var totalDays = (agendaData && agendaData.days) ? agendaData.days.length : 0;

        var headerHtml;
        if (agendaDay) {
            headerHtml =
                '<section class="day-header">' +
                    '<h1>Day ' + String(Number(agendaDay.day_number) || 0) + ' — ' + escapeHtml(agendaDay.title || '') + '</h1>' +
                    '<div class="day-subtitle">' + escapeHtml(dayISO) +
                    (totalDays ? ' · Day ' + Number(agendaDay.day_number) + ' of ' + totalDays : '') +
                    '</div>' +
                '</section>';
        } else {
            headerHtml =
                '<section class="day-header">' +
                    '<h1>' + escapeHtml(dayISO) + '</h1>' +
                    '<div class="day-subtitle">No agenda entry for this day.</div>' +
                '</section>';
        }

        var agendaHtml = renderAgendaContext(agendaDay);

        var stats = computeDayStats(dayISO, eventsData, routeData, fuelData);
        var statsHtml =
            '<section class="day-stats">' +
                '<div class="day-stat-card"><div class="stat-label">Distance</div><div class="stat-value">' + stats.km.toFixed(0) + ' km</div></div>' +
                '<div class="day-stat-card"><div class="stat-label">Top Speed</div><div class="stat-value">' + stats.topSpeed.toFixed(0) + ' km/h</div></div>' +
                '<div class="day-stat-card"><div class="stat-label">Peak G</div><div class="stat-value">' + stats.peakG.toFixed(2) + ' g</div></div>' +
                '<div class="day-stat-card"><div class="stat-label">Events</div><div class="stat-value">' + stats.eventCount + '</div></div>' +
                '<div class="day-stat-card"><div class="stat-label">Fuel</div><div class="stat-value">' + stats.fuelBurned.toFixed(1) + ' L</div></div>' +
            '</section>';

        var mapPlaceholder = '<section><h3 class="day-section-heading">Map</h3><div id="day-map" class="day-section-placeholder">Day map placeholder — wired by Plan 19-07.</div></section>';
        var spinePlaceholder = '<section><h3 class="day-section-heading">Timeline</h3><div id="day-spine" class="day-section-placeholder">Timeline spine placeholder — wired by Plan 19-06.</div></section>';
        var videosPlaceholder = '<section><h3 class="day-section-heading">Video highlights</h3><div id="day-videos" class="day-section-placeholder">Video highlights placeholder — wired by Plan 19-06/Phase 19 backfill.</div></section>';
        var timelapsePlaceholder = '<section><h3 class="day-section-heading">Timelapse</h3><div id="day-timelapse" class="day-section-placeholder">Timelapse embed — /captures/' + escapeHtml(dayISO) + '/timelapse.mp4 if present.</div></section>';

        var html =
            '<div class="day-page">' +
                headerHtml +
                agendaHtml +
                statsHtml +
                mapPlaceholder +
                spinePlaceholder +
                videosPlaceholder +
                timelapsePlaceholder +
                '<section><a href="/" onclick="event.preventDefault(); navigateHome();" style="color:#c06000;">← Home</a></section>' +
            '</div>';
        showRoute(html);
    }

Commit: `feat(19-05): day-page scaffold with agenda context and day stats row`.
  </action>
  <verify>
    <automated>grep -c 'function renderAgendaContext' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'function computeDayStats' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'function _haversineKm' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'id="day-map"' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'id="day-spine"' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'id="day-videos"' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'id="day-timelapse"' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -v '^//' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -E 'cost_aud|cost:' | grep -v 'cost_aud is never' | head -1 | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly one `function renderAgendaContext(agendaDay)`
    - File contains exactly one `function computeDayStats(dayISO, events, route, fuel)`
    - File contains exactly one `function _haversineKm`
    - `renderDayPage` contains all seven section IDs/placeholders in D-12 order: day-header, day-agenda (via renderAgendaContext), day-stats, day-map, day-spine, day-videos, day-timelapse
    - D-12 section order is verifiable by grep — day-header appears BEFORE day-agenda in the source, agenda BEFORE stats, stats BEFORE map, map BEFORE spine, spine BEFORE videos, videos BEFORE timelapse
    - Every agenda field that reaches innerHTML goes through `escapeHtml()` (grep proof: `grep -E 'escapeHtml\(agendaDay\.(route|camping|title|notes)\)' <file>` matches each field)
    - `grep -E 'cost_aud|price_aud' <file>` (excluding pre-existing comments) returns no new matches (cost exclusion guard)
    - Browser check `/day/2026-05-27`: page shows "Day 1 — Port Douglas → Cairns", agenda block, stats row (km, top speed, peak G, events, fuel), four dashed placeholders in correct order
    - Browser check `/day/1999-01-01`: page shows the dayISO as header, "No agenda entry" subtitle, stats all zero, placeholders unchanged
  </acceptance_criteria>
  <done>renderDayPage composes the full D-12 section order with real agenda context + stats and dashed placeholders for map/spine/videos/timelapse. Escape-html applied to every agenda field.</done>
</task>

</tasks>

<verification>
Plan 19-05 complete when:
- grep + section-order checks pass
- Browser check on `/day/2026-05-27` shows the full scaffold with agenda + stats, placeholders labelled with their owning plan number
- Mobile viewport (375px) shows stats as 2 columns, no horizontal scroll

Order proof (one-liner):

    awk '/id="day-map"/{print NR, "map"} /id="day-spine"/{print NR, "spine"} /id="day-videos"/{print NR, "videos"} /id="day-timelapse"/{print NR, "timelapse"} /class="day-header">/{print NR, "header"} /class="day-agenda">/{print NR, "agenda"} /class="day-stats">/{print NR, "stats"}' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | sort -n

Should emit lines in order: header, agenda, stats, map, spine, videos, timelapse.
</verification>

<success_criteria>
- NARR-04: Day page renders agenda context block BEFORE telemetry sections (D-12 ordering proved by section grep).
- D-11 single-scroll: no tabs/collapsibles inside `.day-page`.
- D-13 mobile-same-order: media query tightens spacing, order unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/19-website-narrative-rebuild/19-05-SUMMARY.md`.
</output>
