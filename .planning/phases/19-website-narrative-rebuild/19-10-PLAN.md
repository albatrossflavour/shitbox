---
phase: 19
plan: "10"
type: execute
wave: 3
depends_on: ["19-02", "19-03", "19-04", "19-05", "19-08"]
files_modified:
  - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html
autonomous: true
requirements:
  - NARR-01
must_haves:
  truths:
    - "When detectMode returns 'archive', renderHomepage dispatches to renderHomepageArchive which shows rally-overview + linear day grid"
    - "Overview stats card shows total km driven, days count, total events, total fuel burned, driver count, rally dates"
    - "Day grid is LINEAR (not calendar layout) — one row per day, newest-first, showing day number, date, title, per-day stats teaser, thumbnail"
    - "Thumbnail fallback chain: per-day hero image then timelapse poster then placeholder card with day number only"
    - "Each day row is clickable, navigating to /day/YYYY-MM-DD"
    - "D-18 realised — scrapbook overview, not calendar"
    - "No refresh timer in archive mode (rally is over, data is static)"
    - "Overview km/fuel/event totals are computed from eventsData + fuelData + routeData client-side"
  artifacts:
    - path: "~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html"
      provides: "renderHomepageArchive + _rallyTotals + archive-mode CSS"
      contains: "function renderHomepageArchive"
  key_links:
    - from: "renderHomepage dispatch"
      to: "renderHomepageArchive() when mode==='archive'"
      via: "switch branch"
      pattern: "mode === 'archive'"
    - from: "day row click"
      to: "navigateToDay(dayISO)"
      via: "onclick with event.preventDefault"
      pattern: "navigateToDay\\("
---

<objective>
Land the archive-mode homepage: overview stats + linear day grid with thumbnails (D-18). This is what the homepage becomes AFTER the rally ends — a scrapbook index, not a calendar.

Purpose: NARR-01 archive-leg + D-18. Archive-mode should feel like flicking through a photo album: each row is a day, each day is a scene.

Output additions to `webroot/index.html`:
- `.archive-*` CSS (overview stats card, day-grid rows with thumbnails).
- `function _rallyTotals(events, fuel, route, agenda)` — aggregates across all days.
- `function renderHomepageArchive()` — composes overview + linear day grid.
- Wire archive branch into `renderHomepage()` mode dispatch (replaces Plan 19-08/19-09 placeholder).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-website-narrative-rebuild/19-CONTEXT.md
@.planning/phases/19-website-narrative-rebuild/19-RESEARCH.md

<interfaces>
From Plans 19-02 / existing code:
    agendaData, routeData, eventsData, fuelData, notesData, driverStatsData

From Plan 19-05:
    function _dayOf(iso) - bucket to YYYY-MM-DD in Australia/Sydney
    function _haversineKm(lat1, lng1, lat2, lng2)
    function computeDayStats(dayISO, events, route, fuel)

From Plan 19-08:
    .day-stat-card, .day-section-heading CSS already exists — reuse.

From Plan 19-09:
    renderHomepageArchive is invoked as a soft fallback when today overshoots rally.end_date.
    This plan defines it for real.

Capture-dir layout (existing, NFS-mounted):
    /captures/DATE/timelapse.mp4         (optional)
    /captures/DATE/timelapse-poster.jpg  (convention; fallback-safe)
    /captures/DATE/hero.jpg              (convention for curated hero; fallback-safe)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agenda / events / fuel string fields to innerHTML | Same as previous plans; escape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-10-01 | XSS | day title, driver name concatenated into day-grid rows | mitigate | `escapeHtml()` on every agenda and data string. |
| T-19-10-02 | Information Disclosure | Total cost leaks via fuelData | mitigate | `_rallyTotals` only reads `volume_litres` from fuel. Grep proof in acceptance criteria. |
| T-19-10-03 | 404 noise | Missing hero.jpg / timelapse-poster.jpg logs 404s in browser console | accept | `onerror` handler swaps to placeholder card gracefully. Minor dev-tools friction, not a security issue. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Archive-mode CSS + _rallyTotals aggregator</name>
  <files>~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html</files>
  <read_first>
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html (confirm Plan 19-05 `_haversineKm` and `computeDayStats` exist — reuse)
    - .planning/phases/19-website-narrative-rebuild/19-CONTEXT.md D-18 (linear scrapbook overview)
  </read_first>
  <action>
Edit 1 — CSS. After the `.current-driver-widget` block from Plan 19-09, append:

    /* Archive-mode homepage (Phase 19 — D-18, NARR-01) */
    .archive-overview {
        background: #161b22; border: 1px solid #2a1f0e; border-radius: 8px;
        padding: 1.25rem 1.5rem; margin-bottom: 1.5rem;
    }
    .archive-overview h1 { color: #f0dbb8; margin: 0 0 0.25rem 0; font-size: 1.6rem; }
    .archive-overview .archive-subtitle { color: #8b949e; margin-bottom: 1rem; font-size: 0.95rem; }
    .archive-stats {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(130px, 1fr));
        gap: 0.75rem;
    }
    .archive-day-grid {
        display: flex; flex-direction: column; gap: 0.75rem;
    }
    .archive-day-card {
        display: grid;
        grid-template-columns: 140px 1fr;
        gap: 1rem;
        background: #161b22; border: 1px solid #2a1f0e; border-radius: 8px;
        overflow: hidden;
        text-decoration: none;
        color: #c9d1d9;
        transition: border-color 0.15s;
    }
    .archive-day-card:hover { border-color: #e0a040; }
    .archive-day-card .adc-thumb {
        position: relative;
        background: #21262d;
        min-height: 100px;
        display: flex; align-items: center; justify-content: center;
        overflow: hidden;
    }
    .archive-day-card .adc-thumb img {
        width: 100%; height: 100%; object-fit: cover;
    }
    .archive-day-card .adc-thumb-fallback {
        color: #e0a040; font-size: 2.5rem; font-weight: 600;
    }
    .archive-day-card .adc-body {
        padding: 1rem 1rem 1rem 0; display: flex; flex-direction: column; justify-content: center;
    }
    .archive-day-card .adc-header { display: flex; align-items: baseline; gap: 0.75rem; }
    .archive-day-card .adc-num   { color: #e0a040; font-weight: 600; font-size: 0.95rem; }
    .archive-day-card .adc-date  { color: #8b949e; font-size: 0.8rem; }
    .archive-day-card .adc-title { color: #f0dbb8; font-weight: 600; font-size: 1.1rem; margin: 0.2rem 0; }
    .archive-day-card .adc-stats { color: #8b949e; font-size: 0.8rem; }
    @media (max-width: 600px) {
        .archive-day-card { grid-template-columns: 90px 1fr; }
        .archive-day-card .adc-thumb { min-height: 80px; }
        .archive-day-card .adc-title { font-size: 1rem; }
    }

Edit 2 — _rallyTotals. Inside the IIFE, after the renderCurrentDriverWidget helpers from Plan 19-09, add:

    function _rallyTotals(events, fuel, route, agenda) {
        var totals = {
            km: 0,
            eventCount: (events || []).length,
            fuelLitres: 0,
            driverCount: 0,
            dayCount: (agenda && agenda.days) ? agenda.days.length : 0,
            startDate: agenda && agenda.rally ? agenda.rally.start_date : null,
            endDate:   agenda && agenda.rally ? agenda.rally.end_date   : null
        };
        if (route && route.days) {
            Object.keys(route.days).forEach(function(date) {
                var pts = route.days[date].points;
                if (!pts || pts.length < 2) return;
                for (var i = 1; i < pts.length; i++) {
                    totals.km += _haversineKm(pts[i-1][0], pts[i-1][1], pts[i][0], pts[i][1]);
                }
            });
        }
        (fuel || []).forEach(function(f) {
            if (f.volume_litres != null) totals.fuelLitres += f.volume_litres;
        });
        var drivers = {};
        (events || []).forEach(function(ev) {
            if (ev.active_driver) drivers[ev.active_driver] = true;
        });
        totals.driverCount = Object.keys(drivers).length;
        return totals;
    }

Commit: `feat(19-10): archive-mode CSS + _rallyTotals aggregator`.
  </action>
  <verify>
    <automated>grep -c '\.archive-overview' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c '\.archive-day-card' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'function _rallyTotals' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && ! grep -E 'cost_aud|price_aud' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html</automated>
  </verify>
  <acceptance_criteria>
    - File contains `.archive-overview`, `.archive-stats`, `.archive-day-grid`, `.archive-day-card` CSS selectors
    - File contains exactly one `function _rallyTotals(events, fuel, route, agenda)`
    - `_rallyTotals` sums km across all `route.days` using `_haversineKm`
    - `_rallyTotals` reads ONLY `volume_litres` from fuel (no cost/price fields)
    - `_rallyTotals` counts distinct drivers from `events[].active_driver`
    - Mobile CSS tightens thumbnail column to 90px
    - Grep check: `cost_aud` / `price_aud` remain absent from the file
  </acceptance_criteria>
  <done>CSS + aggregator landed. renderHomepageArchive composed in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: renderHomepageArchive + wire archive branch in renderHomepage dispatch</name>
  <files>~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html</files>
  <read_first>
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html (confirm Plan 19-09 renderHomepage placeholder for archive branch — replace just that branch)
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html (confirm `computeDayStats`, `_formatShortDate`, `navigateToDay`, `escapeHtml` all exist)
  </read_first>
  <action>
Edit 1 — renderHomepageArchive. Add after `_rallyTotals`:

    function renderHomepageArchive() {
        var rally  = (agendaData && agendaData.rally) || {};
        var days   = (agendaData && agendaData.days) || [];
        var totals = _rallyTotals(eventsData, fuelData, routeData, agendaData);

        var overviewHtml =
            '<section class="archive-overview">' +
                '<h1>' + escapeHtml(rally.title || 'Rally') + '</h1>' +
                '<div class="archive-subtitle">' +
                    escapeHtml(rally.start_date || '') +
                    (rally.end_date ? ' — ' + escapeHtml(rally.end_date) : '') +
                    (rally.team ? ' · Team ' + escapeHtml(rally.team) : '') +
                '</div>' +
                '<div class="archive-stats">' +
                    '<div class="day-stat-card"><div class="stat-label">Distance</div><div class="stat-value">' + totals.km.toFixed(0) + ' km</div></div>' +
                    '<div class="day-stat-card"><div class="stat-label">Days</div><div class="stat-value">' + totals.dayCount + '</div></div>' +
                    '<div class="day-stat-card"><div class="stat-label">Events</div><div class="stat-value">' + totals.eventCount + '</div></div>' +
                    '<div class="day-stat-card"><div class="stat-label">Fuel</div><div class="stat-value">' + totals.fuelLitres.toFixed(0) + ' L</div></div>' +
                    '<div class="day-stat-card"><div class="stat-label">Drivers</div><div class="stat-value">' + totals.driverCount + '</div></div>' +
                '</div>' +
            '</section>';

        // Day grid: newest-first. Sort descending by date.
        var sortedDays = days.slice().sort(function(a, b) { return b.date.localeCompare(a.date); });
        var gridParts = ['<section><h3 class="day-section-heading">Day by day</h3><div class="archive-day-grid">'];
        sortedDays.forEach(function(d) {
            var stats = computeDayStats(d.date, eventsData, routeData, fuelData);
            var heroSrc = '/captures/' + d.date + '/hero.jpg';
            var fallbackSrc = '/captures/' + d.date + '/timelapse-poster.jpg';
            var thumb =
                '<div class="adc-thumb">' +
                    '<img src="' + heroSrc + '" alt="" ' +
                        'onerror="if(this.dataset.step!==\'fallback\'){this.dataset.step=\'fallback\';this.src=\'' + fallbackSrc + '\';}else{this.style.display=\'none\';this.parentNode.innerHTML=\'<div class=adc-thumb-fallback>Day ' + Number(d.day_number) + '</div>\';}">' +
                '</div>';
            var statsLine =
                stats.km.toFixed(0) + ' km · ' +
                stats.eventCount + ' events · ' +
                stats.fuelBurned.toFixed(1) + ' L';
            gridParts.push(
                '<a class="archive-day-card" href="/day/' + d.date + '" onclick="event.preventDefault(); navigateToDay(\'' + d.date + '\');">' +
                    thumb +
                    '<div class="adc-body">' +
                        '<div class="adc-header">' +
                            '<div class="adc-num">Day ' + String(Number(d.day_number) || 0) + '</div>' +
                            '<div class="adc-date">' + escapeHtml(_formatShortDate(d.date)) + '</div>' +
                        '</div>' +
                        '<div class="adc-title">' + escapeHtml(d.title || '') + '</div>' +
                        '<div class="adc-stats">' + escapeHtml(statsLine) + '</div>' +
                    '</div>' +
                '</a>'
            );
        });
        gridParts.push('</div></section>');

        var html = '<div class="day-page">' + overviewHtml + gridParts.join('') + '</div>';
        showRoute(html);
    }

Edit 2 — wire archive branch into renderHomepage. Find the Plan 19-09 renderHomepage and replace the archive placeholder. The final renderHomepage body becomes:

    function renderHomepage() {
        _teardownDayMap();
        _stopCountdown();
        _stopLiveRefresh();
        renderDayNav(agendaData, null);

        var mode = detectMode(agendaData, routeData && routeData.generated_at);

        if (!agendaData) {
            showRoute('<div class="day-page"><section class="day-header"><h1>Shit of Theseus</h1><div class="day-subtitle">Agenda loading…</div></section></div>');
            return;
        }

        if (mode === 'before') { renderHomepageBefore(agendaData); return; }
        if (mode === 'live')   { renderHomepageLive(); return; }
        if (mode === 'archive' || mode === 'unknown') { renderHomepageArchive(); return; }

        // Defensive fallback — should never hit.
        renderHomepageArchive();
    }

Commit: `feat(19-10): renderHomepageArchive + final renderHomepage mode dispatch`.
  </action>
  <verify>
    <automated>grep -c 'function renderHomepageArchive' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c "mode === 'archive'" ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'renderHomepageArchive()' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | awk '{exit ($1 >= 2) ? 0 : 1}' && ! grep 'archive homepage lands in Plan 19-10' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly one `function renderHomepageArchive()`
    - renderHomepage dispatches to renderHomepageArchive on 'archive' OR 'unknown' modes
    - Placeholder text 'archive homepage lands in Plan 19-10' NO LONGER appears in the file
    - Day grid is sorted newest-first (descending by `d.date`)
    - Each day card has img onerror fallback chain: hero.jpg then timelapse-poster.jpg then text fallback
    - Overview stats include all 5 tiles: Distance, Days, Events, Fuel, Drivers
    - Every agenda string field (title, team, start_date, end_date, day.title) passes through escapeHtml
    - Browser check artificial archive mode (point agenda.rally.end_date to yesterday in DevTools): `/` renders overview card + day grid sorted newest-first
    - Browser check: clicking any day card navigates to `/day/YYYY-MM-DD` via pushState (no full reload)
    - Browser check: 404s in Network tab for missing hero.jpg fall back gracefully to placeholder card — no broken image icon visible in UI
    - Mobile viewport 375px: day cards re-grid to 90px thumbnail column, titles still readable
  </acceptance_criteria>
  <done>Archive-mode homepage landed: overview + linear day grid with thumbnail fallback chain. Placeholder text purged. All three modes (before, live, archive) now wired.</done>
</task>

</tasks>

<verification>
Plan 19-10 complete when:
- All grep checks pass
- All three modes render: before (countdown), live (today-as-homepage), archive (overview + grid)
- Artificial archive test: set rally.end_date to yesterday → `/` renders archive homepage
- Clicking any day card navigates via pushState (no reload)
- Thumbnail fallback chain behaves: missing hero.jpg → tries timelapse-poster.jpg → renders text placeholder
- Overview totals match expected values (compare against raw events.json/fuel.json counts)
- cost_aud / price_aud remain absent from file
</verification>

<success_criteria>
- NARR-01 (archive-leg): Homepage in archive-mode shows overview stats + linear day grid (D-18).
- renderHomepage mode dispatch is COMPLETE — all three modes wired.
</success_criteria>

<output>
After completion, create `.planning/phases/19-website-narrative-rebuild/19-10-SUMMARY.md`. Note any thumbnail/hero-image conventions adopted for future backfill work.
</output>
