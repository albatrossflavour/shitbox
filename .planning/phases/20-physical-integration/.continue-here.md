---
context: phase
phase: 20-physical-integration
task: null
total_tasks: null
status: paused
last_updated: "2026-04-21T10:25:47.137Z"
---

<current_state>
Two unrelated threads paused together when context hit 96%:

1. **Phase 20 Plan 01 revision — Pi 5 case pre-print audit** (`hardware/pi-case.scad`)
   Uncommitted. PCB tolerance bumped, connector wall assignments fixed (HDMI offsets were measured from the wrong edge; USB-C was on the wrong wall entirely). Not yet rendered in GUI, not yet printed.

2. **Grafana basic-auth refactor** (ad-hoc, not a planned phase)
   Code side uncommitted: `config/config.yaml`, `src/shitbox/utils/config.py`, `src/shitbox/sync/grafana.py`, `src/shitbox/events/engine.py`. k8s side already committed in home-ops (3c85c69).
   Not lint/test/run-tested yet. Pi deploy step not done.

Also touched: `.planning/STATE.md` (minor session-continuity stamps, no content change).
</current_state>

<completed_work>

- Pi 5 case review pass: PCB_TOL 0.6 -> 1.0 (first-print margin); HDMI X-offsets re-anchored from USB/Ethernet short edge; USB-C moved from right short wall to front long wall; FLANGE_THICK 2.0 -> 3.0 decoupled from FLOOR; counterbore depth now a named constant (BOLT_HEAD_H = 1.5); cable slots resized with 1-2mm margin; comment explains intentional HDMI slot merge at 13.5mm centre spacing
- Grafana sidebar hidden on telemetry iframe (`?kiosk` full kiosk) — committed in home-ops 3c85c69
- `GrafanaConfig` / `EngineConfig` flat fields: `api_token` replaced with `username` + `password_file`
- `GrafanaAnnotator`: reads password from file at init; logs `grafana_password_file_unreadable` / `grafana_password_empty` and disables auth gracefully; passes `auth=(user, pw)` tuple to `requests.post`
- `config/config.yaml` grafana block updated: url fixed to `grafana.shit-of-theseus.com`, enabled flipped to true, username + password_file fields added
- home-ops: ExternalSecret `grafana-shitbox-admin` wired via `onepassword-connect` ClusterSecretStore, templating `admin-user` / `admin-password` from `GRAFANA_ADMIN_USERNAME` / `GRAFANA_ADMIN_PASSWORD` fields (matches existing observability/grafana pattern); helmrelease patched with `admin.existingSecret` + `# gitleaks:allow` on the `passwordKey: admin-password` line; kustomization includes the new ExternalSecret
</completed_work>

<remaining_work>

**Shitbox side (code):**
1. `ruff check src/` and `mypy src/` against the Grafana refactor
2. `pytest` to make sure the config-loading path still imports
3. Commit split: one commit for `hardware/pi-case.scad` (Phase 20-01 revision), one for the Grafana code changes (unrelated, keep it atomic)

**Pi deploy (laser):**
4. Write `/etc/shitbox/grafana-password` (root:root, 600) with the admin password from the Discworld `grafana` 1P item
5. `sudo systemctl restart shitbox-telemetry`
6. Tail logs for `grafana_password_file_unreadable` / `grafana_annotation_sent`

**Smoke test:**
7. `curl -u admin:<pw> -X POST https://grafana.shit-of-theseus.com/api/annotations -H 'Content-Type: application/json' -d '{"text":"smoke test","tags":["manual"]}'` should return 200

**Phase 20 Plan 01 follow-on (physical):**
8. Slice and print `pi-case.scad` on Bambu P1S in PETG
9. Mock-fit full Pi 5 stack — particularly HDMI adapter clearance, USB-C cable angle, GX12 + SMA bulkhead fit, M4 flange hole alignment to plywood panel
</remaining_work>

<decisions_made>

- **Grafana auth = admin basic-auth via password_file, not API token.** Pod has no PVC, so SA tokens vanish on restart. `admin.existingSecret` keeps the password stable across pod restarts; Pi reads it from a local file (offline-first compatible).
- **Reuse Discworld `grafana` 1P entry.** Same pattern as existing observability/grafana ExternalSecret. Shares admin password between the two Grafanas — acceptable.
- **`PCB_TOL` 0.6 -> 1.0.** Every prior print needed a second attempt. More clearance now, tighten later if sloppy.
- **HDMI slots merge intentionally.** 13.5mm centre spacing vs 15mm slot width -> one merged opening. Only one HDMI is used at a time, bridge is 29mm PETG — fine.
- **USB-C moved walls.** Was on right short wall in the original file; belongs on the front long wall next to the HDMI ports. Matches actual Pi 5 layout.
</decisions_made>

<blockers>

- Pi case changes unverifiable from laptop — needs a print + mock-fit. Geometry is sound on paper (OpenSCAD render in prior session was manifold, Genus 40, clean).
- Grafana annotator cannot be runtime-tested until the password file exists on the Pi — before that, auth will be `None` and POSTs will 401.
- Shared admin password between observability and shit-of-theseus Grafana assumes the Discworld `grafana` 1P entry is authoritative. If the observability one was set differently, they'll collide on next reconcile.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured snapshot of what's done / what's pending / what's uncommitted
2. `.planning/phases/20-physical-integration/20-01-PLAN.md` — original Plan 01 context for the Pi case
3. `.planning/phases/20-physical-integration/20-01-SUMMARY.md` — what Plan 01 shipped originally (this pause adds a revision pass)
4. `git diff hardware/pi-case.scad` — the uncommitted pre-print changes
5. `git diff src/shitbox/sync/grafana.py src/shitbox/utils/config.py src/shitbox/events/engine.py config/config.yaml` — the uncommitted Grafana refactor

## Critical Anti-Patterns (do NOT repeat these)

- **Asking for confirmation on well-known hardware layout** — When the Pi 5 mechanical drawing is unambiguous, just apply it. Don't hedge with "can you confirm HDMI is on the long edge." The user pushed back on this explicitly mid-session.
- **Inventing 1P entry field shapes** — Always look at an existing ExternalSecret in the same repo before naming template fields. The first attempt used `.username` / `.password` when the house style is `GRAFANA_ADMIN_USERNAME` / `GRAFANA_ADMIN_PASSWORD`.

## Infrastructure State

- **home-ops**: clean, grafana-shitbox-admin ExternalSecret and admin.existingSecret patch committed as 3c85c69. Flux will reconcile once pushed — confirm with `git -C ~/dev/home-ops log origin/main..HEAD` before assuming it's live.
- **shitbox repo**: 6 files dirty (see `uncommitted_files` in HANDOFF.json). Branch `gsd/phase-21-hardware-inventory-and-graceful-degradation` — note that this is the phase 21 branch; phase 21 is already closed and verified. Consider either merging to main before the Phase 20 work, or staying on it and merging the whole bundle later.
- **Pi (laser)**: no `/etc/shitbox/grafana-password` file yet. Service running with old code. Will need restart post-deploy.
- **Grafana (shit-of-theseus.com)**: running. Admin password may or may not already be set from the existing 1P entry — if helmrelease reconciles before 1P entry is verified, admin login may break.

<context>
The pause happened mid-bootstrap of a small infra thread that got tacked onto the Phase 20 Pi-case review. Both threads are logically independent; the Grafana work was triggered by noticing an unused `sync.grafana` config block during the case review, and it ballooned into a full refactor + k8s bootstrap. Keep them in separate commits.

The home-ops side landed cleanly (including gitleaks handling for the `admin-password` key name). The shitbox side is the remaining unknown — code changes look structurally right but have not been exercised. Lint first, then test imports, then commit.
</context>

<next_action>
Start with: `ruff check src/ && mypy src/ && pytest -x` from the shitbox repo root. If any failures land in the Grafana refactor path, fix those first before anything else. Once green, split commits: one for the pi-case.scad revision (Phase 20-01 follow-on), one for the Grafana basic-auth refactor (engine + config + sync + config.yaml). Then move to the Pi deploy step (password file + service restart + smoke test).
</next_action>
