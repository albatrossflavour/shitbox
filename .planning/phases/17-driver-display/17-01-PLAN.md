---
phase: 17-driver-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_thermal_monitor.py
  - tests/test_dashboard.py
  - tests/test_engine_boot.py
  - src/shitbox/events/engine.py
  - src/shitbox/health/thermal_monitor.py
autonomous: true
requirements: [DISP-01, DISP-02, DISP-03, DISP-04]
must_haves:
  truths:
    - "DS18B20 TEMPERATURE readings populate _cabin_temp_c so imu_temp is non-null when BME680 is absent (DISP-01, D-09)"
    - "Thermal warning, thermal critical, and undervoltage in thermal_monitor each call dashboard_push_event() with type='ALERT' payload (DISP-04, D-05)"
    - "Wave 0 test stubs exist for ticker max-5, sse_slow active_driver key, thermal/undervoltage alert bridge, and DS18B20 cabin temp fallback"
  artifacts:
    - path: "src/shitbox/events/engine.py"
      provides: "_on_reading branch for SensorType.TEMPERATURE updating _cabin_temp_c"
      contains: "SensorType.TEMPERATURE"
    - path: "src/shitbox/health/thermal_monitor.py"
      provides: "dashboard_push_event import and three ALERT call sites"
      contains: "dashboard_push_event"
    - path: "tests/test_thermal_monitor.py"
      provides: "test_thermal_warning_pushes_dashboard_alert + test_undervoltage_pushes_dashboard_alert"
      contains: "test_thermal_warning_pushes_dashboard_alert"
    - path: "tests/test_dashboard.py"
      provides: "test_event_ticker_max_five + test_sse_slow_has_active_driver_key"
      contains: "test_event_ticker_max_five"
    - path: "tests/test_engine_boot.py"
      provides: "test_on_reading_temperature_updates_cabin_temp"
      contains: "test_on_reading_temperature_updates_cabin_temp"
  key_links:
    - from: "src/shitbox/health/thermal_monitor.py"
      to: "src/shitbox/dashboard/sse.py push_event"
      via: "try/except import of push_event as dashboard_push_event"
      pattern: "from shitbox.dashboard.sse import push_event"
    - from: "src/shitbox/events/engine.py _on_reading"
      to: "self._cabin_temp_c"
      via: "elif SensorType.TEMPERATURE branch"
      pattern: "SensorType\\.TEMPERATURE"
---

<objective>
Backend fixes and Wave 0 test scaffolds for Phase 17 driver display.

Purpose: Unblock the kiosk UI (Plan 17-02) by fixing the temperature snapshot bug (D-09) and
wiring thermal/undervoltage alerts into the SSE events stream (D-05). Also lands the Wave 0
test files the frontend plan and verification step depend on.

Output: engine.py and thermal_monitor.py updated; four new tests in three test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-driver-display/17-CONTEXT.md
@.planning/phases/17-driver-display/17-RESEARCH.md

@src/shitbox/events/engine.py
@src/shitbox/health/thermal_monitor.py
@src/shitbox/dashboard/sse.py
@src/shitbox/storage/models.py
@src/shitbox/collectors/environment.py

<interfaces>
From src/shitbox/dashboard/sse.py:
```python
def push_event(event: dict) -> None:
    """Non-blocking, thread-safe. Uses queue.Queue.put_nowait(). Drop-on-full."""
```

From src/shitbox/storage/models.py (Reading):
```python
class Reading:
    sensor_type: SensorType   # ENVIRONMENT | TEMPERATURE | ...
    env_temp_celsius: Optional[float]  # set for ENVIRONMENT (BME680)
    temp_celsius: Optional[float]      # set for TEMPERATURE (DS18B20)
```

From src/shitbox/events/engine.py (current _on_reading, approx line 769):
```python
if reading.sensor_type == SensorType.ENVIRONMENT and reading.env_temp_celsius is not None:
    self._cabin_temp_c = reading.env_temp_celsius
```

Existing imports at top of thermal_monitor.py already follow try/except ImportError pattern
for buzzer and speaker modules — mirror that for dashboard_push_event.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — write failing tests for temperature fallback, alert bridge, ticker, and active_driver SSE key</name>
  <files>tests/test_engine_boot.py, tests/test_thermal_monitor.py, tests/test_dashboard.py</files>
  <read_first>
    - tests/test_engine_boot.py (current structure, fixtures, how UnifiedEngine is built in tests)
    - tests/test_thermal_monitor.py (existing ThermalMonitor test patterns and mocks for buzzer/speaker)
    - tests/test_dashboard.py (existing SSE schema tests and events array assertions)
    - src/shitbox/events/engine.py (locate _on_reading and _cabin_temp_c field)
    - src/shitbox/health/thermal_monitor.py (locate _check_thermal and _check_throttled, see how TTS is mocked)
    - src/shitbox/storage/models.py (Reading + SensorType — confirm TEMPERATURE has temp_celsius field)
    - src/shitbox/dashboard/sse.py (push_event signature and module path)
  </read_first>
  <action>
    Add the four Wave 0 tests. Every test must FAIL on current main (they are RED-phase stubs
    for tasks 2 and 3).

    1. tests/test_engine_boot.py — add `test_on_reading_temperature_updates_cabin_temp`:
       - Construct or stub enough of UnifiedEngine to call `_on_reading()` directly. If the
         existing file already constructs an engine via a helper, reuse it; otherwise build
         a minimal instance with `database = MagicMock()`.
       - Create a `Reading(sensor_type=SensorType.TEMPERATURE, temp_celsius=42.5, ...)`
         (fill any other required fields with sensible defaults from the Reading model).
       - Call `engine._on_reading(reading)`.
       - Assert `engine._cabin_temp_c == 42.5`.
       - Also assert the existing ENVIRONMENT branch still works by passing
         `Reading(sensor_type=SensorType.ENVIRONMENT, env_temp_celsius=30.0)` and checking
         `engine._cabin_temp_c == 30.0`.

    2. tests/test_thermal_monitor.py — add two tests:
       a. `test_thermal_warning_pushes_dashboard_alert`:
          - Patch `shitbox.health.thermal_monitor.dashboard_push_event` with a MagicMock.
          - Patch buzzer/speaker calls the same way the existing thermal tests do (to prevent
            hardware side effects).
          - Construct a ThermalMonitor, force `_check_thermal()` into the warning branch
            (stub the CPU temp reader to return 72.0 or whatever exceeds the warning threshold
            but is below critical).
          - Assert `dashboard_push_event` was called once with a dict containing
            `{"type": "ALERT", "subtype": "THERMAL_WARNING"}` and a `message` key that is a
            string containing "72" (or the numeric temp).
       b. `test_undervoltage_pushes_dashboard_alert`:
          - Same patch setup.
          - Force `_check_throttled()` into the undervoltage branch by stubbing the throttle
            bitmask reader to return a value with bit 0 set (e.g. 0x1).
          - Assert `dashboard_push_event` called with
            `{"type": "ALERT", "subtype": "UNDERVOLTAGE"}` and a message string containing
            "UNDERVOLTAGE".

       If the existing thermal_monitor tests already have a fixture that builds a monitor
       with all hardware mocked, reuse it. Otherwise define a local fixture.

    3. tests/test_dashboard.py — add two tests:
       a. `test_sse_slow_has_active_driver_key`:
          - If the existing `test_sse_slow_schema` covers this, extend it rather than
            duplicating. Otherwise add a new test that asserts `"active_driver"` is present
            in the dict returned by the slow SSE payload builder.
       b. `test_event_ticker_max_five`:
          - This is a frontend behaviour test. Since we do not have JS test infrastructure,
            implement it as a regex/string check on `src/shitbox/dashboard/static/index.html`
            asserting that the events array pop threshold is 5, not 10. Specifically:
            `assert re.search(r"events\.length\s*>\s*5", html)` and
            `assert "events.length > 10" not in html`.
          - This test will FAIL on current main (current value is 10) and PASS once
            Plan 17-02 updates the threshold. Mark it clearly with a docstring
            referencing DISP-02 and D-07.

    All four tests must be runnable via `pytest tests/test_engine_boot.py
    tests/test_thermal_monitor.py tests/test_dashboard.py -x`. They are expected to FAIL
    until tasks 2 and 3 (this plan) and Plan 17-02 complete.
  </action>
  <verify>
    <automated>pytest tests/test_engine_boot.py::test_on_reading_temperature_updates_cabin_temp tests/test_thermal_monitor.py::test_thermal_warning_pushes_dashboard_alert tests/test_thermal_monitor.py::test_undervoltage_pushes_dashboard_alert tests/test_dashboard.py -x --collect-only</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def test_on_reading_temperature_updates_cabin_temp" tests/test_engine_boot.py` exits 0
    - `grep -q "def test_thermal_warning_pushes_dashboard_alert" tests/test_thermal_monitor.py` exits 0
    - `grep -q "def test_undervoltage_pushes_dashboard_alert" tests/test_thermal_monitor.py` exits 0
    - `grep -q "def test_event_ticker_max_five" tests/test_dashboard.py` exits 0
    - `grep -q "active_driver" tests/test_dashboard.py` exits 0
    - `pytest tests/test_engine_boot.py tests/test_thermal_monitor.py tests/test_dashboard.py --collect-only -q` shows all four new test names
  </acceptance_criteria>
  <done>Four tests exist and are collected by pytest; they fail on current main (confirming they are real RED tests).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix DS18B20 cabin temp fallback in engine._on_reading</name>
  <files>src/shitbox/events/engine.py</files>
  <read_first>
    - src/shitbox/events/engine.py (read full _on_reading method and surrounding lines, locate SensorType import, find _cabin_temp_c declaration)
    - src/shitbox/storage/models.py (Reading class — confirm temp_celsius is the attribute name for DS18B20 readings)
    - src/shitbox/collectors/temperature.py (confirm to_reading() emits SensorType.TEMPERATURE with temp_celsius set)
    - src/shitbox/collectors/environment.py (already read — BME680 retry loop already present, 5x1s, no changes needed here)
    - tests/test_engine_boot.py (the new test added in Task 1)
  </read_first>
  <behavior>
    - When a Reading with sensor_type=SensorType.TEMPERATURE and temp_celsius=42.5 is passed to
      _on_reading, engine._cabin_temp_c becomes 42.5.
    - The existing ENVIRONMENT branch is unchanged — Reading(ENVIRONMENT, env_temp_celsius=30.0)
      still sets _cabin_temp_c to 30.0.
    - If BME680 (ENVIRONMENT) later emits a reading, it overwrites the DS18B20 value (last-write
      wins — acceptable per RESEARCH Pitfall 3).
  </behavior>
  <action>
    In `src/shitbox/events/engine.py`, locate the `_on_reading(self, reading: Reading)` method.
    The current body contains:

    ```python
    if reading.sensor_type == SensorType.ENVIRONMENT and reading.env_temp_celsius is not None:
        self._cabin_temp_c = reading.env_temp_celsius
    ```

    Add an `elif` branch immediately after it:

    ```python
    elif reading.sensor_type == SensorType.TEMPERATURE and reading.temp_celsius is not None:
        # DS18B20 fallback — keeps the kiosk "cabin temp" tile populated until BME680
        # is reliable (D-09, Phase 17). Last-write wins if both sensors fire.
        self._cabin_temp_c = reading.temp_celsius
    ```

    Do NOT change the ENVIRONMENT branch. Do NOT add any logging that would fire at 1 Hz in
    the hot path (keep the change to the three lines above). Keep the existing try/except
    wrapping around `self.database.insert_reading(reading)` intact.

    Confirm `SensorType.TEMPERATURE` is already importable in engine.py (it should be — the
    same enum already provides `SensorType.ENVIRONMENT`). If not, add it to the existing
    `from shitbox.storage.models import ...` import line.

    Do NOT modify environment.py — the BME680 retry loop (5 attempts, 1s delay) is already
    in place from the earlier hardware stabilisation session (commit ae7676f / f98ef47).
    If BME680 still fails to initialise on boot, that is a separate hardware/boot-timing
    issue; the DS18B20 fallback added here is sufficient for DISP-01.
  </action>
  <verify>
    <automated>pytest tests/test_engine_boot.py::test_on_reading_temperature_updates_cabin_temp -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SensorType.TEMPERATURE" src/shitbox/events/engine.py` shows the new branch inside _on_reading
    - `grep -n "reading.temp_celsius" src/shitbox/events/engine.py` shows at least one match
    - The ENVIRONMENT branch still present: `grep -q "SensorType.ENVIRONMENT and reading.env_temp_celsius" src/shitbox/events/engine.py` exits 0
    - `pytest tests/test_engine_boot.py::test_on_reading_temperature_updates_cabin_temp -x` exits 0
    - `ruff check src/shitbox/events/engine.py` exits 0
  </acceptance_criteria>
  <done>DS18B20 readings populate _cabin_temp_c; RED test from Task 1 now GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bridge thermal and undervoltage alerts into SSE via dashboard_push_event</name>
  <files>src/shitbox/health/thermal_monitor.py</files>
  <read_first>
    - src/shitbox/health/thermal_monitor.py (read full file — find _check_thermal, _check_throttled, existing try/except import blocks for buzzer/speaker, current temp variable names)
    - src/shitbox/dashboard/sse.py (confirm `push_event(event: dict)` signature and that it is importable as `from shitbox.dashboard.sse import push_event`)
    - tests/test_thermal_monitor.py (the two new tests added in Task 1 — match patch target paths exactly)
  </read_first>
  <behavior>
    - When _check_thermal() crosses the warning threshold, dashboard_push_event is called
      with {"type": "ALERT", "subtype": "THERMAL_WARNING", "message": "THERMAL WARNING: {temp}°C", "ts": <float>}
    - When _check_thermal() crosses the critical threshold, dashboard_push_event is called
      with subtype="THERMAL_CRITICAL" and a message containing "THERMAL CRITICAL"
    - When _check_throttled() detects undervoltage (bit 0 of throttle bitmask), dashboard_push_event
      is called with subtype="UNDERVOLTAGE" and message "UNDERVOLTAGE DETECTED"
    - If shitbox.dashboard.sse cannot be imported, the module still loads and the calls become no-ops
  </behavior>
  <action>
    In `src/shitbox/health/thermal_monitor.py`:

    1. Near the top of the file, alongside the existing try/except ImportError blocks for
       buzzer/speaker imports, add:

       ```python
       try:
           from shitbox.dashboard.sse import push_event as dashboard_push_event
       except ImportError:
           def dashboard_push_event(event: dict) -> None:  # type: ignore[misc]
               pass
       ```

       Also ensure `import time` is already present (it almost certainly is — if not, add it).

    2. In `_check_thermal()`, locate the warning branch. It currently contains:
       ```python
       beep_thermal_warning()
       speak_thermal_warning()
       ```
       Immediately after those two calls, add:
       ```python
       dashboard_push_event({
           "type": "ALERT",
           "subtype": "THERMAL_WARNING",
           "message": f"THERMAL WARNING: {round(temp, 1)}°C",
           "ts": time.time(),
       })
       ```
       Use whatever the local variable name is for the current CPU temperature in that
       method (commonly `temp` or `cpu_temp` — use the existing name, do not rename).

    3. In the critical branch of `_check_thermal()`, after `beep_thermal_critical()` and
       `speak_thermal_critical()`, add:
       ```python
       dashboard_push_event({
           "type": "ALERT",
           "subtype": "THERMAL_CRITICAL",
           "message": f"THERMAL CRITICAL: {round(temp, 1)}°C",
           "ts": time.time(),
       })
       ```

    4. In `_check_throttled()`, after `beep_under_voltage()` and `speak_under_voltage()`,
       add:
       ```python
       dashboard_push_event({
           "type": "ALERT",
           "subtype": "UNDERVOLTAGE",
           "message": "UNDERVOLTAGE DETECTED",
           "ts": time.time(),
       })
       ```

    Do NOT add any new rate-limiting. The existing rate-limiting around the TTS/beep calls
    also gates the dashboard push because the new calls sit inside the same conditional
    blocks. Do NOT change bit masking for the throttle check — leave it as-is (PWR-01 is
    Phase 15, not this phase).

    Do NOT remove or modify the existing buzzer/speaker calls. The dashboard push is
    additive, not a replacement.
  </action>
  <verify>
    <automated>pytest tests/test_thermal_monitor.py::test_thermal_warning_pushes_dashboard_alert tests/test_thermal_monitor.py::test_undervoltage_pushes_dashboard_alert -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dashboard_push_event" src/shitbox/health/thermal_monitor.py` returns 4 or more (one import/fallback + three call sites)
    - `grep -q "from shitbox.dashboard.sse import push_event as dashboard_push_event" src/shitbox/health/thermal_monitor.py` exits 0
    - `grep -q '"subtype": "THERMAL_WARNING"' src/shitbox/health/thermal_monitor.py` exits 0
    - `grep -q '"subtype": "THERMAL_CRITICAL"' src/shitbox/health/thermal_monitor.py` exits 0
    - `grep -q '"subtype": "UNDERVOLTAGE"' src/shitbox/health/thermal_monitor.py` exits 0
    - Existing calls intact: `grep -q "speak_thermal_warning" src/shitbox/health/thermal_monitor.py` and `grep -q "speak_under_voltage" src/shitbox/health/thermal_monitor.py` both exit 0
    - `pytest tests/test_thermal_monitor.py -x` exits 0
    - `ruff check src/shitbox/health/thermal_monitor.py` exits 0
  </acceptance_criteria>
  <done>Three alert call sites push ALERT events into the SSE queue; both RED thermal tests now GREEN.</done>
</task>

</tasks>

<verification>
Run:
```bash
pytest tests/test_engine_boot.py tests/test_thermal_monitor.py tests/test_dashboard.py -x
ruff check src/shitbox/events/engine.py src/shitbox/health/thermal_monitor.py
```
All specified tests pass (except test_event_ticker_max_five which will remain RED until Plan 17-02).
</verification>

<success_criteria>
- DS18B20 TEMPERATURE readings populate _cabin_temp_c (DISP-01, D-09)
- thermal_monitor.py pushes ALERT events on warning, critical, and undervoltage (DISP-04, D-05)
- Wave 0 tests exist for all four behaviours flagged in VALIDATION.md
- Three of four Wave 0 tests are GREEN at plan end (event_ticker_max_five is deliberately
  left RED for Plan 17-02)
</success_criteria>

<output>
Create `.planning/phases/17-driver-display/17-01-SUMMARY.md`
</output>
