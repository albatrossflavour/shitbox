---
phase: 23-verification-closure-and-traceability-sweep
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md
autonomous: true
requirements: [NARR-08b]

must_haves:
  truths:
    - "Phase 19 VERIFICATION.md no longer reports NARR-08b as BLOCKED or FAILED"
    - "Phase 19 VERIFICATION.md contains evidence citing `src/shitbox/storage/route.py:22-28, 121, 153-157` for the home-address proximity filter"
    - "Phase 19 VERIFICATION.md frontmatter reflects the closed state (status: passed or equivalent non-gap state) and score increments from 10/11 to 11/11"
  artifacts:
    - path: ".planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md"
      provides: "Corrected verification report with NARR-08b satisfied"
      contains: "status: passed"
  key_links:
    - from: ".planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md"
      to: "src/shitbox/storage/route.py"
      via: "evidence line-range citations"
      pattern: "route\\.py:22-28|route\\.py:121|route\\.py:153-157"
---

<objective>
Correct the stale BLOCKED entry for NARR-08b in `.planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md`. The home-address proximity filter is implemented in `src/shitbox/storage/route.py` at lines 22-28 (`_haversine_m`), 121 (`_exclude_home` flag), and 153-157 (drop points within `home_exclusion_radius_m`), and wired in `engine.py:627-632`. The verifier from 2026-04-16 ran before that implementation was added; the file needs a targeted correction.

Purpose: NARR-08b is the only Phase 19 requirement still reported as BLOCKED, and the v2.0 milestone audit lists it as the one stale blocker preventing milestone close. Flipping the entry from FAILED to VERIFIED based on evidence already captured in the audit closes the gap cleanly.

Output: Edited `19-VERIFICATION.md` with: (1) frontmatter `status: passed`, score `11/11`, `gaps: []`, and a new `re_verification:` block citing 23-02 plus the audit; (2) the NARR-08b row in the Observable Truths table flipped from FAILED to VERIFIED with evidence paths; (3) the NARR-08b row in Requirements Coverage flipped from BLOCKED to SATISFIED; (4) the Anti-Patterns row for `src/shitbox/storage/route.py` removed (no longer applicable); (5) the Gaps Summary section rewritten to state zero blockers remaining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v2.0-MILESTONE-AUDIT.md
@.planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md
@src/shitbox/storage/route.py

<implementation_evidence>
The home-address proximity filter is implemented in src/shitbox/storage/route.py:

- Lines 22-28: `_haversine_m(lat1, lng1, lat2, lng2)`, great-circle distance in metres
- Line 121: `self._exclude_home = home_lat != 0.0 or home_lng != 0.0`, enable flag derived from config
- Lines 153-157: `if self._exclude_home and _haversine_m(flat, flng, self.home_lat, self.home_lng) < self.home_exclusion_radius_m:`, drops points inside the exclusion radius

Wired in src/shitbox/events/engine.py:627-632 where RouteStorage is instantiated with home_lat, home_lng, home_exclusion_radius_m from config.

The 19-VERIFICATION.md from 2026-04-16 pre-dates this implementation. The v2.0 milestone audit (2026-04-22) confirmed the code is present and wired; only the documentation is stale.
</implementation_evidence>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct NARR-08b entry in 19-VERIFICATION.md from BLOCKED to satisfied</name>
  <files>.planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md (current stale state)
    - src/shitbox/storage/route.py (confirm line numbers 22-28, 121, 153-157 still match)
    - .planning/v2.0-MILESTONE-AUDIT.md (source of corrected evidence)
  </read_first>

  <action>
Apply six targeted edits to `.planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md`. Do not rewrite the whole file, use Edit to change only these regions.

**Edit 1: Frontmatter, replace the `status`, `score`, and `gaps` fields.**

Replace the existing:
```yaml
verified: 2026-04-16T05:30:00Z
status: gaps_found
score: 10/11 must-haves verified
gaps:
  - truth: "Home-address exclusion enforced in route.json ..."
    ... (entire gap block, ends with the `missing:` list)
```

With:
```yaml
verified: 2026-04-22T00:00:00Z
status: passed
score: 11/11 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: 10/11
  previous_verified: 2026-04-16T05:30:00Z
  gaps_closed:
    - "NARR-08b: home-address proximity filter confirmed implemented at src/shitbox/storage/route.py:22-28 (_haversine_m), :121 (_exclude_home flag), :153-157 (drop points within home_exclusion_radius_m); wired in engine.py:627-632. The 2026-04-16 verification pre-dated the implementation; this correction reflects actual code state per the 2026-04-22 v2.0 milestone audit."
  gaps_remaining: []
  regressions: []
gaps: []
```

(Keep the existing `human_verification:` block in frontmatter exactly as-is. Do not remove or reorder entries there.)

**Edit 2: Observable Truths table, row 9 (NARR-08b).**

Find the row:
```
| 9 | Home-address exclusion enforced in route.json | FAILED | REQUIREMENTS.md defines NARR-08b as ...
```

Replace with:
```
| 9 | Home-address exclusion enforced in route.json | VERIFIED | `src/shitbox/storage/route.py:22-28` `_haversine_m`, `:121` `_exclude_home` flag, `:153-157` drops points where distance < `home_exclusion_radius_m`; `src/shitbox/events/engine.py:627-632` instantiates `RouteStorage` with `home_lat`/`home_lng`/`home_exclusion_radius_m` from config. Confirmed by v2.0 milestone audit 2026-04-22. |
```

Update the score line directly beneath the table from `**Score: 10/11 truths verified**` to `**Score: 11/11 truths verified**`.

**Edit 3: Requirements Coverage table, NARR-08b row.**

Find the row:
```
| NARR-08b | 19-01 | Home-address exclusion in route.json | BLOCKED | REQUIREMENTS.md defines as proximity filtering. route.py has no such logic...
```

Replace with:
```
| NARR-08b | 19-01 | Home-address exclusion in route.json | SATISFIED | Implemented in `src/shitbox/storage/route.py:22-28, 121, 153-157`; wired in `engine.py:627-632`. Confirmed by v2.0 milestone audit 2026-04-22. |
```

**Edit 4: Anti-Patterns Found table, remove the `src/shitbox/storage/route.py` Blocker row.**

Remove the entire row beginning `| src/shitbox/storage/route.py | — | No home-address exclusion logic | Blocker | NARR-08b in REQUIREMENTS.md is not satisfied...`. Leave the two `webroot/index.html` Warning rows and the `agenda.json` Warning row untouched.

**Edit 5: Gaps Summary section.**

Find the paragraph beginning `One gap blocks goal achievement:` through the end of the paragraph beginning `The real-world impact:` (i.e. the entire NARR-08b gap discussion).

Replace that section with:
```
### Gaps Summary

Zero blockers. NARR-08b was reported BLOCKED in the 2026-04-16 verification but the v2.0 milestone audit (2026-04-22) confirmed the home-address proximity filter is implemented in `src/shitbox/storage/route.py:22-28, 121, 153-157` and wired via `engine.py:627-632`. The original 2026-04-16 verifier ran before the filter was merged; this report corrects the record.

Two non-blocking warnings remain, documented in the Anti-Patterns Found table:

- `#day-videos` and `#day-timelapse` placeholder stubs in `webroot/index.html`, explicitly deferred in Plan 19-11 SUMMARY.
- Rally date placeholders in `agenda.json` (2026-05-27 → 2026-06-05 vs Brain-doc confirmed 2026-07-10), content-accuracy issue flagged in Plan 19-12 as Phase 19.1 candidate.

Neither warning blocks milestone v2.0 closure.
```

**Edit 6: Footer.**

Replace the footer lines:
```
_Verified: 2026-04-16T05:30:00Z_
_Verifier: Claude (gsd-verifier)_
```

With:
```
_Verified: 2026-04-16T05:30:00Z (initial)_
_Re-verified: 2026-04-22T00:00:00Z (Phase 23 closure, NARR-08b correction)_
_Verifier: Claude (gsd-verifier)_
```

**Formatting rules:**
- UK spelling.
- No em-dashes introduced in the edited regions (use commas or parentheses). Pre-existing em-dashes outside the six edit regions (e.g. in the `human_verification:` frontmatter block, the Data-Flow Trace table, the Behavioral Spot-Checks table, the Human Verification section) are out of scope and MUST NOT be touched.
- Preserve exact indentation in the frontmatter YAML.
- Do not touch any truth rows other than row 9, any requirement rows other than NARR-08b, or the `human_verification:` block in the frontmatter.
  </action>

  <verify>
    <automated>grep -q "^status: passed$" .planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md && ! grep -qE "(BLOCKED|FAILED).*NARR-08b|NARR-08b.*(BLOCKED|FAILED)" .planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^status: passed$" .planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md` returns exactly 1
    - `grep -c "^status: gaps_found$" .planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md` returns 0
    - `grep -E "NARR-08b.*BLOCKED|BLOCKED.*NARR-08b" .planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md` returns no matches (exit 1)
    - `grep -E "NARR-08b.*FAILED|FAILED.*NARR-08b" .planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md` returns no matches (exit 1)
    - `grep "NARR-08b" .planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md | grep -E "(VERIFIED|SATISFIED)" | wc -l` returns at least 2 (one Observable Truths row, one Requirements Coverage row)
    - `grep "route.py:22-28" .planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md` matches at least once
    - `grep "route.py:153-157" .planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md` matches at least once
    - `grep "engine.py:627-632" .planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md` matches at least once
    - `grep "11/11" .planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md` matches at least twice (frontmatter score + body score line)
    - `grep "re_verification:" .planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md` matches exactly once
    - File no longer contains the removed anti-pattern row: `grep "No home-address exclusion logic" .planning/phases/19-website-narrative-rebuild/19-VERIFICATION.md` returns no matches
  </acceptance_criteria>

  <done>
    19-VERIFICATION.md frontmatter reports `status: passed`, score `11/11`, `gaps: []`, and a `re_verification:` block. NARR-08b appears as VERIFIED/SATISFIED in both tables with evidence referring to `src/shitbox/storage/route.py:22-28, 121, 153-157` and `engine.py:627-632`. The stale Anti-Pattern row and the old Gaps Summary paragraph are removed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning markdown → git | Mis-correction of verifier output propagates; mitigated by git diff review and by the external milestone audit independently confirming the evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | T (Tampering) | 19-VERIFICATION.md | mitigate | Git diff preserves the original BLOCKED assertion and the reasoning for the correction; `re_verification:` block names the audit as source of truth |
| T-23-02-02 | I (Information Disclosure) | 19-VERIFICATION.md | accept | Public planning document; home_lat/home_lng values themselves never appear here, only the fact that the filter exists |
</threat_model>

<verification>
Run the grep checks in `acceptance_criteria`. All must pass.
</verification>

<success_criteria>
19-VERIFICATION.md reflects NARR-08b as satisfied with concrete source-line evidence; frontmatter status `passed`; score `11/11`; no BLOCKED/FAILED language near NARR-08b remains.
</success_criteria>

<output>
After completion, create `.planning/phases/23-verification-closure-and-traceability-sweep/23-02-SUMMARY.md`
</output>
