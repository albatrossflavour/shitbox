---
phase: 19
plan: "09"
type: execute
wave: 3
depends_on: ["19-02", "19-03", "19-04", "19-05", "19-06", "19-07", "19-08"]
files_modified:
  - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html
autonomous: true
requirements:
  - NARR-01
must_haves:
  truths:
    - "When detectMode returns 'live', renderHomepage dispatches to renderHomepageLive which delegates to renderDayPage(today)"
    - "Today IS the homepage during the rally — D-17 realised"
    - "A current-driver widget sits above the day-page scaffold, showing the active driver from driverStatsData"
    - "Page auto-refreshes every 2 minutes (120_000 ms) via setInterval — fetches fresh events.json + notes.json + fuel.json + route.json + driver-stats.json and re-renders"
    - "The refresh timer tears down when navigating to a specific day page (so backgrounded tabs don't hammer the Pi)"
    - "If 'today' is outside the rally window (edge case: agenda says rally ended yesterday), mode falls through to archive"
    - "Current-driver widget handles null/empty driverStatsData gracefully"
    - "Visual distinction: live-mode header shows a pulsing 'LIVE' badge so it's obvious the data is current"
  artifacts:
    - path: "~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html"
      provides: "renderHomepageLive + currentDriverWidget + _liveRefreshTimer + live-mode CSS"
      contains: "function renderHomepageLive"
  key_links:
    - from: "renderHomepage dispatch"
      to: "renderHomepageLive() when mode==='live'"
      via: "switch branch"
      pattern: "mode === 'live'"
    - from: "_liveRefreshTimer setInterval"
      to: "re-fetch all data sources + re-render"
      via: "120s tick calls refreshLiveData() which promises.all then renderHomepage()"
      pattern: "_liveRefreshTimer"
---

<objective>
Land the live-mode homepage. Per D-17, today IS the homepage during the rally. This plan makes `/` render `renderDayPage(today_iso)` with a current-driver widget prepended and a 2-minute auto-refresh loop.

Purpose: NARR-01 live-leg + D-17. The homepage is the story of RIGHT NOW — the day that is currently being driven.

Output additions to `webroot/index.html`:
- `.live-badge` pulsing indicator CSS.
- `.current-driver-widget` CSS.
- `function renderCurrentDriverWidget(driverStats)` — compact current-driver card.
- `function renderHomepageLive()` — composes live-mode: widget + day-page of today.
- `function refreshLiveData()` — re-fetches all data sources and calls renderHomepage.
- Module-level `_liveRefreshTimer = null` for 2-minute tick.
- Wire live branch into `renderHomepage()` mode dispatch (Plan 19-08 left a placeholder).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-website-narrative-rebuild/19-CONTEXT.md
@.planning/phases/19-website-narrative-rebuild/19-RESEARCH.md

<interfaces>
From Plan 19-02:
    var agendaData, routeData;

Existing data (populated by fetches in webroot/index.html):
    var eventsData, notesData, fuelData, driverStatsData;

From Plan 19-03:
    function detectMode(agenda, routeGeneratedAt) { ... }
    function renderHomepage() { /* dispatches */ }
    function showRoute(html) { ... }

From Plan 19-05:
    function renderDayPage(dayISO) { ... }  // composes the full day scaffold

From Plan 19-08:
    renderHomepage() already has mode dispatch for 'before' and a placeholder for 'live'/'archive'.

driverStatsData shape (from Phase 13):
    {
        active_driver: "Tony" | null,
        drivers: [ { driver_name, total_seconds, pct } ]
    }

Existing fetch URLs:
    /captures/events.json, /captures/notes.json, /captures/fuel.json,
    /captures/route.json, /captures/driver-stats.json, /agenda.json
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driverStatsData.active_driver → innerHTML | From Pi driver_sessions table. Escape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09-01 | XSS | driver_name / active_driver concatenated into widget HTML | mitigate | `escapeHtml()` on all string fields. |
| T-19-09-02 | DoS (self-inflicted) | 2-minute refresh multiplied across many open tabs hammers the Pi | accept | Single-user site with maybe 2-3 concurrent tabs. At 120s interval with ~100KB payloads this is negligible. Pi-side nginx has cache headers tuned by Plan 19-02 (`no-cache` for agenda/route; default for events). |
| T-19-09-03 | Resource leak | Refresh timer survives navigation | mitigate | `_liveRefreshTimer` tracked at module level, cleared in renderDayPage + renderAbout + _stopLiveRefresh(). |
| T-19-09-04 | Logic | Clock skew on Pi vs browser causes 'today' mismatch | mitigate | All date bucketing uses `TIMEZONE` (Australia/Sydney) and the browser's Date. If the browser clock is wrong, no worse than any other web app. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Live-mode CSS + current-driver widget + refresh timer machinery</name>
  <files>~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html</files>
  <read_first>
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html lines 1130-1164 (existing renderDrivers — reuse the `formatDriveTime` helper + data shape)
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html (confirm formatDriveTime helper exists; if not, reference from Phase 13)
    - .planning/phases/19-website-narrative-rebuild/19-CONTEXT.md D-08 (current-driver widget) D-17 (today-is-homepage)
  </read_first>
  <action>
Edit 1 — CSS. After the `.before-*` block from Plan 19-08, append:

    /* Live-mode homepage (Phase 19 — D-17, D-08, NARR-01) */
    .live-badge {
        display: inline-block;
        background: #da3633; color: #fff;
        font-size: 0.7rem; font-weight: 600;
        padding: 0.2rem 0.5rem;
        border-radius: 4px;
        letter-spacing: 0.08em;
        vertical-align: middle;
        margin-left: 0.5rem;
        animation: livepulse 2s ease-in-out infinite;
    }
    @keyframes livepulse {
        0%, 100% { opacity: 1; }
        50%      { opacity: 0.55; }
    }
    .current-driver-widget {
        display: flex; align-items: center; gap: 1rem;
        background: #161b22; border: 1px solid #2a1f0e; border-radius: 8px;
        padding: 1rem 1.25rem;
        margin-bottom: 1.5rem;
    }
    .current-driver-widget .cdw-avatar {
        width: 48px; height: 48px; border-radius: 50%;
        background: #c06000; color: #fff;
        display: flex; align-items: center; justify-content: center;
        font-size: 1.4rem; font-weight: 600;
        flex-shrink: 0;
    }
    .current-driver-widget .cdw-text { flex: 1; }
    .current-driver-widget .cdw-label {
        color: #8b949e; font-size: 0.75rem;
        text-transform: uppercase; letter-spacing: 0.05em;
    }
    .current-driver-widget .cdw-name {
        color: #f0dbb8; font-size: 1.15rem; font-weight: 600;
        margin-top: 0.2rem;
    }
    .current-driver-widget .cdw-time {
        color: #8b949e; font-size: 0.85rem;
    }
    @media (max-width: 600px) {
        .current-driver-widget { padding: 0.75rem 1rem; }
        .current-driver-widget .cdw-avatar { width: 40px; height: 40px; font-size: 1.15rem; }
        .current-driver-widget .cdw-name   { font-size: 1rem; }
    }

Edit 2 — current-driver widget helper. Inside the IIFE, after the spine renderers, add:

    function _formatDriveTimeLocal(seconds) {
        if (seconds == null || isNaN(seconds)) return '';
        var s = Math.floor(seconds);
        var h = Math.floor(s / 3600);
        var m = Math.floor((s % 3600) / 60);
        return (h > 0 ? h + 'h ' : '') + m + 'm';
    }

    function renderCurrentDriverWidget(ds) {
        if (!ds || !ds.active_driver) {
            return ''; // Silent — don't clutter the UI with "no driver" noise.
        }
        var name = escapeHtml(ds.active_driver);
        var initial = ds.active_driver.charAt(0).toUpperCase();
        var driverRow = null;
        if (ds.drivers) {
            for (var i = 0; i < ds.drivers.length; i++) {
                if (ds.drivers[i].driver_name === ds.active_driver) { driverRow = ds.drivers[i]; break; }
            }
        }
        var timeLabel = driverRow ? _formatDriveTimeLocal(driverRow.total_seconds) + ' on the wheel' : '';
        return '<section class="current-driver-widget">' +
            '<div class="cdw-avatar">' + escapeHtml(initial) + '</div>' +
            '<div class="cdw-text">' +
                '<div class="cdw-label">Currently driving</div>' +
                '<div class="cdw-name">' + name + '</div>' +
                (timeLabel ? '<div class="cdw-time">' + escapeHtml(timeLabel) + '</div>' : '') +
            '</div>' +
        '</section>';
    }

Edit 3 — refresh timer machinery. Alongside `var _countdownTimer = null;` (from Plan 19-08), add:

    var _liveRefreshTimer = null;
    var LIVE_REFRESH_MS = 120000;

    function _stopLiveRefresh() {
        if (_liveRefreshTimer) { clearInterval(_liveRefreshTimer); _liveRefreshTimer = null; }
    }

    function refreshLiveData() {
        // Re-fetch the data sources that change during a live rally.
        var p = [
            fetch('/captures/events.json',       { cache: 'no-cache' }).then(function(r){ return r.ok ? r.json() : null; }).catch(function(){ return null; }),
            fetch('/captures/notes.json',        { cache: 'no-cache' }).then(function(r){ return r.ok ? r.json() : null; }).catch(function(){ return null; }),
            fetch('/captures/fuel.json',         { cache: 'no-cache' }).then(function(r){ return r.ok ? r.json() : null; }).catch(function(){ return null; }),
            fetch('/captures/route.json',        { cache: 'no-cache' }).then(function(r){ return r.ok ? r.json() : null; }).catch(function(){ return null; }),
            fetch('/captures/driver-stats.json', { cache: 'no-cache' }).then(function(r){ return r.ok ? r.json() : null; }).catch(function(){ return null; })
        ];
        return Promise.all(p).then(function(results) {
            if (results[0] != null) eventsData       = results[0];
            if (results[1] != null) notesData        = results[1].slice().reverse();
            if (results[2] != null) fuelData         = results[2];
            if (results[3] != null) routeData        = results[3];
            if (results[4] != null) driverStatsData  = results[4];
            // Re-render only if we're still on the homepage.
            if (window.location.pathname === '/' || window.location.pathname === '') {
                renderHomepage();
            }
        });
    }

Edit 4 — teardown the live timer from other route renderers. Add `_stopLiveRefresh();` to the top of renderDayPage and renderAbout (alongside `_teardownDayMap()` / `_stopCountdown()`).

Commit: `feat(19-09): live-mode CSS + current-driver widget + refresh timer`.
  </action>
  <verify>
    <automated>grep -c '\.live-badge' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c '\.current-driver-widget' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'function renderCurrentDriverWidget' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'function refreshLiveData' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'var _liveRefreshTimer' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c '_stopLiveRefresh' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | awk '{exit ($1 >= 3) ? 0 : 1}' && grep -c 'LIVE_REFRESH_MS' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | awk '{exit ($1 >= 1) ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File contains `.live-badge` CSS with `livepulse` animation
    - File contains `.current-driver-widget` CSS
    - File contains exactly one `function renderCurrentDriverWidget(ds)` — returns empty string when no active driver (silent)
    - File contains exactly one `function refreshLiveData()` with Promise.all across 5 JSON sources
    - `refreshLiveData` only triggers `renderHomepage()` when `window.location.pathname` is `/` (no side-effects when user is on a day page)
    - Module-level `_liveRefreshTimer` and `LIVE_REFRESH_MS` (=120000) declared
    - `_stopLiveRefresh()` called from at least 3 places (renderDayPage, renderAbout, and self-replace in renderHomepage live branch)
    - XSS: active_driver + initial + drivers[].driver_name all pass through escapeHtml
    - Mobile CSS tightens widget to 40px avatar
  </acceptance_criteria>
  <done>Widget + refresh machinery landed. Not yet wired — Task 2 adds the renderHomepageLive dispatcher.</done>
</task>

<task type="auto">
  <name>Task 2: renderHomepageLive + wire live branch in renderHomepage dispatch</name>
  <files>~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html</files>
  <read_first>
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html (confirm Plan 19-08 renderHomepage placeholder for 'live' — replace just that branch)
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html (confirm renderDayPage from Plan 19-05/19-06/19-07 is the one we delegate to)
  </read_first>
  <action>
Edit 1 — renderHomepageLive. Add after renderCurrentDriverWidget:

    function renderHomepageLive() {
        var today = new Date().toLocaleDateString('en-CA', { timeZone: TIMEZONE });

        // Guard: if 'today' is outside the rally window, fall through to archive.
        if (agendaData && agendaData.rally) {
            var start = agendaData.rally.start_date;
            var end   = agendaData.rally.end_date;
            if (end && today > end) {
                // We've overshot into archive territory; fall back.
                renderHomepageArchive && renderHomepageArchive(); // optional until 19-10
                return;
            }
            if (start && today < start) {
                // Underflow — before mode. (detectMode should already have caught this; guard anyway.)
                renderHomepageBefore(agendaData);
                return;
            }
        }

        // 1. Render the day-page for today (which handles day-nav, agenda, stats, spine, map).
        renderDayPage(today);

        // 2. Prepend the current-driver widget by inserting it above .day-page content.
        var widgetHtml = renderCurrentDriverWidget(driverStatsData);
        if (widgetHtml) {
            var host = document.getElementById('dynamic-route');
            var dayPage = host && host.querySelector('.day-page');
            if (dayPage) {
                var wrapper = document.createElement('div');
                wrapper.innerHTML = widgetHtml;
                dayPage.insertBefore(wrapper.firstChild, dayPage.firstChild);
            }
        }

        // 3. Inject a pulsing LIVE badge into the day-header h1.
        var headerH1 = document.querySelector('.day-page .day-header h1');
        if (headerH1 && !headerH1.querySelector('.live-badge')) {
            var badge = document.createElement('span');
            badge.className = 'live-badge';
            badge.textContent = 'LIVE';
            headerH1.appendChild(badge);
        }

        // 4. Start the auto-refresh loop.
        _stopLiveRefresh();
        _liveRefreshTimer = setInterval(refreshLiveData, LIVE_REFRESH_MS);
    }

Edit 2 — replace the 'live' placeholder branch in renderHomepage. In the `function renderHomepage()` from Plan 19-08, find the placeholder block (the one that shows "Detected mode: live — full homepage lands in Plan 19-09") and REPLACE the entire mode-dispatch body so it reads:

    function renderHomepage() {
        _teardownDayMap();
        _stopCountdown();
        _stopLiveRefresh();
        renderDayNav(agendaData, null);

        var mode = detectMode(agendaData, routeData && routeData.generated_at);

        if (!agendaData) {
            showRoute('<div class="day-page"><section class="day-header"><h1>Shit of Theseus</h1><div class="day-subtitle">Agenda loading…</div></section></div>');
            return;
        }

        if (mode === 'before') {
            renderHomepageBefore(agendaData);
            return;
        }
        if (mode === 'live') {
            renderHomepageLive();
            return;
        }
        // Archive + unknown → placeholder until Plan 19-10 lands.
        showRoute(
            '<div class="day-page">' +
                '<section class="day-header"><h1>Rally archive</h1>' +
                '<div class="day-subtitle">Detected mode: ' + escapeHtml(mode) + ' — archive homepage lands in Plan 19-10.</div>' +
                '</section>' +
            '</div>'
        );
    }

(Note: the call `renderHomepageArchive && renderHomepageArchive()` in renderHomepageLive is a soft-reference — until Plan 19-10 defines that function, it short-circuits to undefined and the rally-end overshoot silently continues as live. That is acceptable during development; Plan 19-10 fills it in.)

Commit: `feat(19-09): renderHomepageLive with 2-minute refresh loop + LIVE badge`.
  </action>
  <verify>
    <automated>grep -c 'function renderHomepageLive' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c "mode === 'live'" ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'renderHomepageLive()' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | awk '{exit ($1 >= 2) ? 0 : 1}' && grep -c 'setInterval(refreshLiveData' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'live-badge' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | awk '{exit ($1 >= 2) ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly one `function renderHomepageLive()`
    - `renderHomepage()` dispatches to renderHomepageLive when mode==='live'
    - renderHomepageLive delegates to `renderDayPage(today)` where `today = new Date().toLocaleDateString('en-CA', { timeZone: TIMEZONE })`
    - Current-driver widget is prepended to `.day-page` (not into arbitrary DOM)
    - LIVE badge appended to `.day-header h1` once — guard prevents double-insertion on refresh
    - `_liveRefreshTimer` armed with `setInterval(refreshLiveData, LIVE_REFRESH_MS)` at the end of renderHomepageLive
    - Underflow guard (today < start) falls back to renderHomepageBefore
    - Overshoot guard (today > end) soft-references renderHomepageArchive (will be defined in Plan 19-10)
    - Browser check: temporarily set rally.start_date = today's date in agenda.json, then `/` renders today as a day page with LIVE badge + (if driverStatsData exists) current-driver widget
    - Browser check: Network tab shows fetch of /captures/events.json every 120s (2 min interval)
    - Browser check: navigate `/` → `/day/2026-05-29` → Network tab stops showing homepage refreshes (teardown works)
  </acceptance_criteria>
  <done>Live-mode homepage lands: today's day page with current-driver widget, pulsing LIVE badge, 2-minute refresh loop. Timer tears down on navigation.</done>
</task>

</tasks>

<verification>
Plan 19-09 complete when:
- All grep checks pass
- Artificial live-mode test (point agenda.rally.start_date to today in DevTools, reload): `/` renders today's day page with widget + badge, refreshes every 2 min
- Navigating away cleans up the refresh timer (verify via Network tab or `clearInterval` log)
- Underflow/overshoot guards work: rally fully in the past → falls through to archive placeholder; rally fully in the future → falls back to before-mode
</verification>

<success_criteria>
- NARR-01 (live-leg): Today IS the homepage during the rally (D-17) with current-driver widget (D-08) and 2-minute refresh.
</success_criteria>

<output>
After completion, create `.planning/phases/19-website-narrative-rebuild/19-09-SUMMARY.md`. Note whether 2-minute interval is appropriate or should be tuned (observed payload sizes, network load).
</output>
