---
phase: 19
plan: "03"
type: execute
wave: 1
depends_on: ["19-02"]
files_modified:
  - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html
autonomous: true
requirements:
  - NARR-01
  - NARR-02
must_haves:
  truths:
    - "The SPA reads window.location.pathname on load and dispatches to renderDayPage, renderAbout, or renderHomepage"
    - "A URL like /day/2026-05-27 loads the SPA (via existing nginx try_files) and the JS routes to renderDayPage('2026-05-27')"
    - "Invalid day URLs (bad-format, out-of-rally-range) render a 'no data for this day' card without throwing"
    - "detectMode(agenda, routeGeneratedAt) returns 'before' when today < start_date"
    - "detectMode returns 'live' when start_date <= today <= end_date AND routeGeneratedAt is within 6 hours"
    - "detectMode returns 'archive' otherwise (including stale-data-during-rally)"
    - "Mode detection re-runs on tab visibility regain (visibilitychange event with state==='visible')"
    - "history.pushState + popstate drives day-to-day navigation without full page reload"
  artifacts:
    - path: "~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html"
      provides: "detectMode(), route(), renderDayPage() stub, navigateToDay(), popstate handler"
      contains: "function detectMode"
  key_links:
    - from: "window load handler"
      to: "route() dispatcher"
      via: "called after agendaData + routeData fetches settle"
      pattern: "function route\\("
    - from: "detectMode"
      to: "renderHomepage mode switch"
      via: "mode string routed to renderHomepageBefore / renderHomepageLive / renderHomepageArchive stubs"
      pattern: "detectMode\\("
---

<objective>
Add the SPA router + mode detection skeleton. After this plan, `/day/2026-05-27` loads the SPA, dispatches to `renderDayPage`, and mode detection correctly classifies before/live/archive given agenda dates + `routeData.generated_at`. Day-page rendering (Plans 19-05, 19-06, 19-07) and per-mode homepages (Plans 19-08, 19-09, 19-10) plug into the stubs this plan lands.

Purpose: Lock D-01, D-02, D-03, D-04 — the two primary UX primitives (URL routing + mode) that every other web plan needs.

Output additions to `webroot/index.html`:
- `detectMode(agenda, routeGeneratedAtISO)` function — pure, testable by eye.
- `route()` dispatcher reading `window.location.pathname` with regex match for `/day/YYYY-MM-DD`, `/about`, fallback homepage.
- Stubs: `renderDayPage(dayISO)`, `renderHomepage(mode)`, `renderAbout()` — minimum viable renderers that clear main content and print a placeholder card. Real content lands in downstream plans.
- `navigateToDay(dayISO)` using `history.pushState`.
- `popstate` + `visibilitychange` handlers.
- Bootstrap: on page load, wait for `agendaData + routeData` fetches to settle (via `Promise.allSettled`), then call `route()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-website-narrative-rebuild/19-CONTEXT.md
@.planning/phases/19-website-narrative-rebuild/19-RESEARCH.md

<interfaces>
From Plan 19-02 (already landed):

    var agendaData = null;   // { rally: {start_date, end_date, ...}, days: [...] }
    var routeData = null;    // { generated_at: ISO, days: { YYYY-MM-DD: {...} } }

From existing webroot/index.html (to reuse):

    var TIMEZONE = 'Australia/Sydney';   // line 1004
    // en-CA locale trick for YYYY-MM-DD day bucketing (used at line 1348)
    var today = new Date().toLocaleDateString('en-CA', { timeZone: TIMEZONE });

Existing hash-routing at lines 1024-1053 (clicks nav links; sets history.replaceState '#section'). Do NOT delete this yet — Plan 19-11 owns nav teardown. This plan ADDS path-based routing alongside the hash mechanism.

Existing main content container: the `<main>` element at line 679. All sections currently live inside it. New routers render into `<main>` by showing/hiding a new `<div id="dynamic-route"></div>` sibling of the existing `.section` nodes. This avoids fighting the `.section` class system until Plan 19-11 removes it.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| window.location (user-controlled) → SPA router | User can craft any path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | Injection / XSS | Day slug from URL passed into innerHTML | mitigate | Regex `^\/day\/(\d{4}-\d{2}-\d{2})$` strictly validates format. Captured slug used ONLY in (a) date comparisons, (b) object key lookup in agenda.days, (c) textContent setters (never innerHTML concatenation). Any string not matching the regex falls through to homepage. |
| T-19-03-02 | Logic error | `today < start_date` string comparison drifts past midnight in some TZ | mitigate | Always derive `today` via `new Date().toLocaleDateString('en-CA', { timeZone: 'Australia/Sydney' })`. `en-CA` guarantees `YYYY-MM-DD`. String comparison is valid for ISO dates. |
| T-19-03-03 | Information Disclosure | Day URL outside rally range (e.g. `/day/1999-01-01`) | accept | Renders a generic "no data" card; no telemetry leaked. Future dates inside rally render empty state. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add detectMode() pure function + in-code self-test</name>
  <files>~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html</files>
  <read_first>
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html lines 1003-1016 (BADGE_COLORS + TIMEZONE constants — add new constants here)
    - .planning/phases/19-website-narrative-rebuild/19-RESEARCH.md §2 (detectMode reference implementation)
  </read_first>
  <action>
Inside the existing IIFE (starts around line 993 `(function() {`), after the `DISPLAY_NAMES` map (around line 1015) and before the first `var eventsData = null;` declaration, add:

    var LIVE_FRESHNESS_MS = 6 * 3600 * 1000;   // 6 hours — D-03 threshold

    /**
     * Decide which homepage mode to render.
     * @param {object|null} agenda — parsed agenda.json, or null on fetch failure
     * @param {string|null} routeGeneratedAt — ISO-8601 from routeData.generated_at, or null
     * @returns {'before'|'live'|'archive'}
     */
    function detectMode(agenda, routeGeneratedAt) {
        if (!agenda || !agenda.rally || !agenda.rally.start_date || !agenda.rally.end_date) {
            return 'archive';
        }
        var today = new Date().toLocaleDateString('en-CA', { timeZone: TIMEZONE });
        var start = agenda.rally.start_date;
        var end = agenda.rally.end_date;
        if (today < start) return 'before';
        if (today > end) return 'archive';
        if (!routeGeneratedAt) return 'archive';
        var genDate = new Date(routeGeneratedAt);
        if (isNaN(genDate.getTime())) return 'archive';
        var ageMs = Date.now() - genDate.getTime();
        if (ageMs < 0) return 'archive';
        return ageMs < LIVE_FRESHNESS_MS ? 'live' : 'archive';
    }

    // Self-test — logs to console if any assertion fails. No test framework available.
    (function _detectModeSelfTest() {
        function assertEq(actual, expected, label) {
            if (actual !== expected) {
                console.error('detectMode SELF-TEST FAIL:', label, 'got', actual, 'wanted', expected);
            }
        }
        var a = { rally: { start_date: '2099-01-01', end_date: '2099-01-10' } };
        assertEq(detectMode(a, null), 'before', 'future rally');
        var b = { rally: { start_date: '2000-01-01', end_date: '2000-01-10' } };
        assertEq(detectMode(b, null), 'archive', 'past rally');
        var c = { rally: { start_date: '1900-01-01', end_date: '2999-01-01' } };
        assertEq(detectMode(c, new Date().toISOString()), 'live', 'in-range + fresh');
        assertEq(detectMode(c, '2000-01-01T00:00:00Z'), 'archive', 'in-range + stale');
        assertEq(detectMode(null, null), 'archive', 'no agenda');
        assertEq(detectMode({}, null), 'archive', 'malformed agenda');
    })();

Commit: `feat(19-03): add detectMode() with in-code self-test`.
  </action>
  <verify>
    <automated>grep -c 'function detectMode(agenda, routeGeneratedAt)' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c '_detectModeSelfTest' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly one `function detectMode(agenda, routeGeneratedAt)`
    - File contains `var LIVE_FRESHNESS_MS = 6 * 3600 * 1000`
    - File contains `_detectModeSelfTest`
    - Self-test covers all six branches: future rally, past rally, in-range+fresh, in-range+stale, null agenda, malformed agenda
    - Opening https://shit-of-theseus.com after deploy shows no `detectMode SELF-TEST FAIL` message in the browser console
    - `detectMode` uses `en-CA` locale (no new Date parsing of locale strings)
  </acceptance_criteria>
  <done>detectMode() landed with self-test; no console errors on site load.</done>
</task>

<task type="auto">
  <name>Task 2: Add SPA router, stubs, and bootstrap integration</name>
  <files>~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html</files>
  <read_first>
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html lines 679-680 (`<main>` container — router renders a new `<div id="dynamic-route">` child here)
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html lines 1048-1053 (existing hash routing — leave intact for now; Plan 19-11 removes)
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html lines 1167-1224 (parallel fetch pipeline — the router runs after these settle)
    - .planning/phases/19-website-narrative-rebuild/19-RESEARCH.md §1 (routing + pushState + popstate code)
  </read_first>
  <action>
Edit 1 — HTML: inside `<main>` at line 679, add a new div as the FIRST child so router content appears above the existing `.section` blocks. Router shows or hides this div:

    <main>
        <div id="dynamic-route" style="display:none;"></div>
        <div id="status-section" class="section active">
            <!-- existing content unchanged -->

Edit 2 — JS: in the IIFE, after `detectMode()` and its self-test (from Task 1), add the router, navigation helpers, and stubs:

    var DAY_URL_RE = /^\/day\/(\d{4}-\d{2}-\d{2})\/?$/;
    var ABOUT_URL_RE = /^\/about\/?$/;

    function showRoute(html) {
        // Single entry-point for router-owned DOM. Hides all .section children of <main>.
        document.querySelectorAll('main > .section').forEach(function(s) { s.style.display = 'none'; });
        var host = document.getElementById('dynamic-route');
        host.style.display = '';
        host.innerHTML = html;
    }

    function showLegacySections() {
        // Re-show the existing .section-based home. Used when router has nothing to render.
        document.querySelectorAll('main > .section').forEach(function(s) { s.style.display = ''; });
        var host = document.getElementById('dynamic-route');
        host.style.display = 'none';
        host.innerHTML = '';
    }

    function route() {
        var pathname = window.location.pathname;
        var dayMatch = pathname.match(DAY_URL_RE);
        if (dayMatch) {
            var dayISO = dayMatch[1];
            // Validate the captured date parses
            var d = new Date(dayISO + 'T00:00:00Z');
            if (isNaN(d.getTime())) { renderHomepage(); return; }
            renderDayPage(dayISO);
            return;
        }
        if (ABOUT_URL_RE.test(pathname)) {
            renderAbout();
            return;
        }
        renderHomepage();
    }

    function navigateToDay(dayISO) {
        history.pushState({ route: 'day', day: dayISO }, '', '/day/' + dayISO);
        renderDayPage(dayISO);
        window.scrollTo(0, 0);
    }

    function navigateHome() {
        history.pushState({ route: 'home' }, '', '/');
        renderHomepage();
        window.scrollTo(0, 0);
    }

    window.addEventListener('popstate', function(e) {
        route();
    });

    // Stubs — Plan 19-05/06/07 replace these with real renderers.
    function renderDayPage(dayISO) {
        var dayExists = agendaData && agendaData.days && agendaData.days.some(function(d) { return d.date === dayISO; });
        var headerText = 'Day ' + dayISO;
        if (dayExists) {
            var agendaDay = agendaData.days.filter(function(d) { return d.date === dayISO; })[0];
            headerText = 'Day ' + agendaDay.day_number + ' — ' + agendaDay.title;
        }
        var safeHeader = document.createElement('div');
        safeHeader.textContent = headerText;
        var body = dayExists
            ? '<p style="color:#8b949e;">Day page content lands in Plan 19-05/06/07.</p>'
            : '<p style="color:#8b949e;">No agenda entry for ' + dayISO.replace(/[^0-9-]/g, '') + '.</p>';
        showRoute(
            '<div style="background:#161b22;border:1px solid #2a1f0e;border-radius:8px;padding:1.5rem;">' +
            '<h2 style="color:#f0dbb8;margin-top:0;">' + safeHeader.innerHTML + '</h2>' +
            body +
            '<p><a href="/" onclick="event.preventDefault(); navigateHome();" style="color:#c06000;">← Home</a></p>' +
            '</div>'
        );
    }

    function renderHomepage() {
        // Phase 18 behaviour: show legacy sections. Plans 19-08/09/10 replace this with
        // mode-specific renderers keyed off detectMode(agendaData, routeData && routeData.generated_at).
        var mode = detectMode(agendaData, routeData && routeData.generated_at);
        console.log('homepage mode:', mode);
        showLegacySections();
    }

    function renderAbout() {
        // Stub — Plan 19-11 moves /about content here.
        showRoute(
            '<div style="background:#161b22;border:1px solid #2a1f0e;border-radius:8px;padding:1.5rem;">' +
            '<h2 style="color:#f0dbb8;margin-top:0;">About</h2>' +
            '<p style="color:#8b949e;">About content lands in Plan 19-11.</p>' +
            '<p><a href="/" onclick="event.preventDefault(); navigateHome();" style="color:#c06000;">← Home</a></p>' +
            '</div>'
        );
    }

    document.addEventListener('visibilitychange', function() {
        if (document.visibilityState === 'visible') {
            // Re-run mode detection on tab focus return (D-04)
            var pathname = window.location.pathname;
            if (pathname === '/' || pathname === '/index.html') {
                renderHomepage();
            }
        }
    });

Edit 3 — wire the router into the existing bootstrap path. The file already has parallel fetches for events/notes/fuel/driver-stats/agenda/route. After those `fetch().then()` chains (around line 1220), append a `Promise.allSettled`-based gate that runs `route()` once agenda + route have settled:

    // Wait for agenda + route to settle (success or fail), then run the SPA router.
    // The other fetches keep resolving independently into renderEvents/renderNotes/etc.
    Promise.allSettled([
        fetch('/agenda.json', { cache: 'no-cache' }).then(function(r) { return r.ok ? r.json() : null; }).then(function(x) { agendaData = x || agendaData; }),
        fetch('/captures/route.json', { cache: 'no-cache' }).then(function(r) { return r.ok ? r.json() : null; }).then(function(x) { routeData = x || routeData; })
    ]).then(function() { route(); });

Note — the two fetches added in Plan 19-02 ALREADY populate `agendaData` / `routeData`. The `allSettled` block above duplicates the fetch; change the Plan 19-02 pattern in this edit by converting those two fetches into Promise references the new `allSettled` reuses. Simpler: DELETE the two original fetch chains added in Plan 19-02 and REPLACE them with this single `allSettled` block that handles both fetches and then calls `route()`.

Grep-verify after edit: `grep -c "fetch('/agenda.json'" index.html` → 1 (the one inside `allSettled`), `grep -c "Promise.allSettled" index.html` → 1.

Commit: `feat(19-03): add SPA pathname router + stubs + visibility-change rehydrate`.
  </action>
  <verify>
    <automated>grep -c '<div id="dynamic-route"' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'function route()' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'DAY_URL_RE' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | awk '{exit ($1 >= 2) ? 0 : 1}' && grep -c 'Promise.allSettled' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c "addEventListener('popstate'" ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c "addEventListener('visibilitychange'" ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly one `<div id="dynamic-route"` (as first child of `<main>`)
    - File contains `var DAY_URL_RE = /^\/day\/(\d{4}-\d{2}-\d{2})\/?$/;`
    - File contains `function route()`, `function navigateToDay(dayISO)`, `function navigateHome()`
    - File contains stubs `function renderDayPage(dayISO)`, `function renderHomepage()`, `function renderAbout()`
    - File contains exactly one `Promise.allSettled` call
    - File contains `addEventListener('popstate'` (popstate handler)
    - File contains `addEventListener('visibilitychange'` (D-04 re-check on tab focus)
    - Visiting `https://shit-of-theseus.com/day/2026-05-27` (after Flux reconcile) returns 200 and the page shows "Day 1 — Port Douglas → Cairns" (from agenda.json)
    - Visiting `https://shit-of-theseus.com/day/1999-01-01` returns 200 and the page shows "No agenda entry for 1999-01-01"
    - Visiting `https://shit-of-theseus.com/about` returns 200 and shows the about stub card
    - No JS console errors on `/`, `/day/2026-05-27`, or `/about`
  </acceptance_criteria>
  <done>SPA routes /day/YYYY-MM-DD to renderDayPage stub, /about to renderAbout stub, everything else to renderHomepage (which currently renders legacy sections). pushState + popstate + visibility change all wired.</done>
</task>

</tasks>

<verification>
Plan 19-03 complete when:
- `grep` checks in acceptance_criteria above all pass
- Smoke via curl after deploy: `curl -s -o /dev/null -w "%{http_code}\n" https://shit-of-theseus.com/day/2026-05-27` returns `200`
- Browser console on `/` and `/day/2026-05-27` shows zero errors
- DevTools Network tab shows exactly one fetch each of `/agenda.json` and `/captures/route.json` (the `allSettled` de-dup collapses Plan 19-02's original two fetches)

Manual check:
1. Open `/` — legacy site renders (status/drivers/etc. tabs still work, per Plan 19-11 not having removed them yet)
2. Manually change URL to `/day/2026-05-27` — the stub day card renders; legacy sections are hidden
3. Browser back — returns to `/` and legacy sections reappear (popstate wiring)
4. Change tab away and back — visibility-change fires `renderHomepage()` (verifiable via `console.log('homepage mode:')` output)
</verification>

<success_criteria>
- NARR-01 (foundation): `detectMode()` correctly classifies all six branches — verified by self-test running on every page load.
- NARR-02: `/day/YYYY-MM-DD` routes to `renderDayPage` via `location.pathname` (not hash). Verified via deploy curl + browser walkthrough.
- Stubs unblock downstream plans 19-05, 19-06, 19-07, 19-08, 19-09, 19-10, 19-11.
</success_criteria>

<output>
After completion, create `.planning/phases/19-website-narrative-rebuild/19-03-SUMMARY.md`. Note:
- Whether the existing hash-routing at lines 1024-1053 needed any edits (should be none — Plan 19-11 owns removal)
- Any browser console issues observed during manual check
</output>
