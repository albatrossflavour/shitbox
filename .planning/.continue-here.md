---
context: default
status: paused
last_updated: "2026-04-21T10:50:00.000Z"
repo: home-ops (shit-of-theseus website) + NAS uploads
---

<current_state>
Mid-task on folding car/tech build photos into the Car page on shit-of-theseus.com. Resized 9 photos, created `build/` dir on NAS, but `scp`/`rsync` to the NAS are failing — DSM's SFTP subsystem appears to restrict paths to the user homedir even though `ssh` shell can see `/volume2/apps/shitbox/build/`. Nothing committed or edited in repos.

The 9 resized photos (1600px longest edge, q85) are saved durably at:
`~/Documents/projects/Shitbox Rally 2026/build images/_web-ready/`

They're also still in `/tmp/sot-photos/` but `/tmp` is volatile on macOS reboots.
</current_state>

<completed_work>

- Surveyed ~44 source photos across `~/Documents/projects/Shitbox Rally 2026/build images/{car,tech}/` and picked 9 for the site
- Source→web mapping decided (see table below)
- All 9 photos resized to 1600px longest edge, JPEG q85, EXIF stripped, saved to `_web-ready/`
- `ssh tgreen@192.168.1.22 mkdir -p /volume2/apps/shitbox/build` — dir exists
- Confirmed `/usr/share/nginx/html/captures` already NFS-mounts `/volume2/apps/shitbox`, so the `build/` dir is already visible inside the pod at `/usr/share/nginx/html/captures/build/` — so the images would be reachable at `/captures/build/...` right now, no helmrelease change needed if you accept the URL path
- Identified helmrelease.yaml pattern for adding a cleaner `/build/` URL (second `persistence` entry, `subPath: build`, `readOnly: true`)
</completed_work>

<remaining_work>

1. **Upload photos to NAS** — `scp` and `rsync` both fail. DSM SFTP chroot likely. Workarounds to try:
   - `cat file.jpg | ssh tgreen@192.168.1.22 "cat > /volume2/apps/shitbox/build/file.jpg"` (piped through shell, bypasses SFTP)
   - Loop over the 9 files with the above
   - Or: scp to `~/build/` first, then `ssh mv ~/build/* /volume2/apps/shitbox/build/`
   - Or: DSM File Station web UI upload
2. **helmrelease.yaml** edit (only if you want `/build/...` URLs instead of `/captures/build/...`) — add a second NFS persistence entry in `kubernetes/apps/default/shit-of-theseus/app/helmrelease.yaml` mirroring `captures` but with `subPath: build` and mount path `/usr/share/nginx/html/build`
3. **index.html renderCar()** edits in `kubernetes/apps/default/shit-of-theseus/app/webroot/index.html`:
   - Line ~2135: swap `car-front.jpg` / `car-side.jpg` to `/build/car-front.jpg` / `/build/car-side.jpg` (remove old files from ConfigMap)
   - Under "The Car" intro, add a hood image: `<img src="/build/car-decals-hood.jpg" ...>`
   - Under "Why Shit of Theseus?" (line ~2150), add an origin-story pair (`car-origin-front.jpg` + `car-origin-yard.jpg`) with a lead-in sentence about how it looked when bought
   - In "The Hardware" section, add a new `<h4 id="hw-retro-panel">Retro Dash Panel</h4>` with `car-retro-panel.jpg` and a sentence about the Fallout-style meters / kill switch / Anderson plug
   - Lines 2305-2307: the three Build Photos `<img>` sources already use the right filenames (`car-interior.jpg` / `car-sensor-cluster.jpg` / `car-engine-bay.jpg`) — just change the paths to `/build/...`
4. **Remove `car-front.jpg` and `car-side.jpg`** from `webroot/` dir and from `kustomization.yaml` configMapGenerator files list — they move to NFS
5. **Commit + push** home-ops
</remaining_work>

## File Mapping (source photo → NAS filename)

| Source | NFS filename | Slot in page |
|---|---|---|
| `car/IMG_1639.JPG` | `car-front.jpg` | "The Car" intro, replaces existing |
| `car/IMG_1640.JPG` | `car-side.jpg` | "The Car" intro, replaces existing |
| `car/signal-2026-03-20-142650.jpg` | `car-decals-hood.jpg` | NEW: add to "The Car" intro |
| `car/CE4CC104-*.jpeg` | `car-origin-front.jpg` | NEW: origin pair under "Why" |
| `car/D530A436-*.jpeg` | `car-origin-yard.jpg` | NEW: origin pair under "Why" |
| `tech/IMG_7D5C0D04-*.jpeg` | `car-retro-panel.jpg` | NEW: `h4` in "The Hardware" |
| `car/IMG_1800.JPG` | `car-interior.jpg` | Build Photos slot 1 |
| `tech/IMG_1980.JPG` | `car-sensor-cluster.jpg` | Build Photos slot 2 |
| `car/IMG_AA0EB866-*.jpeg` | `car-engine-bay.jpg` | Build Photos slot 3 |

<decisions_made>

- Decided to go NFS-backed (`/build/` URL) instead of stuffing 9 photos into the webroot ConfigMap. ConfigMap cap is 1MiB and at q85/1600px the 9 photos total ~2.7MB. NFS has no such limit, and build photos will grow over time.
- Picked 1600px q85 (not 900px q80) once NFS was on the table — file sizes range 137KB-417KB, perfectly fine for a photo gallery.
- `IMG_7D5C0D04` (clean retro panel close-up) chosen over `IMG_1799` (retro panel installed in-car, same subject but busier frame).
- `signal-2026-03-20-142650.jpg` (full hood panorama with knights + both decals) chosen over `IMG_1796` (similar but crop slightly off).
</decisions_made>

<blockers>

- **scp/rsync to NAS failing**: `scp ... tgreen@192.168.1.22:/volume2/apps/shitbox/build/` → "remote mkdir no such file or directory". `ssh tgreen@192.168.1.22 ls /volume2/apps/shitbox/build/` succeeds. This is DSM's SFTP subsystem restricting paths. Use piped-shell-cat workaround (see remaining_work #1) or DSM File Station.
</blockers>

## Required Reading (in order)

1. `kubernetes/apps/default/shit-of-theseus/app/helmrelease.yaml` (home-ops) — existing `persistence.captures` NFS pattern to mirror for `build`
2. `kubernetes/apps/default/shit-of-theseus/app/webroot/index.html` lines 2124-2320 — the `renderCar()` function that needs the 4 edits
3. `kubernetes/apps/default/shit-of-theseus/app/kustomization.yaml` — where car-front.jpg and car-side.jpg need to be removed from the configMapGenerator files list

## Infrastructure State

- **NAS** `192.168.1.22:/volume2/apps/shitbox/build/` — exists, empty, owned `tgreen:users`
- **Resized photos** — at `~/Documents/projects/Shitbox Rally 2026/build images/_web-ready/` (durable) and `/tmp/sot-photos/` (volatile). 9 files, ~2.7MB total.
- **home-ops repo** — clean (last commit 3c85c69, the earlier Grafana admin-secret work)
- **shitbox repo** — main at a3914c7 (WIP commit from earlier session), pushed to origin, not on this task

<context>
Conversation started as "look at these photos and suggest what to fold into the Car page". Settled on option 2 of three options: drop-ins into existing slots plus new blocks (origin story, retro panel, decals image). NFS route chosen after confirming ConfigMap would blow its 1MB cap. Resize-and-upload reached the upload step before DSM's SFTP restriction blocked progress and context ran out.
</context>

<next_action>
Unblock the NAS upload first — simplest option is the piped-cat workaround:

```bash
for f in ~/Documents/projects/Shitbox\ Rally\ 2026/build\ images/_web-ready/*.jpg; do
  name=$(basename "$f")
  ssh tgreen@192.168.1.22 "cat > /volume2/apps/shitbox/build/$name" < "$f"
done
```

Once the 9 files land in `/volume2/apps/shitbox/build/`, decide whether to use `/captures/build/...` URLs (zero infra changes, uglier path) or add the second NFS mount for `/build/...` URLs (one helmrelease edit, cleaner path). Then do the four index.html edits per the mapping table above, drop car-front.jpg/car-side.jpg from webroot+kustomization, commit + push home-ops, and Flux reconciles.
</next_action>
