---
phase: 11-v2-hardware-migration
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/events/test_sampler_lsm6dsox.py
  - tests/collectors/test_imu_heading.py
  - tests/collectors/test_temperature_ds18b20.py
  - tests/collectors/test_light_veml7700.py
  - tests/collectors/test_pm25_sen0460.py
  - tests/collectors/test_power_ina226.py
  - .planning/phases/11-v2-hardware-migration/HARDWARE_IDS.md
autonomous: false
requirements:
  - D-01
  - D-03
  - D-04
  - D-05
  - D-06
  - D-09
  - D-10
  - D-11
  - D-13
must_haves:
  truths:
    - "Failing test stubs exist for every new collector and the LSM6DSOX sampler"
    - "DS18B20 1-Wire sensor IDs are recorded from the Pi 5 for exterior and engine_bay roles"
    - "ELP 4K USB camera vendor:product ID is recorded for the udev rule"
  artifacts:
    - path: "tests/events/test_sampler_lsm6dsox.py"
      provides: "RED tests for LSM6DSOX sampler unit conversion + graceful absent"
    - path: "tests/collectors/test_imu_heading.py"
      provides: "RED tests for complementary filter + LIS3MDL heading"
    - path: "tests/collectors/test_temperature_ds18b20.py"
      provides: "RED tests for DS18B20 dual-probe role mapping"
    - path: "tests/collectors/test_light_veml7700.py"
      provides: "RED tests for VEML7700 lux collector"
    - path: "tests/collectors/test_pm25_sen0460.py"
      provides: "RED tests for SEN0460 disabled + graceful absent paths"
    - path: "tests/collectors/test_power_ina226.py"
      provides: "RED tests for INA226 disabled + graceful absent paths"
    - path: ".planning/phases/11-v2-hardware-migration/HARDWARE_IDS.md"
      provides: "Recorded DS18B20 IDs + ELP VID:PID for config and udev rule"
  key_links:
    - from: "tests/events/test_sampler_lsm6dsox.py"
      to: "shitbox.events.sampler.HighRateSampler"
      via: "import, will fail until plan 01 rewrites sampler"
      pattern: "from shitbox.events.sampler import"
    - from: ".planning/phases/11-v2-hardware-migration/HARDWARE_IDS.md"
      to: "config/config.yaml (plan 03)"
      via: "DS18B20 ids referenced by exterior/engine_bay roles"
      pattern: "sensor_id:"
---

<objective>
Wave 0 RED phase. Create failing test stubs for every new collector and the LSM6DSOX sampler so later waves implement against tests. Also perform the SSH pre-flight to record DS18B20 1-Wire IDs and the ELP 4K USB vendor:product ID, because config/udev rules in later waves need concrete values, not TODOs.

Purpose: Make failure visible before implementation starts. Hardware discovery up front so wave 3 can write a real udev rule and wave 3 config can ship real sensor IDs.
Output: Six failing pytest files + a HARDWARE_IDS.md note checked into the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-v2-hardware-migration/11-CONTEXT.md
@.planning/phases/11-v2-hardware-migration/11-RESEARCH.md
@.planning/phases/11-v2-hardware-migration/11-VALIDATION.md
@CLAUDE.md
@src/shitbox/events/sampler.py
@src/shitbox/events/ring_buffer.py
@src/shitbox/collectors/base.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED stubs for LSM6DSOX sampler + complementary filter</name>
  <files>tests/events/test_sampler_lsm6dsox.py, tests/collectors/test_imu_heading.py</files>
  <read_first>
    - src/shitbox/events/sampler.py (current MPU6050 sampler, being replaced)
    - src/shitbox/events/ring_buffer.py (IMUSample dataclass — g and deg/s fields)
    - .planning/phases/11-v2-hardware-migration/11-RESEARCH.md (unit conversion notes)
  </read_first>
  <behavior>
    test_sampler_lsm6dsox.py:
    - test_acceleration_converted_from_m_s2_to_g: mock adafruit LSM6DSOX `acceleration` returning (9.81, 0.0, 0.0) m/s², assert IMUSample.ax == pytest.approx(1.0, abs=0.001)
    - test_gyro_converted_from_rad_s_to_deg_s: mock `gyro` returning (math.pi, 0.0, 0.0) rad/s, assert IMUSample.gx == pytest.approx(180.0, abs=0.01)
    - test_sampler_graceful_when_lsm6dsox_absent: patch busio.I2C to raise, assert HighRateSampler.start() logs `sensor_init_failed` and sets `_sensor = None` without raising
    - test_sampler_produces_104_hz_rate: run sampler for 0.5s against fake sensor, assert sample count between 45 and 60
    - test_calibration_offsets_applied: ax_offset=0.1, raw 1.0 g -> stored 0.9 g

    test_imu_heading.py:
    - test_complementary_filter_alpha_098: given pitch=0 and gyro_x=10 deg/s for dt=0.01s, filter output ~0.098° toward gyro integration; alpha=0.98
    - test_tilt_compensated_heading_at_standstill: mag (20, 0, 0) µT, pitch=0 roll=0 -> heading ≈ 0° (±1°); mag (0, 20, 0) -> heading ≈ 90°
    - test_heading_available_at_zero_gps_speed: explicit assertion that filter output updates when velocity==0 (covers D-12)
    - test_lis3mdl_collector_graceful_absent: patch LIS3MDL to raise on init, assert collector logs and continues
  </behavior>
  <action>
    Create tests/events/test_sampler_lsm6dsox.py and tests/collectors/test_imu_heading.py.

    These MUST fail on first run (the implementation does not exist yet).

    Imports that will drive the RED state:
      `from shitbox.events.sampler import HighRateSampler`  (class exists but uses MPU6050; tests will fail on missing `_lsm6dsox` attr / wrong values)
      `from shitbox.collectors.imu_heading import IMUHeadingCollector`  (module does not exist yet — ImportError is the RED signal)

    **Unit conversion reminder, call it out in a comment at the top of test_sampler_lsm6dsox.py:**
      `# LSM6DSOX lib returns accel in m/s² and gyro in rad/s.`
      `# Sampler MUST convert: ax_g = ax_ms2 / 9.81 ; gx_dps = gx_rads * (180.0 / math.pi)`
      `# Getting this wrong silently breaks event detection thresholds.`

    Use pytest fixtures with `unittest.mock.MagicMock` to fake the sensor objects. Do not import real adafruit libs at module top (they may be absent on dev laptop).

    Run after creating: `pytest tests/events/test_sampler_lsm6dsox.py tests/collectors/test_imu_heading.py -x -q`
    Expected: all fail (ImportError or AssertionError). Record the failure output in the commit message.
  </action>
  <verify>
    <automated>pytest tests/events/test_sampler_lsm6dsox.py tests/collectors/test_imu_heading.py -q 2>&1 | grep -E "(failed|error)"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/events/test_sampler_lsm6dsox.py` exists
    - File `tests/collectors/test_imu_heading.py` exists
    - `grep -c "def test_" tests/events/test_sampler_lsm6dsox.py` >= 5
    - `grep -c "def test_" tests/collectors/test_imu_heading.py` >= 4
    - `grep -q "9.81" tests/events/test_sampler_lsm6dsox.py` succeeds (unit conversion explicit)
    - `grep -q "180" tests/events/test_sampler_lsm6dsox.py` succeeds (rad->deg conversion explicit)
    - `pytest tests/events/test_sampler_lsm6dsox.py tests/collectors/test_imu_heading.py -q` exits non-zero (tests are RED)
  </acceptance_criteria>
  <done>All listed tests exist, contain the listed assertions, and fail with a clear message. No skipped tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED stubs for DS18B20, VEML7700, SEN0460, INA226 collectors</name>
  <files>tests/collectors/test_temperature_ds18b20.py, tests/collectors/test_light_veml7700.py, tests/collectors/test_pm25_sen0460.py, tests/collectors/test_power_ina226.py</files>
  <read_first>
    - src/shitbox/collectors/base.py (BaseCollector lifecycle pattern)
    - src/shitbox/collectors/environment.py (reference pattern for graceful-absent handling)
    - src/shitbox/collectors/temperature.py (current MCP9808 code — being replaced)
    - src/shitbox/collectors/power.py (current INA219 code — being replaced)
  </read_first>
  <behavior>
    test_temperature_ds18b20.py:
    - test_reads_both_probes_by_id: mock W1ThermSensor with ids "exterior-id" and "engine-bay-id", assert two Reading objects produced, labelled exterior and engine_bay
    - test_role_mapping_from_config: config {exterior: "abc", engine_bay: "def"} -> Reading.role field set correctly
    - test_missing_probe_logged_not_raised: W1ThermSensor raises NoSensorFoundError, assert collector logs `ds18b20_sensor_not_found` and collect() returns None (no exception propagates)
    - test_graceful_when_w1thermsensor_import_fails: patch to ImportError, assert collector init logs and sets disabled

    test_light_veml7700.py:
    - test_reads_lux: mock VEML7700 `.lux` = 12345.6, assert Reading.value == 12345.6
    - test_graceful_when_sensor_absent: I2C NACK -> log sensor_init_failed, collect() returns None

    test_pm25_sen0460.py:
    - test_disabled_by_default_does_not_touch_i2c: config enabled=False, assert no I2C calls made
    - test_cable_pinout_docstring: assert collector module docstring contains "cyan=SDA" and "blue=SCL"
    - test_graceful_when_sensor_absent: enabled=True but I2C raises, assert logs and continues

    test_power_ina226.py:
    - test_disabled_by_default_does_not_touch_i2c
    - test_reads_bus_voltage_and_current_when_enabled: mock smbus2 reads, assert Reading contains voltage_v and current_a
    - test_graceful_when_sensor_absent
  </behavior>
  <action>
    Create four test files in tests/collectors/. All must fail at import time because the target modules do not exist yet:
      `from shitbox.collectors.temperature import DS18B20Collector`
      `from shitbox.collectors.light import VEML7700Collector`
      `from shitbox.collectors.particulate import SEN0460Collector`
      `from shitbox.collectors.power import INA226Collector`

    Use MagicMock heavily — no real hardware libraries imported at the top of test files.

    For SEN0460 cable-pinout test, the test asserts the final module's docstring (after wave 2 writes it) contains the exact strings "cyan=SDA" and "blue=SCL". This is a guardrail against future-Tony rewiring blind (D-05).

    Graceful degradation pattern the tests enforce (each collector): catch exceptions in `setup()` / `collect()`, log via `structlog` with event name `sensor_init_failed` or `sensor_read_error`, return None, never propagate. This mirrors `src/shitbox/collectors/environment.py`.

    Run after creating: `pytest tests/collectors/test_temperature_ds18b20.py tests/collectors/test_light_veml7700.py tests/collectors/test_pm25_sen0460.py tests/collectors/test_power_ina226.py -q`
    Expected: all ImportError / AssertionError.
  </action>
  <verify>
    <automated>pytest tests/collectors/test_temperature_ds18b20.py tests/collectors/test_light_veml7700.py tests/collectors/test_pm25_sen0460.py tests/collectors/test_power_ina226.py -q 2>&1 | grep -E "(failed|error)"</automated>
  </verify>
  <acceptance_criteria>
    - All four test files exist under tests/collectors/
    - `grep -c "def test_" tests/collectors/test_temperature_ds18b20.py` >= 4
    - `grep -c "def test_" tests/collectors/test_light_veml7700.py` >= 2
    - `grep -c "def test_" tests/collectors/test_pm25_sen0460.py` >= 3
    - `grep -c "def test_" tests/collectors/test_power_ina226.py` >= 3
    - `grep -q "cyan=SDA" tests/collectors/test_pm25_sen0460.py` succeeds
    - `grep -q "engine_bay" tests/collectors/test_temperature_ds18b20.py` succeeds
    - `pytest tests/collectors/test_temperature_ds18b20.py tests/collectors/test_light_veml7700.py tests/collectors/test_pm25_sen0460.py tests/collectors/test_power_ina226.py -q` exits non-zero
  </acceptance_criteria>
  <done>All four test files committed, failing at import time, and each asserts graceful-absent behaviour.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Hardware discovery on Pi 5 (DS18B20 IDs + ELP VID:PID)</name>
  <read_first>
    - ~/.claude/projects/-Users-tgreen-dev-shitbox/memory/project_pi5_setup.md (Pi 5 IP: 10.10.20.107 wlan0; services stopped)
    - .planning/phases/11-v2-hardware-migration/11-CONTEXT.md D-03 and D-13
  </read_first>
  <what-built>
    Nothing code-wise. This is hardware discovery that cannot be automated from the dev laptop.
  </what-built>
  <action>See how-to-verify block below for concrete SSH commands and expected outputs.</action>
  <how-to-verify>
    SSH to the Pi 5 (10.10.20.107) and run the following. Record the output into
    `.planning/phases/11-v2-hardware-migration/HARDWARE_IDS.md`:

    1. **DS18B20 1-Wire sensor IDs (for D-03 exterior + engine_bay roles):**
       ```bash
       ssh tgreen@10.10.20.107 'ls /sys/bus/w1/devices/ | grep -E "^28-"'
       ssh tgreen@10.10.20.107 'for id in $(ls /sys/bus/w1/devices/ | grep ^28-); do echo "$id: $(cat /sys/bus/w1/devices/$id/w1_slave | tail -1)"; done'
       ```
       Physically warm one probe (hand it, or dip in warm water) to identify which
       ID is which. Label one `exterior` and one `engine_bay` by intended mounting location.

    2. **ELP 4K USB camera VID:PID (for D-13 udev rule):**
       ```bash
       ssh tgreen@10.10.20.107 'lsusb | grep -i -E "(elp|imx317|32e4|05a3)"'
       ssh tgreen@10.10.20.107 'v4l2-ctl --list-devices'
       ```
       Record the `ID xxxx:yyyy` pair and the `/dev/videoN` device it enumerates as.

    3. **Brio 100 cabin camera VID:PID (for continuity — D-14 says unchanged, confirm):**
       ```bash
       ssh tgreen@10.10.20.107 'lsusb | grep -i logitech'
       ```

    4. **Write `.planning/phases/11-v2-hardware-migration/HARDWARE_IDS.md`** with:
       ```markdown
       # Phase 11 — Hardware IDs (recorded 2026-04-09)

       ## DS18B20 1-Wire probes
       - exterior: `28-xxxxxxxxxxxx`
       - engine_bay: `28-yyyyyyyyyyyy`

       ## ELP 4K front camera
       - USB ID: `xxxx:yyyy`
       - Current device: `/dev/videoN`

       ## Brio 100 cabin camera
       - USB ID: `xxxx:yyyy` (unchanged from v1)
       ```
  </how-to-verify>
  <resume-signal>
    Type "recorded" once HARDWARE_IDS.md is written and committed, or paste issues encountered.
  </resume-signal>
  <acceptance_criteria>
    - File `.planning/phases/11-v2-hardware-migration/HARDWARE_IDS.md` exists
    - `grep -E "^- exterior: \`28-" .planning/phases/11-v2-hardware-migration/HARDWARE_IDS.md` matches
    - `grep -E "^- engine_bay: \`28-" .planning/phases/11-v2-hardware-migration/HARDWARE_IDS.md` matches
    - `grep -E "USB ID:" .planning/phases/11-v2-hardware-migration/HARDWARE_IDS.md | wc -l` >= 2
  </acceptance_criteria>
  <done>HARDWARE_IDS.md committed with real values for both DS18B20 probes and both cameras. Wave 2 and wave 3 cannot proceed without these.</done>
</task>

</tasks>

<verification>
- `pytest tests/events/test_sampler_lsm6dsox.py tests/collectors/ -q` exits non-zero (RED phase)
- `cat .planning/phases/11-v2-hardware-migration/HARDWARE_IDS.md` shows real IDs, not placeholders
</verification>

<success_criteria>
Six failing test files committed, HARDWARE_IDS.md committed, `pytest -q` has a clear failure list that waves 1-2 will turn green.
</success_criteria>

<output>
Create `.planning/phases/11-v2-hardware-migration/11-00-SUMMARY.md` listing: test files created, failing assertions, DS18B20 IDs discovered, ELP VID:PID discovered.
</output>
