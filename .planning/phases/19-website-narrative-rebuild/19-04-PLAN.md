---
phase: 19
plan: "04"
type: execute
wave: 2
depends_on: ["19-02", "19-03"]
files_modified:
  - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html
autonomous: true
requirements:
  - NARR-05
must_haves:
  truths:
    - "A site-wide day-nav progress bar sits under the top nav and renders one segment per rally day"
    - "Each segment shows a large day number and a small date (DD MMM)"
    - "Segments have three visual states: completed (filled orange), current (bright amber), future (grey, inert)"
    - "Completed and current segments are anchors linking to /day/YYYY-MM-DD; future segments are non-clickable spans"
    - "Clicking a segment calls navigateToDay(dayISO) (pushState, no full reload)"
    - "On mobile (<600px), the date sub-line hides so only day numbers show"
    - "renderDayNav(agenda, highlightDayISO) is called on homepage render AND on day-page render (site-wide visibility)"
  artifacts:
    - path: "~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html"
      provides: "renderDayNav() function + CSS for .day-nav / .day-nav-segment.{completed,current,future}"
      contains: "function renderDayNav"
  key_links:
    - from: "renderDayPage / renderHomepage stubs (from Plan 19-03)"
      to: "renderDayNav(agendaData, dayISO)"
      via: "called first thing in both render functions so day-nav is always visible"
      pattern: "renderDayNav\\("
    - from: ".day-nav-segment.completed anchor click"
      to: "navigateToDay"
      via: "href + onclick that calls event.preventDefault() + navigateToDay(date)"
      pattern: "navigateToDay\\("
---

<objective>
Land the site-wide day-nav progress bar component (D-14, D-15). This visual primitive is the single continuous element that makes the site feel day-shaped — it renders identically on the homepage and on every day page. Plans 19-05/08/09/10 reuse this without touching it.

Purpose: Collapse three orthogonal UX roles into one element: (1) arc indicator (how much of the rally is done), (2) navigation (click to jump), (3) emotional shape (visual density of the rally). Extends the existing `.rally-progress` visual language from lines 99-134 rather than introducing a new pattern.

Output:
- CSS block `.day-nav`, `.day-nav-segment`, `.day-nav-segment.completed`, `.day-nav-segment.current`, `.day-nav-segment.future`, mobile media query.
- HTML anchor `<div id="day-nav" class="day-nav"></div>` placed immediately after `<nav>` (line 677).
- JS function `renderDayNav(agenda, highlightDayISO)` that builds segment HTML from `agenda.days`.
- Call sites in `renderDayPage()` and `renderHomepage()` (stubs from Plan 19-03) to invoke `renderDayNav`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-website-narrative-rebuild/19-CONTEXT.md
@.planning/phases/19-website-narrative-rebuild/19-RESEARCH.md

<interfaces>
From webroot/index.html lines 99-134 (extend, don't replace — day-nav shares typographic treatment):

    .rally-progress { background:#161b22; border:1px solid #2a1f0e; border-radius:8px; ... }
    .rally-progress-bar { height:8px; background:#21262d; ... }
    .rally-progress-fill { background:#c06000; ... }

From Plan 19-03:

    function navigateToDay(dayISO) { history.pushState({route:'day', day:dayISO}, '', '/day/'+dayISO); renderDayPage(dayISO); window.scrollTo(0,0); }
    function renderHomepage() { ... }
    function renderDayPage(dayISO) { ... }

From Plan 19-02:

    agendaData = { rally: {start_date, end_date, ...}, days: [{date, day_number, title, ...}, ...] }

Dark theme tokens (locked from Phase 18):
- Completed segment: `#c06000` orange + `#fff` text
- Current segment: `#e0a040` amber + `#0d1117` dark text + `font-weight:600`
- Future segment: `#21262d` grey + `#484f58` muted text
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agendaData (fetched JSON) → DOM via innerHTML | Content is from our own ConfigMap; not user-controlled, but still escape defensively. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-01 | XSS | Agenda title injected into day-nav innerHTML | mitigate | Day-nav only prints `day_number` (integer) and a formatted date derived from `date` field. Agenda `title` is NOT rendered in day-nav segments (only on day pages). Integer coercion: `String(Number(day.day_number))`. Date formatting via `Date` object + `toLocaleDateString`. |
| T-19-04-02 | Denial of Service | Agenda with thousands of days renders wide strip | accept | Agenda is ≤14 days for a normal rally. Even a badly-authored 100-day agenda renders in <10 ms and stays under 1 viewport height. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: CSS + HTML container for day-nav</name>
  <files>~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html</files>
  <read_first>
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html lines 99-134 (rally-progress CSS — the visual base)
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html lines 667-678 (`<nav>` block — insertion point for day-nav sits RIGHT AFTER this)
    - .planning/phases/19-website-narrative-rebuild/19-RESEARCH.md §7 (day-nav CSS reference)
  </read_first>
  <action>
Edit 1 — add CSS block. Inside the existing `<style>` element, AFTER the `.rally-progress-fill` rule (around line 134), insert:

    /* Day-nav progress bar (Phase 19 — D-14, D-15) */
    .day-nav {
        display: grid;
        grid-auto-columns: 1fr;
        grid-auto-flow: column;
        gap: 2px;
        background: #161b22;
        border: 1px solid #2a1f0e;
        border-radius: 8px;
        padding: 0.5rem;
        margin-bottom: 1.5rem;
    }
    .day-nav-segment {
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 0.5rem 0.25rem;
        border-radius: 4px;
        background: #21262d;
        color: #8b949e;
        text-align: center;
        text-decoration: none;
        transition: background 0.15s, opacity 0.15s;
    }
    .day-nav-segment.completed { background: #c06000; color: #fff; }
    .day-nav-segment.current   { background: #e0a040; color: #0d1117; font-weight: 600; }
    .day-nav-segment.future    { background: #21262d; color: #484f58; cursor: default; pointer-events: none; }
    .day-nav-segment.completed:hover,
    .day-nav-segment.current:hover { opacity: 0.85; }
    .day-nav-segment:focus-visible { outline: 2px solid #e0a040; outline-offset: 2px; }
    .day-nav-number { font-size: 1.1rem; font-weight: 600; line-height: 1.1; }
    .day-nav-date   { font-size: 0.7rem; margin-top: 0.15rem; }
    @media (max-width: 600px) {
        .day-nav-date { display: none; }
        .day-nav-segment { padding: 0.4rem 0.15rem; }
    }

Edit 2 — add HTML container. Immediately after the closing `</nav>` tag (line ~677) and before the `<main>` tag (line ~679), insert:

    <div id="day-nav" class="day-nav" style="display:none;"></div>

`display:none` is the default; `renderDayNav` unhides when agendaData is available.

Commit: `feat(19-04): CSS + container for day-nav progress bar`.
  </action>
  <verify>
    <automated>grep -c '\.day-nav-segment\.completed' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^2$' && grep -c '\.day-nav-segment\.current' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^2$' && grep -c '\.day-nav-segment\.future' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c '<div id="day-nav" class="day-nav"' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - File contains `.day-nav {` CSS rule
    - File contains `.day-nav-segment.completed` rule (with `background: #c06000`)
    - File contains `.day-nav-segment.current` rule (with `background: #e0a040`)
    - File contains `.day-nav-segment.future` rule (with `pointer-events: none`)
    - File contains `@media (max-width: 600px)` with `.day-nav-date { display: none; }`
    - File contains one `<div id="day-nav" class="day-nav"` (inserted between `</nav>` and `<main>`)
    - File still contains original `.rally-progress` CSS (no deletion)
    - File contains `:focus-visible` rule on `.day-nav-segment` (keyboard a11y)
  </acceptance_criteria>
  <done>CSS block + container landed, not wired yet. Deploy shows zero visual change (container is display:none).</done>
</task>

<task type="auto">
  <name>Task 2: renderDayNav function + wire into renderDayPage + renderHomepage</name>
  <files>~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html</files>
  <read_first>
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html (re-read to confirm Plan 19-03 stub signatures landed — `renderDayPage(dayISO)` and `renderHomepage()`)
    - .planning/phases/19-website-narrative-rebuild/19-RESEARCH.md §7 (renderDayNav reference)
  </read_first>
  <action>
Inside the existing IIFE, after `function route()` / `navigateToDay()` (from Plan 19-03) and before the stub `renderDayPage()`, add:

    function _formatShortDate(iso) {
        // '2026-05-27' → '27 May' using Sydney locale
        var parts = iso.split('-');
        if (parts.length !== 3) return iso;
        var d = new Date(Date.UTC(parseInt(parts[0],10), parseInt(parts[1],10)-1, parseInt(parts[2],10)));
        if (isNaN(d.getTime())) return iso;
        return d.toLocaleDateString('en-GB', { day: '2-digit', month: 'short', timeZone: 'UTC' });
    }

    function renderDayNav(agenda, highlightDayISO) {
        var host = document.getElementById('day-nav');
        if (!host) return;
        if (!agenda || !agenda.days || agenda.days.length === 0) {
            host.style.display = 'none';
            host.innerHTML = '';
            return;
        }
        var today = new Date().toLocaleDateString('en-CA', { timeZone: TIMEZONE });
        var html = '';
        agenda.days.forEach(function(day) {
            var state;
            if (highlightDayISO && day.date === highlightDayISO) {
                state = 'current';
            } else if (day.date < today) {
                state = 'completed';
            } else if (day.date === today) {
                state = 'current';
            } else {
                state = 'future';
            }
            var num = String(Number(day.day_number) || 0);
            var dateLabel = _formatShortDate(day.date);
            if (state === 'future') {
                html += '<span class="day-nav-segment future" aria-label="Day ' + num + ' — upcoming">'
                     +  '<span class="day-nav-number">' + num + '</span>'
                     +  '<span class="day-nav-date">' + dateLabel + '</span>'
                     +  '</span>';
            } else {
                // Anchor form — navigateToDay handles pushState; real href is the fallback for middle-click / copy-link.
                html += '<a class="day-nav-segment ' + state + '" href="/day/' + day.date + '" '
                     +  'onclick="event.preventDefault(); navigateToDay(\'' + day.date + '\');" '
                     +  'aria-label="Day ' + num + ' — ' + (state === 'current' ? 'today' : 'past') + '">'
                     +  '<span class="day-nav-number">' + num + '</span>'
                     +  '<span class="day-nav-date">' + dateLabel + '</span>'
                     +  '</a>';
            }
        });
        host.innerHTML = html;
        host.style.display = '';
    }

Then update the Plan 19-03 stubs to call `renderDayNav` at the TOP of each renderer:

- In `renderDayPage(dayISO)`, as the FIRST line of the function body: `renderDayNav(agendaData, dayISO);`
- In `renderHomepage()`, as the FIRST line of the function body: `renderDayNav(agendaData, null);`
- In `renderAbout()`, as the FIRST line of the function body: `renderDayNav(agendaData, null);`

The day-nav sits in a shared site-wide container so it appears on all three page types.

Also — the `agendaData` fetch completes asynchronously. The router in Plan 19-03 uses `Promise.allSettled` to await it, so by the time any `render*` runs, `agendaData` is either populated or null. `renderDayNav` handles the null case via its early-return.

Commit: `feat(19-04): renderDayNav with per-segment states + site-wide wiring`.
  </action>
  <verify>
    <automated>grep -c 'function renderDayNav(agenda, highlightDayISO)' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'renderDayNav(agendaData,' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | awk '{exit ($1 >= 3) ? 0 : 1}' && grep -c '_formatShortDate' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | awk '{exit ($1 >= 2) ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly one `function renderDayNav(agenda, highlightDayISO)`
    - File contains exactly one `function _formatShortDate(iso)`
    - File contains at least 3 call sites of `renderDayNav(agendaData,` (in renderDayPage, renderHomepage, renderAbout)
    - `renderDayNav` uses `String(Number(day.day_number) || 0)` (integer coercion for XSS safety)
    - Each anchor contains an `aria-label` attribute (a11y)
    - Loading the site after deploy shows 10 segments (one per day from agenda.json) under the top nav
    - All 10 segments currently render as `.future` (rally is in the future as of 2026-04-16)
    - Clicking a future segment does nothing (pointer-events: none)
    - After rally `start_date`, past days render as `.completed` and today as `.current`
    - Keyboard Tab traverses to day-nav segments; focused segment shows the amber outline
  </acceptance_criteria>
  <done>renderDayNav() landed. Every page (home, day, about) shows the site-wide day-nav with correct segment states.</done>
</task>

</tasks>

<verification>
Plan 19-04 complete when:
- grep checks in acceptance_criteria all pass
- Browser manual check on `/` shows 10 day-nav segments under the top nav
- Navigating to `/day/2026-05-28` highlights day 2 as `.current` and day 1 as `.completed`
- Mobile viewport (DevTools responsive mode, width 375px) hides the date sub-line
</verification>

<success_criteria>
- NARR-05: Site-wide day-nav progress bar renders on all three page types with correct segment states.
</success_criteria>

<output>
After completion, create `.planning/phases/19-website-narrative-rebuild/19-04-SUMMARY.md`. Note any hover/focus state deviations.
</output>
