---
phase: 19
plan: "06"
type: execute
wave: 2
depends_on: ["19-02", "19-03", "19-05"]
files_modified:
  - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html
autonomous: true
requirements:
  - NARR-03
must_haves:
  truths:
    - "buildSpine(dayISO, data) produces a single timestamp-sorted array merging events, notes, fuel, driver changes, stage bookends, and agenda markers for that day"
    - "The spine renders into the #day-spine placeholder in renderDayPage — no more dashed placeholder"
    - "Each spine item has a kind-specific card: event, note, fuel, driver_change, stage_start, stage_end, agenda_meal, agenda_camping"
    - "Spine items are left-rail timeline-styled: time on left, icon in middle rail, card on right"
    - "Event cards reuse the existing BADGE_COLORS mapping for consistency with the legacy Videos tab"
    - "Note cards show body prose; fuel cards show litres + km/L if present; driver_change shows from→to"
    - "Agenda markers (meals, camping) interleave by time so the day reads narratively"
    - "Empty day (no data) shows a muted 'Nothing logged yet.' placeholder, not the dashed box"
  artifacts:
    - path: "~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html"
      provides: "buildSpine + renderSpine + per-kind renderers + .spine* CSS"
      contains: "function buildSpine"
  key_links:
    - from: "renderDayPage #day-spine placeholder (Plan 19-05)"
      to: "renderSpine(buildSpine(dayISO, {events, notes, fuel, driverStats, agenda}))"
      via: "innerHTML assignment after renderDayPage composes its skeleton"
      pattern: "renderSpine\\("
    - from: "buildSpine()"
      to: "sorted spine items"
      via: "Array.concat across 6 sources + sort by timestamp ascending"
      pattern: "buildSpine"
---

<objective>
Materialise the timeline spine — the narrative heart of every day page. This plan replaces the Plan 19-05 `#day-spine` placeholder with a single timestamp-sorted stream of everything that happened on a given day.

Purpose: Deliver D-06 (one timeline, not separate tabs) and D-07 (interleave events + notes + fuel + driver changes + agenda markers). This is the element that makes a day page read like a scrapbook page rather than a dashboard tab.

Output additions to `webroot/index.html`:
- `.spine`, `.spine-item`, `.spine-time`, `.spine-rail`, `.spine-dot`, `.spine-card` CSS with kind modifiers.
- `function buildSpine(dayISO, data)` — pure merge-and-sort across 6 sources.
- `function renderSpine(items)` — dispatches each item to its per-kind renderer.
- Per-kind renderers: `_spineEventCard`, `_spineNoteCard`, `_spineFuelCard`, `_spineDriverChange`, `_spineStageBookend`, `_spineAgendaMeal`, `_spineAgendaCamping`.
- Call from `renderDayPage` to populate `#day-spine` after the scaffold mounts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-website-narrative-rebuild/19-CONTEXT.md
@.planning/phases/19-website-narrative-rebuild/19-RESEARCH.md

<interfaces>
From Plan 19-02 / existing code:

    eventsData = [ { id, timestamp, type, peak_g, peak_speed_kmh, duration_ms, lat, lng, video_url, video_path, distance_from_start_km, active_driver } ]
    notesData  = [ { id, timestamp_utc, body, event_id, lat, lng } ]
    fuelData   = [ { id, timestamp_utc, volume_litres, lat, lng, odometer_km, km_per_litre } ]
    driverStatsData = { active_driver, drivers:[ {driver_name, total_seconds, pct} ] }

From Plan 19-05:

    function renderDayPage(dayISO) {
        ...
        var spinePlaceholder = '<section><h3 ...>Timeline</h3><div id="day-spine" class="day-section-placeholder">...</div></section>';
        ...
        showRoute(html);
    }

From webroot/index.html line 994-1002:

    var BADGE_COLORS = {
        HIGH_G:     '#da3633',
        BIG_CORNER: '#d29922',
        HARD_BRAKE: '#f85149',
        ROUGH_ROAD: '#8957e5',
        MANUAL:     '#238636',
        BUTTON:     '#238636',
        BOOT:       '#1f6feb'
    };

From Plan 19-05 helpers (already landed):

    function escapeHtml(s) { ... }
    function _dayOf(iso) { ... }  // en-CA / Australia/Sydney
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notes.body / fuel.odometer / driver_name → innerHTML | Content is from Pi SQLite, not user-supplied via browser input, but we still escape defensively. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06-01 | XSS | notes.body concatenated into spine card HTML | mitigate | Every string field (body, driver_name, event type, meals.where, camping) flows through `escapeHtml()` before innerHTML. Numeric fields coerced via `Number()` + `toFixed`. |
| T-19-06-02 | Information Disclosure | Fuel cost shown on public site | mitigate | `_spineFuelCard` reads ONLY `volume_litres` and `km_per_litre`. Grep proof in acceptance criteria. |
| T-19-06-03 | DoS | Massive day with 1000s of events renders slowly | accept | A rally day has <~50 events realistically. Even 500 renders in <30 ms (pure string concat). No virtualisation needed. |
| T-19-06-04 | Logic error | Missing timestamps cause sort to misbehave | mitigate | `buildSpine` filters items with falsy `_ts` before sort. Each source-to-spine mapper computes `_ts = Date.parse(<field>)` and skips NaN results. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: CSS + buildSpine merge + renderSpine dispatcher</name>
  <files>~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html</files>
  <read_first>
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html (confirm BADGE_COLORS, TIMEZONE, escapeHtml, _dayOf exist)
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html lines 99-200 (existing CSS conventions — border-radius, padding, dark tokens)
    - .planning/phases/19-website-narrative-rebuild/19-RESEARCH.md buildSpine reference (merge algorithm, per-kind renderers)
  </read_first>
  <action>
Edit 1 — CSS. Inside `<style>`, after the `.day-stat-card` / `.day-section-placeholder` block from Plan 19-05, append:

    /* Timeline spine (Phase 19 — D-06, D-07) */
    .spine { position: relative; padding-left: 0; }
    .spine-empty {
        background: #161b22; border: 1px dashed #30363d; border-radius: 8px;
        padding: 1.25rem; color: #8b949e; font-style: italic; text-align: center;
    }
    .spine-item {
        display: grid;
        grid-template-columns: 72px 24px 1fr;
        gap: 0.75rem;
        align-items: start;
        margin-bottom: 1rem;
    }
    .spine-time {
        color: #8b949e; font-size: 0.8rem; font-variant-numeric: tabular-nums;
        padding-top: 0.5rem; text-align: right;
    }
    .spine-rail {
        position: relative; width: 24px; min-height: 100%;
    }
    .spine-rail::before {
        content: ''; position: absolute; left: 11px; top: 0; bottom: -1rem;
        width: 2px; background: #2a1f0e;
    }
    .spine-item:last-child .spine-rail::before { bottom: 50%; }
    .spine-dot {
        position: absolute; left: 4px; top: 0.6rem;
        width: 16px; height: 16px; border-radius: 50%;
        background: #21262d; border: 2px solid #0d1117;
    }
    .spine-card {
        background: #161b22; border: 1px solid #2a1f0e; border-radius: 8px;
        padding: 0.75rem 1rem; color: #c9d1d9;
    }
    .spine-card .spine-card-title { color: #f0dbb8; font-weight: 600; margin-bottom: 0.25rem; }
    .spine-card .spine-card-meta  { color: #8b949e; font-size: 0.8rem; }
    .spine-card .spine-card-body  { color: #c9d1d9; margin-top: 0.35rem; line-height: 1.5; }
    .spine-card video { display: block; margin-top: 0.5rem; max-width: 100%; border-radius: 4px; }
    .spine-item.event .spine-dot        { background: #da3633; }
    .spine-item.note  .spine-dot        { background: #1f6feb; }
    .spine-item.fuel  .spine-dot        { background: #2ea043; }
    .spine-item.driver_change .spine-dot { background: #8957e5; }
    .spine-item.stage_start .spine-dot  { background: #e0a040; }
    .spine-item.stage_end   .spine-dot  { background: #8b949e; }
    .spine-item.agenda_meal .spine-dot    { background: #d29922; }
    .spine-item.agenda_camping .spine-dot { background: #6e7681; }
    @media (max-width: 600px) {
        .spine-item { grid-template-columns: 56px 20px 1fr; gap: 0.5rem; }
        .spine-time { font-size: 0.72rem; }
        .spine-dot  { left: 2px; width: 14px; height: 14px; }
    }

Edit 2 — buildSpine + helper + renderSpine. Inside the IIFE, after the `computeDayStats` block from Plan 19-05, add:

    function _formatSpineTime(iso) {
        try {
            var d = new Date(iso);
            if (isNaN(d.getTime())) return '';
            return d.toLocaleTimeString('en-GB', { hour: '2-digit', minute: '2-digit', timeZone: TIMEZONE });
        } catch (e) { return ''; }
    }

    function buildSpine(dayISO, data) {
        var items = [];
        var events = (data && data.events) || [];
        var notes  = (data && data.notes)  || [];
        var fuel   = (data && data.fuel)   || [];
        var agendaDay = (data && data.agendaDay) || null;

        events.forEach(function(ev) {
            if (_dayOf(ev.timestamp) !== dayISO) return;
            var ts = Date.parse(ev.timestamp);
            if (isNaN(ts)) return;
            items.push({ kind: 'event', _ts: ts, ts_iso: ev.timestamp, payload: ev });
        });

        notes.forEach(function(n) {
            if (_dayOf(n.timestamp_utc) !== dayISO) return;
            var ts = Date.parse(n.timestamp_utc);
            if (isNaN(ts)) return;
            items.push({ kind: 'note', _ts: ts, ts_iso: n.timestamp_utc, payload: n });
        });

        fuel.forEach(function(f) {
            if (_dayOf(f.timestamp_utc) !== dayISO) return;
            var ts = Date.parse(f.timestamp_utc);
            if (isNaN(ts)) return;
            items.push({ kind: 'fuel', _ts: ts, ts_iso: f.timestamp_utc, payload: f });
        });

        // Driver-change markers: derived from events[].active_driver transitions within this day.
        var driverSeq = events
            .filter(function(ev) { return _dayOf(ev.timestamp) === dayISO && ev.active_driver; })
            .sort(function(a, b) { return Date.parse(a.timestamp) - Date.parse(b.timestamp); });
        var prev = null;
        driverSeq.forEach(function(ev) {
            if (ev.active_driver !== prev) {
                var ts = Date.parse(ev.timestamp);
                if (!isNaN(ts)) {
                    items.push({
                        kind: 'driver_change', _ts: ts, ts_iso: ev.timestamp,
                        payload: { from: prev, to: ev.active_driver }
                    });
                }
                prev = ev.active_driver;
            }
        });

        // Agenda markers: meals + camping. Meals have HH:MM time; camping pins to 20:00 local.
        if (agendaDay && agendaDay.meals) {
            agendaDay.meals.forEach(function(m) {
                if (!m.time) return;
                var iso = dayISO + 'T' + m.time + ':00';
                var ts = Date.parse(iso + '+10:00'); // Sydney-ish; close enough for ordering
                if (isNaN(ts)) return;
                items.push({ kind: 'agenda_meal', _ts: ts, ts_iso: iso, payload: m });
            });
        }
        if (agendaDay && agendaDay.camping) {
            var campIso = dayISO + 'T20:00:00';
            var ts = Date.parse(campIso + '+10:00');
            if (!isNaN(ts)) {
                items.push({ kind: 'agenda_camping', _ts: ts, ts_iso: campIso, payload: { where: agendaDay.camping } });
            }
        }

        // Stage bookends: first and last event of the day = start/end markers for now.
        // (Future: derive from GPS motion transitions. Out of scope this plan — comment as extension point.)
        var dayEvents = items.filter(function(i) { return i.kind === 'event'; });
        if (dayEvents.length > 0) {
            items.push({
                kind: 'stage_start', _ts: dayEvents[0]._ts - 1, ts_iso: dayEvents[0].ts_iso,
                payload: { label: 'Day started' }
            });
            var last = dayEvents[dayEvents.length - 1];
            items.push({
                kind: 'stage_end', _ts: last._ts + 1, ts_iso: last.ts_iso,
                payload: { label: 'Last activity' }
            });
        }

        items.sort(function(a, b) { return a._ts - b._ts; });
        return items;
    }

    function renderSpine(items) {
        if (!items || items.length === 0) {
            return '<div class="spine-empty">Nothing logged yet for this day.</div>';
        }
        var out = '<div class="spine">';
        items.forEach(function(it) {
            var time = _formatSpineTime(it.ts_iso);
            var card;
            switch (it.kind) {
                case 'event':           card = _spineEventCard(it.payload); break;
                case 'note':            card = _spineNoteCard(it.payload); break;
                case 'fuel':            card = _spineFuelCard(it.payload); break;
                case 'driver_change':   card = _spineDriverChange(it.payload); break;
                case 'stage_start':
                case 'stage_end':       card = _spineStageBookend(it.kind, it.payload); break;
                case 'agenda_meal':     card = _spineAgendaMeal(it.payload); break;
                case 'agenda_camping':  card = _spineAgendaCamping(it.payload); break;
                default:                card = '<div class="spine-card">' + escapeHtml(it.kind) + '</div>';
            }
            out += '<div class="spine-item ' + it.kind + '">'
                +    '<div class="spine-time">' + escapeHtml(time) + '</div>'
                +    '<div class="spine-rail"><div class="spine-dot"></div></div>'
                +    card
                +  '</div>';
        });
        out += '</div>';
        return out;
    }

Commit: `feat(19-06): spine CSS + buildSpine merge + renderSpine dispatcher`.
  </action>
  <verify>
    <automated>grep -c 'function buildSpine' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'function renderSpine' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c '\.spine-item\.event' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c '\.spine-item\.note' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c '\.spine-item\.fuel' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c '\.spine-item\.driver_change' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c '\.spine-item\.agenda_meal' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly one `function buildSpine(dayISO, data)`
    - File contains exactly one `function renderSpine(items)`
    - File contains exactly one `function _formatSpineTime(iso)`
    - CSS includes all 8 spine-item kind modifiers: event, note, fuel, driver_change, stage_start, stage_end, agenda_meal, agenda_camping
    - CSS `@media (max-width: 600px)` tightens spine grid columns for mobile
    - `buildSpine` filters items by `_dayOf(...) === dayISO` — no cross-day leaks
    - `buildSpine` skips items with unparseable timestamps (NaN guard)
    - Items are sorted ascending by `_ts` (stable order for narrative read)
    - Empty input returns the `.spine-empty` message, not an empty `.spine` div
  </acceptance_criteria>
  <done>CSS + merge algorithm landed. Dispatcher wired but per-kind renderers stubbed in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Per-kind spine card renderers + wire into renderDayPage</name>
  <files>~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html</files>
  <read_first>
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html (confirm Task 1 `renderSpine` dispatcher already calls `_spine*` helpers — this task implements them)
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html lines 1485-1530 (existing event card rendering pattern — mirror BADGE_COLORS + video element)
    - ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html line 994-1002 (BADGE_COLORS constant)
  </read_first>
  <action>
Edit 1 — per-kind renderers. Add BEFORE `renderSpine` (or after — order does not matter inside the IIFE, they are all function declarations hoisted):

    function _spineEventCard(ev) {
        var type = escapeHtml(ev.type || 'EVENT');
        var colour = BADGE_COLORS[ev.type] || '#8b949e';
        var metaBits = [];
        if (ev.peak_g != null)         metaBits.push(Number(ev.peak_g).toFixed(2) + ' g');
        if (ev.peak_speed_kmh != null) metaBits.push(Number(ev.peak_speed_kmh).toFixed(0) + ' km/h');
        if (ev.duration_ms != null)    metaBits.push((Number(ev.duration_ms)/1000).toFixed(1) + ' s');
        var video = '';
        var videoSrc = ev.video_url || (ev.video_path ? '/captures/' + ev.video_path : null);
        if (videoSrc) {
            video = '<video controls preload="metadata" playsinline src="' + escapeHtml(videoSrc) + '"></video>';
        }
        return '<div class="spine-card">'
             +   '<div class="spine-card-title" style="color:' + colour + ';">' + type + '</div>'
             +   (metaBits.length ? '<div class="spine-card-meta">' + escapeHtml(metaBits.join(' · ')) + '</div>' : '')
             +   video
             + '</div>';
    }

    function _spineNoteCard(n) {
        return '<div class="spine-card">'
             +   '<div class="spine-card-title">Note</div>'
             +   '<div class="spine-card-body">' + escapeHtml(n.body || '') + '</div>'
             + '</div>';
    }

    function _spineFuelCard(f) {
        // PII guard: NEVER read cost_aud or price_aud from fuel payloads.
        var bits = [];
        if (f.volume_litres != null) bits.push(Number(f.volume_litres).toFixed(1) + ' L');
        if (f.km_per_litre != null)  bits.push(Number(f.km_per_litre).toFixed(1) + ' km/L');
        if (f.odometer_km != null)   bits.push('odo ' + Number(f.odometer_km).toFixed(0) + ' km');
        return '<div class="spine-card">'
             +   '<div class="spine-card-title" style="color:#2ea043;">Fuel stop</div>'
             +   '<div class="spine-card-meta">' + escapeHtml(bits.join(' · ')) + '</div>'
             + '</div>';
    }

    function _spineDriverChange(p) {
        var from = p.from ? escapeHtml(p.from) : 'Start';
        var to   = escapeHtml(p.to || '');
        return '<div class="spine-card">'
             +   '<div class="spine-card-title" style="color:#8957e5;">Driver change</div>'
             +   '<div class="spine-card-body">' + from + ' → ' + to + '</div>'
             + '</div>';
    }

    function _spineStageBookend(kind, p) {
        var label = escapeHtml(p.label || kind);
        var colour = (kind === 'stage_start') ? '#e0a040' : '#8b949e';
        return '<div class="spine-card">'
             +   '<div class="spine-card-title" style="color:' + colour + ';">' + label + '</div>'
             + '</div>';
    }

    function _spineAgendaMeal(m) {
        return '<div class="spine-card">'
             +   '<div class="spine-card-title" style="color:#d29922;">Meal</div>'
             +   '<div class="spine-card-body">' + escapeHtml(m.where || '') + '</div>'
             + '</div>';
    }

    function _spineAgendaCamping(p) {
        return '<div class="spine-card">'
             +   '<div class="spine-card-title" style="color:#6e7681;">Camp</div>'
             +   '<div class="spine-card-body">' + escapeHtml(p.where || '') + '</div>'
             + '</div>';
    }

Edit 2 — wire spine into renderDayPage. In the `renderDayPage(dayISO)` function from Plan 19-05, REPLACE the `spinePlaceholder` line with a post-render fill step. The scaffold still contains `<div id="day-spine">`; after `showRoute(html)`, immediately populate it:

    // ... existing renderDayPage body ends with showRoute(html);

    // Append AFTER showRoute(html) — the #day-spine node is now in the DOM:
    var spineHost = document.getElementById('day-spine');
    if (spineHost) {
        spineHost.classList.remove('day-section-placeholder');
        spineHost.innerHTML = renderSpine(buildSpine(dayISO, {
            events: eventsData || [],
            notes:  notesData  || [],
            fuel:   fuelData   || [],
            agendaDay: agendaDay  // same var already computed earlier in renderDayPage
        }));
    }

Replace the placeholder scaffolding line in `renderDayPage`:

    // BEFORE:
    var spinePlaceholder = '<section><h3 class="day-section-heading">Timeline</h3><div id="day-spine" class="day-section-placeholder">Timeline spine placeholder — wired by Plan 19-06.</div></section>';
    // AFTER:
    var spinePlaceholder = '<section><h3 class="day-section-heading">Timeline</h3><div id="day-spine"></div></section>';

The `day-section-placeholder` class is removed so the empty spine uses its own `.spine-empty` styling rather than the dashed placeholder box.

Commit: `feat(19-06): spine per-kind cards + wire into day-page render`.
  </action>
  <verify>
    <automated>grep -c 'function _spineEventCard' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'function _spineNoteCard' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'function _spineFuelCard' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'function _spineDriverChange' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'function _spineStageBookend' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'function _spineAgendaMeal' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'function _spineAgendaCamping' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && grep -c 'renderSpine(buildSpine' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html | grep -q '^1$' && ! grep -E 'cost_aud|price_aud' ~/dev/home-ops/kubernetes/apps/default/shit-of-theseus/app/webroot/index.html</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly one of each: `_spineEventCard`, `_spineNoteCard`, `_spineFuelCard`, `_spineDriverChange`, `_spineStageBookend`, `_spineAgendaMeal`, `_spineAgendaCamping`
    - File contains exactly one `renderSpine(buildSpine(dayISO,` call (inside renderDayPage)
    - No `cost_aud` or `price_aud` strings appear anywhere in the file (PII exclusion guard)
    - `_spineEventCard` uses BADGE_COLORS[ev.type] with an `#8b949e` fallback
    - `_spineFuelCard` reads only `volume_litres`, `km_per_litre`, `odometer_km` — grep proof in verify block
    - renderDayPage scaffold line no longer contains the "Timeline spine placeholder" text (replaced with empty `<div id="day-spine"></div>`)
    - Browser check `/day/2026-05-27` (pre-rally, no data): #day-spine renders "Nothing logged yet for this day."
    - Browser check on a past day with events (use any historical date after 2026-05-27 post-rally): spine shows events + notes + fuel interleaved by time
    - Mobile viewport (375px): spine grid re-columns to 56px/20px/1fr, still readable
  </acceptance_criteria>
  <done>Timeline spine populates #day-spine with interleaved event / note / fuel / driver-change / agenda-meal / agenda-camping / stage bookends, all chronologically sorted. Empty day shows "Nothing logged yet."</done>
</task>

</tasks>

<verification>
Plan 19-06 complete when:
- All grep checks pass
- Browser check `/day/2026-05-27` (rally start, pre-data) shows spine with agenda meals + camping interleaved only
- Browser check on a day with events shows correct chronological ordering (events mixed with notes and fuel stops)
- No `cost_aud` or `price_aud` leak anywhere in file (PII guard)
- Event cards reuse BADGE_COLORS (red HIGH_G matches the legacy Videos tab)
</verification>

<success_criteria>
- NARR-03: Day page has a single timeline spine merging events + notes + fuel + driver changes + agenda markers + stage bookends, sorted by timestamp.
- D-06 single timeline realised (no per-kind tabs).
- D-07 interleave realised across all six sources.
</success_criteria>

<output>
After completion, create `.planning/phases/19-website-narrative-rebuild/19-06-SUMMARY.md`. Note any stage-bookend heuristic tweaks (future GPS-motion derivation stays out of scope).
</output>
