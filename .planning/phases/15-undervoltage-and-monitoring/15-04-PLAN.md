---
phase: 15-undervoltage-and-monitoring
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [MON-01, MON-02]
tags: [paperwork, home-ops, cleanup, mon-01, mon-02]

must_haves:
  truths:
    - "MON-01 is marked complete in REQUIREMENTS.md (line 56 changes from `- [ ]` to `- [x]`) — metrics are already flowing; only the checkbox flip is needed"
    - "MON-02 is marked complete in REQUIREMENTS.md (line 57 changes from `- [ ]` to `- [x]`) after the home-ops shitbox-mqtt-exporter directory is deleted"
    - "The home-ops directory apps/observability/shitbox-mqtt-exporter/ no longer exists"
    - "The commented line in apps/observability/kustomization.yaml referencing the deleted path is removed (optional but preferred)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "MON-01 and MON-02 marked complete"
      contains: "[x] **MON-01**"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "home-ops apps/observability/shitbox-mqtt-exporter (deleted)"
      via: "deletion proves MON-02 preconditions satisfied"
      pattern: "MON-02.*resolved"
---

<objective>
Close out two bookkeeping requirements that Phase 15 inherited but whose technical work is already done:

- **MON-01**: CPU temp, disk percent, and sync backlog metrics are already flowing to Prometheus (confirmed during Phase 11-14 work). The `insert_readings_batch` cpu_percent bug was fixed in Phase 11. Only the checkbox flip remains.
- **MON-02**: The `shitbox-mqtt-exporter` HelmRelease was replaced by direct Prometheus scrape in Phase 14. Its ks.yaml is already commented out in `apps/observability/kustomization.yaml:20`. The directory itself still sits in the repo as dead weight — delete it and the now-pointless comment.

Purpose: Stop carrying "resolved in spirit, not in the ledger" debt into v2.0 handover. One flip and one `rm -rf` — not enough to plan around, but enough to leave hanging would cost context next time someone greps for "shitbox-mqtt-exporter" and has to re-discover it's dead.

Output: REQUIREMENTS.md shows MON-01 and MON-02 checked. Home-ops repo has a commit deleting `apps/observability/shitbox-mqtt-exporter/` and the commented kustomization entry. Both commits land on main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-undervoltage-and-monitoring/15-CONTEXT.md
@.planning/phases/15-undervoltage-and-monitoring/15-RESEARCH.md

<read_first>
1. This plan touches TWO repos: `/Users/tgreen/dev/shitbox/` (for REQUIREMENTS.md) and `/Users/tgreen/dev/home-ops/` (for the directory deletion). Commits go to each repo independently.
2. Before deleting the home-ops directory, run `git log --oneline -- apps/observability/shitbox-mqtt-exporter/` in home-ops to confirm the path is actually tracked and not already half-removed.
3. **Human verification checkpoint required**: after the home-ops changes are staged, pause for user confirmation before commit + push. This is external infra — the user owns the blast radius.
4. MON-01 has already been verified operationally. Do NOT re-run the verification from Phase 11 (`curl` against Prometheus, `SELECT cpu_percent` from the SQLite schema, etc.) — trust the prior evidence. The living project doc at `~/Brain/projects/shitbox-rally-2026.md` and REQUIREMENTS.md Phase 11-14 SUMMARYs already record the proof. If that feels uncomfortable, spend 30 seconds `gh api` against Prometheus before flipping the box — don't spend 30 minutes on it.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip MON-01 and MON-02 checkboxes in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Edit `.planning/REQUIREMENTS.md`:

    - Line 56: change `- [ ] **MON-01**: HLTH-01 closed — CPU temp, disk %, and sync backlog metrics confirmed reaching Prometheus end-to-end; `insert_readings_batch` cpu_percent bug fixed` to `- [x] **MON-01**: ...` (keep the rest of the line verbatim).
    - Line 57: change `- [ ] **MON-02**: Prometheus scrape job label conflict resolved (job: shitbox-mqtt-exporter no longer collides with shitbox metrics)` to `- [x] **MON-02**: ...`.

    Do NOT rewrite the descriptive text. Do NOT touch PWR-01, PWR-02, or MON-03 — those are closed by the other plans in this phase.

    Per D-12 (CONTEXT.md): "done is checking a box." No ceremony.
  </action>
  <verify>
    <automated>grep -c "^\- \[x\] \*\*MON-01\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -c "^\- \[x\] \*\*MON-02\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "^\- \[x\] \*\*MON-01\*\*" .planning/REQUIREMENTS.md` returns exactly one line
    - `grep "^\- \[x\] \*\*MON-02\*\*" .planning/REQUIREMENTS.md` returns exactly one line
    - PWR-01 and PWR-02 still show `- [ ]` (they're closed by 15-02, not this plan)
    - MON-03 still shows `- [ ]` (closed by 15-03 + 15-05)
  </acceptance_criteria>
  <done>REQUIREMENTS.md shows MON-01 and MON-02 checked; other Phase 15 items remain unchecked.</done>
</task>

<task type="auto">
  <name>Task 2: Delete shitbox-mqtt-exporter from home-ops</name>
  <files>/Users/tgreen/dev/home-ops/kubernetes/apps/observability/shitbox-mqtt-exporter/ (whole dir), /Users/tgreen/dev/home-ops/kubernetes/apps/observability/kustomization.yaml</files>
  <action>
    In the home-ops repo (NOT the shitbox repo — switch working directory):

    1. Delete the entire directory:
       ```bash
       rm -rf /Users/tgreen/dev/home-ops/kubernetes/apps/observability/shitbox-mqtt-exporter/
       ```
       This removes `ks.yaml`, `app/helmrelease.yaml`, `app/externalsecret.yaml`, and any other files under that path.

    2. Edit `/Users/tgreen/dev/home-ops/kubernetes/apps/observability/kustomization.yaml`:
       - Remove line 20: `  #- ./shitbox-mqtt-exporter/ks.yaml`
       - Leave all other lines unchanged (including the other commented lines like `#  - ./smartctl-exporter/ks.yaml` on line 18 and `#- ./overseerr-exporter/ks.yaml` on line 28 — those belong to other deferred work, not MON-02).

    3. Do NOT touch the active `mqtt-exporter` directory at `apps/observability/mqtt-exporter/` — that's a different thing (it's the Zigbee2MQTT bridge exporter, unrelated to shitbox).

    4. Run `git status` in the home-ops repo and confirm the changeset is exactly:
       - deleted: `apps/observability/shitbox-mqtt-exporter/app/externalsecret.yaml`
       - deleted: `apps/observability/shitbox-mqtt-exporter/app/helmrelease.yaml`
       - deleted: `apps/observability/shitbox-mqtt-exporter/ks.yaml`
       - modified: `apps/observability/kustomization.yaml` (one line removed)

    If anything else shows up (e.g. untracked local changes in an unrelated directory), STOP and surface it — do not commit blindly.

    Do NOT commit yet — the next checkpoint is for user confirmation.
  </action>
  <verify>
    <automated>test ! -d /Users/tgreen/dev/home-ops/kubernetes/apps/observability/shitbox-mqtt-exporter &amp;&amp; ! grep -q "shitbox-mqtt-exporter" /Users/tgreen/dev/home-ops/kubernetes/apps/observability/kustomization.yaml</automated>
  </verify>
  <acceptance_criteria>
    - Directory `apps/observability/shitbox-mqtt-exporter/` does not exist in home-ops
    - `grep "shitbox-mqtt-exporter" apps/observability/kustomization.yaml` returns nothing
    - `git status` in home-ops shows ONLY the expected 4-file changeset (3 deletions + 1 modification)
    - The active `apps/observability/mqtt-exporter/` directory is UNCHANGED
  </acceptance_criteria>
  <done>home-ops working tree has the four expected changes staged; no commit yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (Checkpoint): Human verification of cross-repo changeset</name>
  <action>
    Pause before committing or pushing anything. Present the staged changes to the user and wait for explicit approval. The user owns the blast radius — home-ops is push-to-deploy, so any accidental diff reaches the home cluster on next Flux reconcile.
  </action>
  <what-built>
    - shitbox repo: REQUIREMENTS.md lines 56-57 flipped to `[x]` for MON-01 and MON-02
    - home-ops repo: `apps/observability/shitbox-mqtt-exporter/` deleted (3 files) and one commented line removed from `kustomization.yaml`
  </what-built>
  <how-to-verify>
    From the shitbox repo:
    1. Run `git -C /Users/tgreen/dev/shitbox diff .planning/REQUIREMENTS.md` and confirm you see ONLY the two checkbox flips (no stray description changes).

    From the home-ops repo:
    2. Run `git -C /Users/tgreen/dev/home-ops status` and confirm the 4-file changeset matches Task 2's expected list.
    3. Run `git -C /Users/tgreen/dev/home-ops diff apps/observability/kustomization.yaml` and confirm only `  #- ./shitbox-mqtt-exporter/ks.yaml` is removed — nothing else.
    4. Flux sanity check (optional but recommended): `kubectl get kustomizations -n flux-system | grep shitbox-mqtt-exporter` — should return nothing. If it still shows a Kustomization object, flux hasn't reconciled the prior comment-out yet; run `flux reconcile kustomization cluster-apps` after the push.

    Expected outcome: clean diff in both repos, no stray changes, mqtt-exporter (active) untouched.

    If the diff looks right, type "approved" to proceed with commit + push. If anything looks off (extra files modified, wrong line removed), describe it and I'll adjust before committing.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe what needs fixing</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit and push</name>
  <files>/Users/tgreen/dev/shitbox/.planning/REQUIREMENTS.md, /Users/tgreen/dev/home-ops/kubernetes/apps/observability/</files>
  <action>
    Two commits, two repos:

    1. **Shitbox repo** (working dir `/Users/tgreen/dev/shitbox`):
       ```bash
       gsd-sdk query commit "docs(15): close MON-01 and MON-02 in requirements" .planning/REQUIREMENTS.md
       ```
       If `gsd-sdk query commit` is unavailable, fall back to:
       ```bash
       git -C /Users/tgreen/dev/shitbox add .planning/REQUIREMENTS.md
       git -C /Users/tgreen/dev/shitbox commit -m "docs(15): close MON-01 and MON-02 in requirements"
       ```
       Do NOT push the shitbox repo unless the user asks.

    2. **Home-ops repo** (working dir `/Users/tgreen/dev/home-ops`):
       ```bash
       git -C /Users/tgreen/dev/home-ops add kubernetes/apps/observability/kustomization.yaml
       git -C /Users/tgreen/dev/home-ops add -A kubernetes/apps/observability/shitbox-mqtt-exporter/
       git -C /Users/tgreen/dev/home-ops commit -m "chore(observability): remove dead shitbox-mqtt-exporter"
       ```
       Then push so Flux picks up the removal:
       ```bash
       git -C /Users/tgreen/dev/home-ops push
       ```
       Home-ops is push-to-deploy — the push IS the deployment.

    After both commits, run `git -C /Users/tgreen/dev/shitbox status` and `git -C /Users/tgreen/dev/home-ops status` to confirm both trees are clean.

    Co-author trailer NOT required for home-ops (different project; it has its own conventions). The shitbox commit should follow the project's conventional-commit style (`docs(15): ...`). No Claude co-author trailer unless the user's shitbox `git log` already has them.
  </action>
  <verify>
    <automated>git -C /Users/tgreen/dev/shitbox log --oneline -1 .planning/REQUIREMENTS.md | grep -q "MON-01" &amp;&amp; git -C /Users/tgreen/dev/home-ops log --oneline -1 -- kubernetes/apps/observability/ | grep -q "shitbox-mqtt-exporter"</automated>
  </verify>
  <acceptance_criteria>
    - Shitbox repo: HEAD commit touches REQUIREMENTS.md with a subject mentioning MON-01
    - Home-ops repo: HEAD commit touches `kubernetes/apps/observability/` with a subject mentioning `shitbox-mqtt-exporter`
    - Home-ops push succeeded (remote HEAD matches local HEAD on the pushed branch)
    - Both working trees clean (`git status` reports nothing to commit)
  </acceptance_criteria>
  <done>Both repos committed; home-ops pushed; Flux will drop the shitbox-mqtt-exporter Kustomization on next reconcile (already gone from kustomization.yaml — this commit just removes the asset files behind it).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shitbox repo → home-ops repo | Two separate codebases with separate deployment consequences; cross-repo changes must stage correctly |
| home-ops working tree → Flux | Push-to-deploy: an unintended file change would deploy immediately to the home cluster |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Tampering | home-ops kustomization.yaml | mitigate | Task 2 pins the exact line number + content to remove; Task 3 checkpoint requires human `git diff` review before commit |
| T-15-04-02 | Denial of Service | Flux reconciliation after push | accept | Flux will notice the removed ks.yaml reference and drop the Kustomization; this is the intended outcome. If reconciliation fails, `flux get kustomization` will flag it and the user can roll back with `git revert` |
| T-15-04-03 | Information Disclosure | externalsecret.yaml deletion | accept | The ExternalSecret referenced a 1Password secret for an already-disabled HelmRelease; the secret itself stays in 1Password. Deleting the manifest cannot leak credentials |
| T-15-04-04 | Repudiation | REQUIREMENTS.md flip without re-verification | accept | Per D-12 and `<read_first>` point 4, operational verification already happened in Phases 11-14 and is recorded in the living project doc. Re-verification is context spend for zero new information |
</threat_model>

<verification>
- `grep "^\- \[x\] \*\*MON-01\*\*" .planning/REQUIREMENTS.md` → exactly one line
- `grep "^\- \[x\] \*\*MON-02\*\*" .planning/REQUIREMENTS.md` → exactly one line
- `test -d /Users/tgreen/dev/home-ops/kubernetes/apps/observability/shitbox-mqtt-exporter` → fails (directory gone)
- `grep "shitbox-mqtt-exporter" /Users/tgreen/dev/home-ops/kubernetes/apps/observability/kustomization.yaml` → no output
- Both repos have clean `git status`
- Home-ops has been pushed; Flux reconciles within ~1-2 minutes and the (already inactive) Kustomization is fully dropped
</verification>

<success_criteria>
- MON-01 and MON-02 carry `[x]` in REQUIREMENTS.md
- home-ops repo no longer carries the dead `shitbox-mqtt-exporter` directory or the commented kustomization.yaml entry
- Both commits landed cleanly; home-ops pushed
- No other files changed in either repo (strict diff)
</success_criteria>

<output>
After completion, create `.planning/phases/15-undervoltage-and-monitoring/15-04-SUMMARY.md` with:
- Checkboxes flipped (confirm MON-01, MON-02 marked done)
- Home-ops changeset summary (3 deletions + 1 modification)
- Commit hashes from both repos
- Any surprises (e.g. flux reconcile status, orphaned objects in the cluster)
</output>
