---
phase: 12
slug: schema-foundation-and-logbook-api
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-09
---

# Phase 12 — UI Design Contract

> Visual and interaction contract for Phase 12: Schema Foundation and Logbook API.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (plain HTML + Alpine.js + Tailwind CSS CDN) |
| Icon library | none (text labels only, consistent with existing dashboard) |
| Font | system-ui stack: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif |

**Source:** Detected from `src/shitbox/dashboard/static/index.html`. No build step, no component registry.
This is a plain HTML SPA — the shadcn gate does not apply (not a React/Next.js/Vite project).

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge padding (px-1 py-0.5) |
| sm | 8px | Compact element spacing, badge padding (px-2) |
| md | 16px | Default element spacing, card inner padding (p-4) |
| lg | 24px | Section padding, header/footer horizontal padding (px-6) |
| xl | 32px | Layout gaps (gap-4 in grid = 16px; max gap between sections) |
| 2xl | 48px | Not used in dashboard; available for modal vertical rhythm |
| 3xl | 64px | Not used; modal max-height breakpoint reference only |

Exceptions:

- Top bar fixed height: 80px (source: existing `header` style)
- Bottom event strip fixed height: 88px (source: existing `footer` style)
- Modal overlay: min-height 320px, max-height 80vh
- Input fields: min-height 44px (keyboard + trackpad use; no touch targets needed but 44px improves visual clarity on 7" display)

**Source:** Detected from `index.html` inline styles and Tailwind class usage.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px | 400 (regular) | 1.5 |
| Label / badge | 12px | 600 (semibold) | 1.0 |
| Modal heading | 20px | 600 (semibold) | 1.2 |
| Numeric display | 36px (text-4xl) | 600 (semibold) | 1.0 |

Notes:

- The `.num` class applies `font-variant-numeric: tabular-nums; font-weight: 600` — carry this into any numeric readouts inside modals (e.g. displaying GPS coordinates or fuel volume).
- `text-sm text-gray-400` (14px, regular, muted) is the established pattern for field labels above data values. Use this for form field labels inside the modal.
- Two weights only: regular (400) for body text and semibold (600) for labels, headings, numbers, and CTAs.

**Source:** Detected from `index.html` class patterns and `.num` / `.badge` CSS rules.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0d1117` | Page background, map background, modal backdrop |
| Secondary (30%) | `#161b22` | Card surfaces, modal panel background |
| Border/divider | `#21262d` | Card borders, modal border, input borders |
| Body text | `#e6edf3` | Primary text everywhere |
| Muted text | `#8b949e` | Labels, secondary info, empty states |
| Accent (10%) | `#1f6feb` | Primary CTA button background, active focus ring |
| Warning | `#ca8a04` | GPS stale warning text and icon |
| Destructive | `#da3633` | Stale indicator text (`.stale`), no destructive actions in this phase |

Accent reserved for:

- "Save Note" CTA button (background)
- "Log Fuel Stop" CTA button (background)
- Input focus ring (border-color on focus)

The warning colour (`#ca8a04`) is reserved for the GPS staleness warning only: "Location from X minutes ago" message displayed inside the modal before submission when `gps_stale` would be true.

No destructive actions exist in this phase. The cancel/close button uses a neutral ghost style (no background, `#8b949e` text, hover: `#21262d` background).

**Source:** All hex values detected directly from `index.html` inline styles and Tailwind overrides.

---

## Modal Structure Contract

This section is specific to Phase 12. Both modals share the same structural pattern.

### Overlay

- Full-screen semi-transparent backdrop: `background: rgba(13, 17, 23, 0.85)` (dominant at 85% opacity)
- Modal panel: centred vertically and horizontally, max-width 480px, background `#161b22`, border `1px solid #21262d`, border-radius 8px, padding 24px
- `x-show` / `x-transition` (Alpine.js) for enter/leave animations — fade opacity 0 to 1 over 150ms
- Live telemetry dashboard remains mounted and running behind the backdrop (SSE streams continue)
- ESC key closes the modal (Alpine.js `@keydown.escape.window` on each modal component)

### Trigger Buttons

Both trigger buttons sit in the existing dashboard header bar (alongside GPS badge and sync badge).

| Button | Label | Placement | Style |
|--------|-------|-----------|-------|
| Note trigger | `+ Note` | Top bar, right of "Driver: —" | `badge` class, background `#161b22`, border `1px solid #21262d`, text `#e6edf3`, hover background `#21262d` |
| Fuel trigger | `+ Fuel` | Top bar, right of `+ Note` | Same style as Note trigger |

Both buttons use the existing `.badge` class as a base, with cursor: pointer added. No filled colour — neutral ghost style to avoid competing with status badges.

### Field Note Modal

Fields (in order):

1. **Note body** (required): `<textarea>`, 4 rows, full width, placeholder `"What happened?"`, font 16px/400, background `#0d1117`, border `#21262d`, focus border `#1f6feb`, border-radius 4px, padding 12px
2. **Pin to event** (optional): `<select>` populated from the last 10 events in the SSE events array. Default option: `"No event pin"`. Font 16px/400, same styling as textarea. If no events exist, field is disabled with placeholder `"No recent events"`.
3. **GPS location line** (read-only display, not a form field): Shows current GPS status below the fields. Two states:
   - Current fix: `"Location captured (3D fix)"` — muted text `#8b949e`, font 14px
   - Stale position: `"Location from X minutes ago"` — warning colour `#ca8a04`, font 14px, weight 600. This appears as a warning, not an error — the user can still submit.

Actions (bottom row, right-aligned):

- Cancel: ghost button, label `"Cancel"`, text `#8b949e`, no background, hover background `#21262d`, padding 8px 16px, border-radius 4px
- Submit: filled button, label `"Save Note"`, background `#1f6feb`, text `#fff`, padding 8px 16px, border-radius 4px, hover background `#388bfd` (lighter blue), disabled state: background `#21262d`, text `#8b949e`, cursor not-allowed

### Fuel Stop Modal

Fields (in order):

1. **Volume in litres** (required): `<input type="number" step="0.1" min="0">`, full width, placeholder `"Litres"`, font 16px/400, same styling as note textarea. Value must be > 0 to enable submit.
2. **Cost (optional, private)**: `<input type="number" step="0.01" min="0">`, full width, placeholder `"Cost (AUD) — private, never synced"`, same styling. The placeholder explicitly states this is private. Field is optional.
3. **Odometer (optional)**: `<input type="number" step="1" min="0">`, full width, placeholder `"Odometer (km) — optional"`, same styling.
4. **GPS location line** (same read-only display as Note modal, same two states and styling).

Actions: identical pattern to Note modal. Submit label: `"Log Fuel Stop"`.

### Loading / Saving State

- While the POST is in flight: disable both action buttons, change submit button text to `"Saving..."`, cursor `wait` on button.
- On success: close modal, show a transient confirmation badge in the top bar for 3 seconds: `"Note saved"` or `"Fuel stop logged"`, background `#238636` (green), text `#fff`, same `.badge` class. Alpine `setTimeout` dismiss.
- On API error: keep modal open, show error message below the form fields (above the action buttons): `"Could not save — try again"`, text `#da3633`, font 14px.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Note trigger button | `+ Note` |
| Fuel trigger button | `+ Fuel` |
| Note modal heading | `Field Note` |
| Fuel modal heading | `Fuel Stop` |
| Note body placeholder | `What happened?` |
| Volume placeholder | `Litres` |
| Cost placeholder | `Cost (AUD) — private, never synced` |
| Odometer placeholder | `Odometer (km) — optional` |
| Event pin default option | `No event pin` |
| Event pin disabled | `No recent events` |
| GPS current fix | `Location captured (3D fix)` |
| GPS stale warning | `Location from X minutes ago` (X = integer minutes) |
| Primary CTA (notes) | `Save Note` |
| Primary CTA (fuel) | `Log Fuel Stop` |
| Cancel CTA | `Cancel` |
| Saving state | `Saving...` |
| Success confirmation (notes) | `Note saved` |
| Success confirmation (fuel) | `Fuel stop logged` |
| Error state | `Could not save — try again` |
| Event strip empty state | `No events yet` (pre-existing, unchanged) |

No destructive actions in this phase. No confirmation dialogs required.

**Source:** CONTEXT.md D-06 (GPS stale warning copy), REQUIREMENTS.md NOTE-01/NOTE-02/FUEL-01 (form fields). Placeholder copy and CTA labels are Claude's discretion per CONTEXT.md.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| None | Not applicable | Not applicable — no shadcn, no component registry in this stack |

No third-party component registries. All UI is hand-authored Alpine.js + Tailwind inline in `index.html`, consistent with the existing pattern.

---

## Interaction Summary

| Interaction | Mechanism | Source |
|-------------|-----------|--------|
| Open Note modal | Click `+ Note` in header | Alpine `x-on:click`, sets `showNoteModal = true` |
| Open Fuel modal | Click `+ Fuel` in header | Alpine `x-on:click`, sets `showFuelModal = true` |
| Close modal | Click Cancel or press ESC | `@keydown.escape.window`, `x-on:click` on Cancel |
| Submit note | Click `Save Note` | `x-on:click.prevent` → `fetch('/api/notes', {method:'POST'})` |
| Submit fuel stop | Click `Log Fuel Stop` | `x-on:click.prevent` → `fetch('/api/fuel', {method:'POST'})` |
| GPS stale warning | Reactive display | Alpine reactive property `gpsStaleMinutes`, shown `x-show="gpsStaleMinutes > 0"` |
| Dismiss success badge | Auto-dismiss | `setTimeout` 3000ms |
| Populate event pin select | Reactive | Bound to existing `events` array in Alpine data |

Input method: USB keyboard and trackpad. No touch targets required. No on-screen keyboard.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
